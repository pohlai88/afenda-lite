interface:
  display_name: "Afenda Elite Kernel"
  short_description: "Build and cut over semantic package kernels"
  default_prompt: "Use $afenda-elite-kernel to centralize one shared concept behind a durable capability surface, execute any authorized final cutover, and verify it with reproducible evidence."
