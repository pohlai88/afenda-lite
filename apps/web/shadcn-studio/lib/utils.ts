/**
 * DNA-local `cn` for Shadcn Studio CLI aliases (`@/shadcn-studio/lib/utils`).
 * Re-exports the product barrel helper — no parallel clsx stack in the app.
 */
export { cn } from "@afenda/ui-system";
