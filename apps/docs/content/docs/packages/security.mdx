---
title: "@afenda/security"
description: "Canonical, framework-neutral CSP, CORS, and security-header policy."
---

{/* This file is auto-generated by `pnpm --filter @afenda/docs generate:package-docs`. Do not edit. */}

<Callout type="info" title="Generated from packages">
  Source: `packages/runtime/security/` · regenerate with
  `pnpm --filter @afenda/docs generate:package-docs`.
</Callout>

## Package exports

| Import path | Export key |
| --- | --- |
| `@afenda/security` | `.` |

## Package README

Canonical, framework-neutral CSP, CORS, and security-header policy.

## Consume

Use the single package-root capability:

```ts
import { security } from "@afenda/security";

const baseline = security.headers.create();
const strict = security.headers.strict({ reportUri: "https://example.test/csp" });

const headers = new Headers();
security.headers.apply(headers, baseline);

const cors = { origins: ["https://www.nexuscanon.com"] } as const;
const corsHeaders = security.cors.project({
	config: cors,
	requestOrigin: request.headers.get("Origin"),
});
const preflight = security.cors.preflight({ request, config: cors });
```

The stable capability groups are `headers`, `csp`, and `cors`. Only types are additionally exported. There are no public subpaths.

## Framework boundary

The package emits neutral `{ name, value }` header entries. Framework adaptation stays at the application composition root:

```ts
// apps/web/next.config.ts
const nextHeaders = security.headers
	.create()
	.map(({ name, value }) => ({ key: name, value }));
```

No Next.js import, type, function name, or response shape belongs in this package.

## Ownership

| Owner | Meaning |
|---|---|
| `@afenda/security` | CSP directives/serialization, security-header policy, explicit-origin CORS policy and Fetch projections |
| `apps/web` | Next.js configuration adaptation and route-pipeline composition |
| `@afenda/http` | Correlation, generic Fetch pipeline, pagination, timing and quota header mechanics |
| `@afenda/auth` | Authentication/session policy |
| `@afenda/rate-limit` | Rate-limit decision and storage |

This package is a Rank-1 leaf with no `@afenda/*` runtime dependencies. It does not own RBAC, authentication, CSRF stores, rate limiting, audit, identity headers, or framework middleware. See [CONTRACT.md](https://github.com/pohlai88/afenda-lite/blob/main/packages/runtime/security/CONTRACT.md).

## Maintain

```bash
pnpm --filter @afenda/security lint
pnpm --filter @afenda/security typecheck
pnpm --filter @afenda/security test
pnpm check:security-boundary
pnpm test:security-boundary
```
