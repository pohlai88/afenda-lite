# HRM Enterprise Completion Roadmap

## Final verdict

**Continue with the existing `@afenda/human-resources` architecture. Do not redesign or split the package.**

**Program status (2026-07-25):** Phase **0–1 DONE** · Phase **2 partially complete** (talent sensitive enforcement closed; WFP/compensation runner consolidation open) · Phase **3 DONE** — Slices **3.1–3.8 DONE**; **290/290** mutations classified; event catalog **106/106** registry `domain_event` types; strict registry CI (**0** HR-AUD-06 exemptions) · Phase **4 partially complete** — Slices **4.1–4.5 DONE** (effective-truth classification · person/worker · employment · contract · assignment historical truth); **next: 4.6** organization truth.

The package already has a substantial domain kernel:

* 290 mutation commands
* 144 queries
* 106 HR mutation tables
* 99 permissions
* Memory and Drizzle store architecture
* strong time, leave, workforce-foundation and employee-relations foundations

The remaining work is a controlled completion program covering safety, parity, historical truth, product composition, integrations and operational readiness.

---

# Program objective

Complete an enterprise HRM that supports:

1. workforce identity and employment;
2. organization structure and assignments;
3. recruitment and hiring;
4. employee lifecycle;
5. leave and time;
6. compensation and benefits;
7. performance;
8. learning;
9. talent and succession;
10. compliance and employee relations;
11. workforce planning;
12. payroll handoff;
13. privacy, authorization and audit;
14. product Actions and operator workflows;
15. reporting, integration and production operations.

---

# Phase 0 — Authority and program control

## Objective

Establish one authoritative roadmap, remove stale findings and prevent architecture decisions from reopening during implementation.

## Slice 0.1 — Close completed findings — **DONE** (2026-07-24)

Marked **closed** in Scratch audit authority (`41` · `42` · `43` · `44` · `40` · `46`):

* `HR-ENT-ER-CASE-LIST-ACL` — **CLOSED**
* `HR-COREORG-CANDIDATE-CONSENT-ALIGN` — **CLOSED**
* `HR-OPS-OVERTIME-APPROVAL-AUTHORITY` — **CLOSED**
* `HR-COREORG-P0-001` — **CLOSED**
* `HR-OPS-P1-001` — **CLOSED**

**Evidence residual only (not an active blocker reopen):** Employee Relations Memory/Drizzle list ACL database parity.

These five IDs must not appear as open/next blockers. Slice 1.1 calendar fixtures **CLOSED** (`HR-OPS-P1-005`). Next vertical repair named in [`44-next-repair-mission.md`](erp/human-resources-enterprise-audit/44-next-repair-mission.md): `HR-OPS-LEAVE-EMISSION-REGISTRY` (Phase 0 exit complete; Slice 0.1–0.3 **DONE**).

## Slice 0.2 — Ratify architecture decisions — **DONE** (2026-07-24)

Ratified in Scratch audit authority ([`41-consolidated-conflict-register.md`](erp/human-resources-enterprise-audit/41-consolidated-conflict-register.md) · Tier 0 [`HR-AUD-04-DECISION-REGISTER`](erp/human-resources-enterprise-audit/43-repair-roadmap.md) **DONE**). Choices match OPEN-DECISION canonical recommendations (no new options). OPEN-DECISION-A1 recorded ratified without reopening CLOSED implementation.

| Decision                | Final authority                                         | OPEN-DECISION |
| ----------------------- | ------------------------------------------------------- | --- |
| Authorization           | One contextual authorization facade                     | OPEN-DECISION-02 **A** |
| Employee Relations ACL  | Specialized authorization policy under the facade       | OPEN-DECISION-02 **A** (ER plugin) |
| Privacy execution       | Platform-owned privacy service through HR port          | OPEN-DECISION-03 **A** |
| Candidate consent       | Durable HR-owned candidate fact                         | OPEN-DECISION-A1 **A** (CLOSED; do not reopen) |
| Organization dimensions | Master-data keys plus assignment snapshots              | OPEN-DECISION-04 **C** |
| Effective truth         | All tables classified; lineage applied selectively      | OPEN-DECISION-01 **B** then **A** |
| Hire process            | Documented saga first; atomic orchestration for enterprise production bar | OPEN-DECISION-A2 **B** then **A** |
| Workforce variance      | Approved plan versus actual employment/assignment facts | OPEN-DECISION-C1 (employment actuals) |
| Payroll money           | Decimal-string handoff with explicit scale and rounding | OPEN-DECISION-05 |

Register completeness (same Slice): OPEN-DECISION-A3 → document-only (HR-COREORG-STRUCTURE-ALIGN). Zero orphan OPEN-DECISION rows.

## Slice 0.3 — Refresh authority artifacts — **DONE** (2026-07-24)

Authority pack refreshed for Phase 0 exit (Scratch only; no product/package behavior):

* [`00-authority-map.md`](erp/human-resources-enterprise-audit/00-authority-map.md)
* [`40-enterprise-completeness-matrix.tsv`](erp/human-resources-enterprise-audit/40-enterprise-completeness-matrix.tsv)
* [`41-consolidated-conflict-register.md`](erp/human-resources-enterprise-audit/41-consolidated-conflict-register.md)
* [`42-five-axis-domain-scorecard.md`](erp/human-resources-enterprise-audit/42-five-axis-domain-scorecard.md)
* [`43-repair-roadmap.md`](erp/human-resources-enterprise-audit/43-repair-roadmap.md)
* [`44-next-repair-mission.md`](erp/human-resources-enterprise-audit/44-next-repair-mission.md) (active mission index)
* [`45-architecture-composition-and-dual-scores.md`](erp/human-resources-enterprise-audit/45-architecture-composition-and-dual-scores.md)
* [`46-dual-score-matrix.tsv`](erp/human-resources-enterprise-audit/46-dual-score-matrix.tsv)
* [`packages/erp/human-resources/README.md`](../packages/erp/human-resources/README.md) (count/status align only)

Aligned to Slice 0.1 **CLOSED** findings + Slice 0.2 **RATIFIED** OPEN-DECISION-01…05, A1…A3, C1. Disk inventory (2026-07-24): **286** commands · **141** queries · **99** permissions · **106** `hr_*` mutation / hard-tenant tables.

**Next vertical (Phase 1):** `HR-OPS-LEAVE-EMISSION-REGISTRY` — sole active mission queue in [`44`](erp/human-resources-enterprise-audit/44-next-repair-mission.md) (Slice 1.1 calendar fixtures **CLOSED**).

## Phase 0 exit criteria — **MET** (2026-07-24)

| Criterion | Status |
|---|---|
| No duplicated active findings | **Met** — Slice 0.1 closed IDs recorded closed; not listed as open/next blockers |
| No unresolved architecture decision without an owner | **Met** — Slice 0.2 ratified OPEN-DECISION-01…05, A1…A3, C1; each has owner mission |
| One active mission queue | **Met** — [`44-next-repair-mission.md`](erp/human-resources-enterprise-audit/44-next-repair-mission.md) names only `HR-OPS-LEAVE-EMISSION-REGISTRY` (after Slice 1.1 close) |
| Disk counts match documentation | **Met** — Scratch pack + package README = 286 / 141 / 99 / 106 |
| Completed missions no longer appear as blockers | **Met** — ER ACL, candidate consent, overtime authority, calendar fixtures removed from active blocker queues |

**Residual (non-blocking hygiene):** `docs-V2/_scratch/slice/enterprise.md` still cites 284 / 138 / 98 — tracked as HR-XCUT-P1-003; does not reopen Phase 0.

---

# Phase 1 — Full engineering baseline

## Objective

Make the complete package testable and green before broad product development.

## Slice 1.1 — Calendar resolution repair — **DONE** (2026-07-24)

### Mission

`HR-OPS-TIME-CALENDAR-RESOLUTION-FIXTURES` — **CLOSED** (`HR-OPS-P1-005`)

### Complete

* assignment-context fixture wiring;
* work-calendar scope precedence;
* legal-entity scope;
* department scope;
* employee override;
* employment calendar assignment;
* deterministic assignment resolution;
* calendar conflict error codes;
* calendar timezone resolution;
* Memory/Drizzle parity.

**Evidence:** `calendar-scope*` + `human-resources.time` unit green; `REQUIRE_DATABASE_TESTS=1` Memory/Drizzle parity green; package typecheck exit 0.

## Slice 1.2 — Lifecycle serialization parity — **DONE** (2026-07-24)

### Mission

`HR-COREORG-LIFECYCLE-SERIALIZE-PARITY` — **CLOSED** (`HR-COREORG-P2-002`)

### Complete

* normalize `createdAt`;
* normalize `updatedAt`;
* normalize transfer movement timestamps;
* enforce ISO datetime strings at public boundaries;
* test Memory and Drizzle equality.

**Evidence:** `EmploymentMovement.createdAt` / `updatedAt` = ISO datetime strings (Memory + Drizzle `mapMovement`); `human-resources.lifecycle.parity` transfer case green.

## Slice 1.3 — Workforce planning reservation repair — **DONE** (2026-07-24)

### Mission

`HR-ENT-WFP-RESERVATION-CONSUME` — **CLOSED** (`HR-GOV-P2-004`)

### Complete

* offer acceptance consumes the correct reservation;
* candidate fixture succeeds;
* duplicate consumption is rejected;
* released reservations cannot be consumed;
* cross-tenant reservation consumption is rejected.

**Evidence:** `consumes reservation on offer acceptance` + duplicate/released/cross-tenant consume reject unit tests green; package typecheck exit 0.

## Slice 1.4 — Test-environment parity — **DONE** (2026-07-24)

### Mission

`HR-OPS-TEST-ENV-PARITY` — Vitest `DATABASE_URL` resolution, unit/parity project split, CI fail-closed.

### Complete

* load `DATABASE_URL` consistently in Vitest (env injection + local `.env.local` via `resolveDatabaseUrlForTests`; CI env-only);
* shared HR gate `helpers/database-gate.ts` + parity setup `testing/setup-hr-parity-database.ts`;
* remove accidental drizzle skips that required `REQUIRE_DATABASE_TESTS` even when URL was available;
* fail closed when `CI` / `REQUIRE_DATABASE_TESTS=1` without `DATABASE_URL` (skip ≠ PASS);
* separate Vitest projects `human-resources-unit` (parallel) and `human-resources-parity` (serial);
* document local + CI commands in `testing/README.md` and package README.

**Evidence:** `pnpm test:hr:unit` exit 0; `REQUIRE_DATABASE_TESTS=1 pnpm test:hr:parity` exit 0 with `DATABASE_URL` available.

## Slice 1.5 — Test cleanup stability — **DONE** (2026-07-24)

### Complete

* `createNeonOrgTracker()` + standardized org registration across parity / concurrency suites;
* leave-concurrency + leave-failure-injection deterministic teardown via shared tracker;
* `neon-cleanup` delete order extended (`hr_worker` → `hr_employee` → `hr_person`);
* project-level parity timeouts only (30s test / 90s hook) documented in `testing/README.md`;
* no ad-hoc partial deletes for synthetic org fixtures.

**Evidence:** `REQUIRE_DATABASE_TESTS=1 pnpm test:hr:parity` exit 0; `REQUIRE_DATABASE_TESTS=1 pnpm --filter @afenda/human-resources test -- leave-concurrency time-policy-concurrency` exit 0; back-to-back parity run exit 0.

## Slice 1.6 — Full package gate — **DONE** (2026-07-24)

### Commands (recorded exit codes)

| Command | Exit | Summary |
|---|---|---|
| `pnpm --filter @afenda/human-resources lint` | **0** | 354 files, no fixes |
| `pnpm --filter @afenda/human-resources typecheck` | **0** | |
| `pnpm --filter @afenda/human-resources test` | **0** | 65 passed files · 525 passed · 125 skipped (Drizzle parity gated off without flag — expected) |
| `REQUIRE_DATABASE_TESTS=1 pnpm --filter @afenda/human-resources test` | **0** | 75 passed files · 649 passed · **1 skipped** · 650 total |
| `pnpm validate:modules` | **0** | manifests + registers + 21 negative fixtures |
| `pnpm audit:tenancy-nulls` | **0** | 199 hard tenant roots, 0 null org |

### Skip inventory (Phase 1 honesty)

| Skip | Required? | Reason |
|---|---|---|
| 125 tests (no `REQUIRE_DATABASE_TESTS`) | **No** | `describe.skipIf(!runDrizzleParity)` — inner loop by design (Slice 1.4) |
| 1 test under flag | **No** | `human-resources.organization.parity.test.ts` · `it.runIf(adapter === "memory")` · `"rolls back department create when audit port fails"` — memory-only audit-port rollback; Drizzle audits inside SQL CTE (comment on disk) |
| Hard `it.skip` | **Cleared** | Removed from `end-to-end-authorization-parity.test.ts` (empty Cross-Store stub + broken ER temporal park) |

**Blockers cleared this slice:** ER case-list ACL parity seed isolation (`employee-relations-case-list-acl.parity.test.ts` per-test org + runId). Neon `hr_candidate` consent columns present (0017 applied on branch).

**Next:** Phase 2 Slice 2.1 · active queue [`44-next-repair-mission.md`](erp/human-resources-enterprise-audit/44-next-repair-mission.md) → **HR-OPS-LEAVE-EMISSION-REGISTRY** remains sole vertical until Phase 2 auth facade opens.

## Phase 1 exit criteria — **MET** (2026-07-24)

| Criterion | Status |
|---|---|
| Lint exit 0 | **Met** |
| Typecheck exit 0 | **Met** |
| Unit tests exit 0 | **Met** |
| DB parity tests exit 0 | **Met** |
| No required suite skipped | **Met** — 1 intentional memory-only adapter skip; 0 hard `it.skip`; Drizzle describes active under flag |
| Module validation exit 0 | **Met** |
| Tenancy audit exit 0 | **Met** |

**Residual (non-blocking):** Phase 2 will add Drizzle-equivalent audit rollback coverage for organization create if desired — not a Phase 1 gate failure.

---

# Phase 2 — Authorization and privacy foundation

## Objective

Close the largest remaining enterprise safety gap.

**Status (2026-07-25):** **Partially complete** — facade + privacy composition + ER policy + privacy command surface (6/6) closed; talent sensitive **enforcement CLOSED** (Slice 2.3); WFP/compensation sensitive runners remain forward missions.

## Slice 2.1 — Unified authorization facade — **DONE** (2026-07-25)

### Mission

`HR-ENT-04-AUTH-PRIVACY` (auth facade half; privacy composition closed under same mission Slice 2.10)

### Implement one flow

```text
Manifest permission
→ contextual authorization
→ resource policy
→ actor/subject scope
→ field projection
→ command or query
```

### Complete (evidence)

* one public authorization entry — `authorizeHumanResourcesOperation` (+ `runAuthorizedHumanResourcesOperation` runners);
* manifest permission always evaluated — facade Step 2 before policy evaluate;
* sensitive policy always evaluated — exclusive `HUMAN_RESOURCES_AUTHORIZATION_POLICIES` resolve;
* resource context supplied where required — `resourceRequired` fail-closed;
* subject identity resolved where required — subject-scoped policies + `resolveActorEmployeeIdentity` helpers;
* field projection applied to sensitive reads — compensation/compliance/ER projection paths.

**Verify:** `pnpm --filter @afenda/human-resources test -- authorization-policy-registry contextual-authorization-privacy authorization-facade-boundary authorization-policy-coverage`

**Closes:** `HR-XCUT-P0-001` · implements ratified `OPEN-DECISION-02`. Talent runner consolidation = **CLOSED** Slice 2.3 (`HR-ENT-TALENT-SENSITIVE-ENFORCE`).

## Slice 2.2 — Register Employee Relations policy — **DONE** (2026-07-25)

`hr.employee-relations.case` policy plugin; list/get via `authorized-case-read.ts` with projection parity.

**Verify:** `pnpm --filter @afenda/human-resources test -- employee-relations-case-authorization-policy employee-relations-case-list-acl`

## Slice 2.3 — Talent-sensitive policy — **DONE** (2026-07-25)

Policies registered (`hr.talent-assessment` · `hr.talent-profile` · `hr.succession`). Talent runners route through authorization facade with per-operation `resolveResource`; sensitive reads use field projection + `includeSensitive`; employee-scoped lists via `runAuthorizedTalentSubjectListQuery`; load-then-authorize via `runAuthorizedTalentLoadedReadQuery` (`getCareerPlanById`). Ad-hoc `requireTalentProfileSensitiveRead` removed.

**Owner:** `HR-ENT-TALENT-SENSITIVE-ENFORCE` (**CLOSED**)

**Verify:**

```bash
pnpm --filter @afenda/human-resources typecheck
# Exit 0

pnpm --filter @afenda/human-resources test -- \
  sensitive-domain-policies human-resources.talent end-to-end-authorization talent-authorization-enforcement
# Exit 0 — 4 files · 42 passed · 3 skipped

REQUIRE_DATABASE_TESTS=1 pnpm --filter @afenda/human-resources test -- human-resources.talent.parity
# Exit 0 — 1 file · 5 passed · 3 skipped
```

## Slice 2.4 — Workforce-planning-sensitive policy — **PARTIAL** (classification DONE · enforcement open)

`hr.workforce-planning` policy + employee-actual field deny. WFP runner injects privileged parity shell.

**Owner:** `HR-ENT-WFP-SENSITIVE-ENFORCE`

## Slice 2.5 — Compensation-sensitive policy — **PARTIAL** (classification DONE · enforcement open)

`hr.compensation` policy + `COMPENSATION_FIELD_CLASSES` masking tested in isolation; domain runner uses parity shell.

**Owner:** `HR-ENT-COMP-SENSITIVE-ENFORCE`

## Slice 2.6 — Privacy adapter — **DONE** (2026-07-25)

`createHumanResourcesPrivacyPort()` wired in `apps/web/lib/erp/human-resources-command-options.ts`; platform privacy service; tenant-scoped audited ops.

**Verify:** `pnpm --filter @afenda/web test -- human-resources-privacy-port`

**Closes:** `HR-XCUT-P0-004` (composition). DSAR inventory depth = `HR-ENT-07-DSAR-DEPTH`.

## Slice 2.7 — Privacy command surface — **DONE** (6/6 · 2026-07-25)

| Command / query | Status |
|---|---|
| `exportHumanResourcesSubjectData` | **DONE** — returns `HumanResourcesSubjectExportBundle` via collector |
| `evaluateHumanResourcesAnonymization` | **DONE** |
| `evaluateHumanResourcesRetention` | **DONE** |
| `placeHumanResourcesLegalHold` | **DONE** |
| `releaseHumanResourcesLegalHold` | **DONE** |
| `anonymizeHumanResourcesSubject` | **DONE** |
| `getHumanResourcesPrivacyCase` | **DONE** — platform case-read port + HR query |

**Verify:** `pnpm --filter @afenda/human-resources test -- human-resources.privacy` · `pnpm --filter @afenda/web test -- human-resources-privacy-port`

## Slice 2.8 — Sensitive coverage gate — **PARTIAL** (classification DONE · bypass gate open)

Derived policy coverage (`authorization-policy-coverage.test.ts`) green; fail-closed unknown ops. Does not yet prove every sensitive runner invokes facade (subject-aware + parity-shell bypass).

**Verify:** `pnpm --filter @afenda/human-resources test -- authorization-policy-coverage authorization-facade-boundary`

## Phase 2 exit criteria

| Criterion | Status |
|---|---|
| One authorization facade | **Met** |
| Privacy adapter composed in production | **Met** |
| Cross-tenant privacy tests pass | **Met** |
| Employee Relations list/get parity passes | **Met** |
| Talent, headcount and compensation surfaces **classified** | **Met** |
| Talent sensitive reads **enforced without bypass** | **Met** (Slice 2.3 · 2026-07-25) |
| WFP and compensation **enforced without bypass** | **Not met** |
| No sensitive command bypass (package-wide) | **Not met** |
| No fixed manual sensitive-policy count | **Met** |
| Privacy command surface (roadmap list) | **6/6** — all commands including case query |

**Phase 2 verdict:** **Partially complete** — safety architecture landed; WFP/compensation runner consolidation remain; talent sensitive enforcement **closed** Slice 2.3.

---

# Phase 3 — Audit, event and correlation integrity

## Objective

Make every mutation observable and explicitly classified.

Mutation-emission registry coverage (2026-07-25): **290 / 290** commands classified (**100%**). Split: **179** `audit_only` · **111** `domain_event`. **13** domain packs composed (time via legacy + leave · workforce-foundation · core-organization · recruitment · lifecycle · governance · compensation · performance · learning · privacy). Audit authority: [`HR-AUD-06-COMPENSATION-PERFORMANCE-LEARNING`](erp/human-resources-enterprise-audit/HR-AUD-06-COMPENSATION-PERFORMANCE-LEARNING.md). Inventory fixture: `__tests__/fixtures/mutation-inventory.json` (regenerate via `scripts/generate-mutation-inventory.mjs`). Slices **3.1–3.8 DONE**.

## Slice 3.1 — Leave emission registry — **DONE** (2026-07-25)

### Mission

`HR-OPS-LEAVE-EMISSION-REGISTRY`

### Complete (evidence)

* `HUMAN_RESOURCES_LEAVE_COMMAND_IDS` — **18** leave mutations classified in `emissions/domains/leave.ts`
* composed into `HUMAN_RESOURCES_MUTATION_EMISSION_REGISTRY_RECORD`
* Drizzle leave executors emit via `planLeaveMutationOutboxEventType` (registry-gated)
* leave emission + emission-registry + correlation fixtures green

Covered:

* policy creation and amendment;
* policy publication and retirement;
* entitlement creation and amendment;
* entitlement adjustment;
* request submission;
* request approval;
* request rejection;
* request cancellation;
* request amendment.

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- leave-emission emission-registry correlation-integrity
pnpm --filter @afenda/human-resources typecheck
```

Time already provides the reference registry pattern with full command coverage.

## Slice 3.2 — Workforce-foundation emissions — **DONE** (2026-07-25)

### Complete (evidence)

* `HUMAN_RESOURCES_WORKFORCE_FOUNDATION_COMMAND_IDS` — **5** (person / worker)
* `HUMAN_RESOURCES_CORE_ORGANIZATION_COMMAND_IDS` — **23** (department / job / position / reporting-line / employment / contract / assignment)
* domain packs: `emissions/domains/workforce-foundation.ts` · `emissions/domains/core-organization.ts`
* drained from legacy classifications; matrix locked in `workforce-foundation-emission-registry.test.ts`
* correlation fixtures for workforce-foundation + core-organization `domain_event` commands

Classified:

* person;
* worker;
* employee;
* employment;
* contract;
* assignment;
* organization changes.

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- workforce-foundation-emission emission-registry correlation-integrity
pnpm --filter @afenda/human-resources typecheck
```

## Slice 3.3 — Recruitment emissions — **DONE** (2026-07-25)

### Mission

`HR-REC-CANDIDATE-CONSENT-LIFECYCLE` (3.3A) → Slice 3.3 recruitment emissions (3.3B)

### Complete (evidence)

* **3.3A:** durable `candidate.withdraw-consent` · `candidate.change-retention` (commands · schemas · store · Memory/Drizzle · `@afenda/events` · manifest)
* **3.3B:** `HUMAN_RESOURCES_RECRUITMENT_COMMAND_IDS` — **27** classified in `emissions/domains/recruitment.ts`
* Memory + Drizzle happy paths registry-gated (`appendRegistryGatedOutbox` / `planRecruitmentDrizzleOutbox`)
* transition helpers pass `config.command` as mutation `operation` (requisition · application · offer)
* event catalog delta in `@afenda/events` + manifest `events.emits`
* matrix lock: `recruitment-emission-registry.test.ts`; correlation fixtures for every recruitment `domain_event` command

Classified:

* requisition;
* candidate;
* application;
* interview;
* offer;
* offer acceptance;
* candidate consent withdrawal;
* candidate retention change.

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- recruitment-emission emission-registry correlation-integrity candidate-consent
pnpm --filter @afenda/events test
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — recruitment-emission/emission-registry/correlation/candidate-consent green; events 32 passed; typecheck clean
```

**Next:** Slice 3.4 — Lifecycle emissions.

## Slice 3.4 — Lifecycle emissions — **DONE** (2026-07-25)

### Mission

Lifecycle domain pack + effective-dated outbox payloads + rehire dual-outcome on `human-resources.employment.create`.

### Complete (evidence)

* `HUMAN_RESOURCES_LIFECYCLE_COMMAND_IDS` — **14** lifecycle mutations classified in `emissions/domains/lifecycle.ts`
* `employment.create` registry permits `employment.started.v1` **or** `employee.rehired.v1` (adapter emits exactly one with `effectiveOn: startsOn`)
* `@afenda/events` effective-dated payload schema + **5** new event types (`probation.extended.v1` · `probation.reviewed.v1` · `employee.confirmed.v1` · `employee.rehired.v1` · `clearance.completed.v1`)
* Memory + Drizzle: `effectiveOn` on transfer/termination/employment-start/rehire outbox payloads; probation extend/outcome · confirm · clearance domain events on happy paths
* matrix lock: `lifecycle-emission-registry.test.ts`; correlation fixtures for every lifecycle `domain_event` command + rehire branch + `effectiveOn` asserts

Classified:

* onboarding;
* probation;
* confirmation;
* transfer;
* termination;
* offboarding;
* clearance;
* rehire (via `employment.create`).

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- lifecycle-emission emission-registry correlation-integrity
pnpm --filter @afenda/events test
pnpm --filter @afenda/human-resources typecheck
```

**Next:** Slice 3.5 — Governance emissions.

## Slice 3.5 — Governance emissions — **DONE** (2026-07-25)

### Mission

Governance domain packs: employee relations · compliance · talent · workforce planning.

### Complete (evidence)

* `HUMAN_RESOURCES_EMPLOYEE_RELATIONS_COMMAND_IDS` — **15** · `HUMAN_RESOURCES_COMPLIANCE_COMMAND_IDS` — **19** · `HUMAN_RESOURCES_TALENT_COMMAND_IDS` — **32** · `HUMAN_RESOURCES_WORKFORCE_PLANNING_COMMAND_IDS` — **13** classified in `emissions/domains/{employee-relations,compliance,talent,workforce-planning}.ts`
* composed into `HUMAN_RESOURCES_MUTATION_EMISSION_REGISTRY_RECORD` (**228/290 ~79%** total classified; legacy drain **82 → 63**)
* **14** new §15-aligned event types in `@afenda/events` (ER assign/appeal/reopen · doc reject/expire · eligibility verify/renew/expire · talent profile/pool · WFP approve/reserve/release/consume)
* matrix lock: `governance-emission-registry.test.ts` · `emission-registry-infrastructure.test.ts`; emission-registry parity blocks for all four command-id packs
* correlation fixtures updated for compliance work-eligibility/document domain_event commands + ER assign/appeal/reopen + talent profile/pool + WFP approve/reserve/release/consume
* mutation inventory fixture synced (**228** classified · **62** unclassified)
* shipped **`37742a54`** on `main`

Classified:

* employee relations;
* compliance (document · work-eligibility · policy acknowledgement);
* talent (competency · profile · pool · career · succession);
* workforce planning (headcount plan · reservation lifecycle).

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- governance-emission emission-registry correlation-integrity emission-registry-infrastructure
pnpm --filter @afenda/events test
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — governance/emission-registry/correlation/infrastructure 63 passed; events 34 passed; typecheck clean
```

**Next:** Slice 3.6 — Compensation, performance and learning emissions (scaffold).

## Slice 3.6 — Compensation, performance and learning emissions — **DONE** (2026-07-25)

### Mission

`HR-OPS-COMP-PERF-LEARN-EMISSION-REGISTRY`

### Complete (classification evidence)

* audit artifact: [`HR-AUD-06-COMPENSATION-PERFORMANCE-LEARNING`](erp/human-resources-enterprise-audit/HR-AUD-06-COMPENSATION-PERFORMANCE-LEARNING.md)
* `HUMAN_RESOURCES_COMPENSATION_BENEFITS_COMMAND_IDS` — **18** · `HUMAN_RESOURCES_PERFORMANCE_COMMAND_IDS` — **30** · `HUMAN_RESOURCES_LEARNING_COMMAND_IDS` — **11** · privacy — **3**
* full `Record` domain packs: `emissions/domains/compensation.ts` · `performance.ts` · `learning.ts` · `privacy.ts`
* legacy reclass: `certification.expire` → `domain_event` · `certification.expiring.v1` (Memory + Drizzle outbox wired)
* classified count: **290/290**; **0** unclassified

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- slice-36 compensation-emission performance-emission learning-emission emission-registry correlation-integrity
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — slice-36 + emission-registry + correlation 92 passed; typecheck clean
```

## Slice 3.7 — Event catalog alignment — **DONE** (2026-07-25)

### Mission

`HR-ENT-EVENT-CATALOG-COMPLIANCE`

Ensure every domain event:

* exists in `@afenda/events`;
* has a schema;
* has a version;
* has an owner;
* has a projection or documented consumer;
* carries correlation and organization context.

### Delivered

| Artifact | Path |
|---|---|
| Event catalog types + entries | `packages/erp/human-resources/src/event-catalog/` |
| Classified catalog | `HUMAN_RESOURCES_EVENT_CATALOG` — **106** registry `domain_event` types |
| Package export | `@afenda/human-resources/event-catalog` |
| Parity test | `packages/erp/human-resources/__tests__/event-catalog-parity.test.ts` |
| Payload invariants | `packages/data-plane/events/__tests__/human-resources-schemas.test.ts` — `organizationId` + `correlationId` on all **106** schemas |
| Forward exclusions | `SLICE_36_FORWARD_DOMAIN_EVENT_TYPES` — **0** (HR-AUD-06 closed) |

### Verify

```bash
pnpm --filter @afenda/events test
pnpm --filter @afenda/human-resources test -- emission-registry manifest event-catalog
pnpm --filter @afenda/human-resources typecheck
```

## Slice 3.8 — Registry CI gate — **DONE** (2026-07-25)

**Status: DONE** — strict fail-closed CI gate for **290/290** classified commands; **0** `missing_command` exemptions.

Every mutating command must declare:

```ts
{
  commandId,
  emissionMode: "audit_only" | "domain_event",
  eventTypes: [],
  correlationRequired: true
}
```

Gate tests: `registry-ci-gate.test.ts` · `no-direct-outbox-emission.test.ts` · `emission-registry-infrastructure.test.ts`.

## Phase 3 exit criteria

| Criterion | Status |
|---|---|
| 100% mutation-command classification | **Met** — **290/290** (100%); HR-AUD-06 classification **DONE** |
| Slice 3.6 classified registry (command packs · Record registry · test locks) | **Met** — 18 + 30 + 11 + 3 commands classified · inventory alignment locked |
| Correlation coverage for classified `domain_event` commands | **Met** — all slices including compensation · performance · learning · certification.expire |
| No undocumented adapter events (closed domain packs) | **Met** — registry-gated executors across closed packs |
| Event catalog entries for emitted types | **Met** — **106/106** in `HUMAN_RESOURCES_EVENT_CATALOG` with owner + `documented_no_consumer` |
| Audit-only commands explicitly registered (closed packs) | **Met** |
| Registry parity enforced in CI | **Met** — strict **290/290** (`registry-ci-gate` · `no-direct-outbox-emission`); **0** exempt `missing_command` |

**Phase 3 verdict:** **DONE** — Slices **3.1 · 3.2 · 3.3 · 3.4 · 3.5 · 3.6 · 3.7 · 3.8 DONE** (2026-07-25); **290/290** classified · catalog **106/106** · strict registry CI with **0** HR-AUD-06 exemptions.

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- registry-ci-gate emission-registry event-catalog correlation-integrity slice-36
pnpm --filter @afenda/events test
pnpm --filter @afenda/human-resources typecheck
```

---

# Phase 4 — Historical truth and database integrity

## Objective

Make historical HR facts deterministic, auditable and concurrency-safe.

## Slice 4.1 — Effective-truth classification — **DONE** (2026-07-25)

### Mission

`HR-ENT-03-EFFECTIVE-TRUTH`

Classify all 106 mutation tables as:

1. effective definition;
2. bounded assignment;
3. versioned current fact;
4. append-only operational fact;
5. transactional state machine;
6. derived projection;
7. explicit exclusion.

**109/109** classified in `packages/erp/human-resources/src/effective-truth-classification.ts` with CI validator `validateEffectiveTruthClassificationRegister()`. Phase 3 adoption matrix (36 tables) remains scoped to temporal mechanism rows in `effective-truth-adoption.ts`.

## Slice 4.2 — Person and worker truth — **DONE** (2026-07-25)

Evidence:

* Migration `0027_hr_person_worker_history` — `hr_person_identity_version` + `hr_worker_classification_version` with backfill
* Commands unchanged: `human-resources.person.update`, `human-resources.worker.change-type`, `human-resources.worker.change-status`
* Queries: `human-resources.person.as-of`, `human-resources.worker.as-of`
* Verify: `pnpm --filter @afenda/human-resources test -- workforce-foundation foundation effective-truth`

Delivered:

* identity correction via close+succeed lineage (non-destructive);
* worker status/type history on one classification chain with predecessor/successor;
* `asOf` queries;
* Memory + Drizzle adapter parity;
* effective-truth adoption/classification reconciled for person, worker, and lineage tables.

## Slice 4.3 — Employment truth — **DONE** (2026-07-25)

Evidence:

* Migration `0028_hr_employment_status_history` — append-only `hr_employment_status_history` + `hr_employment` effective-range CHECK
* Commands: `human-resources.employment.create`, `human-resources.employment.amend`, `human-resources.employment.correct`
* Queries: `human-resources.employment.as-of`, `human-resources.employment.status-history.list`
* Verify: `pnpm --filter @afenda/human-resources test -- human-resources.core` · `pnpm --filter @afenda/human-resources typecheck`

Delivered:

* future-dated employment with as-of tenure resolution;
* terminated employment with lifecycle status history;
* rehire as new tenure row with `employee.rehired` emission;
* one open employment policy plus write-time overlap prevention;
* retroactive `correctEmployment` with required reason;
* queryable employment status history and status-as-of projection;
* Memory + Drizzle adapter parity including `finalizeTermination` history append.

## Slice 4.4 — Contract truth — **DONE** (2026-07-25)

Evidence:

* Migration `0029_hr_employment_contract_lineage` — `lineage_status`, supersession FKs, `reason_code`, `source_reference`; partial unique on active `reference_code`
* Commands: `human-resources.employment-contract.create`, `human-resources.employment-contract.correct`, `human-resources.employment-contract.supersede`
* Queries: `human-resources.employment-contract.get`, `human-resources.employment-contract.as-of`
* Events: `human-resources.employment-contract.changed.v1`, `human-resources.employment-contract.superseded.v1` (supersede also emits `created` for successor)
* Verify: `pnpm --filter @afenda/human-resources test -- human-resources.core` · `pnpm --filter @afenda/human-resources typecheck` · `pnpm --filter @afenda/db test -- hr-employment-contract-lineage`

Delivered:

* contract date validation and parent employment range containment;
* in-place `correctEmploymentContract` with required reason + source reference;
* non-destructive supersession with bidirectional lineage links;
* overlapping active-contract prevention and sequential non-overlapping create;
* as-of contract resolution including boundary dates;
* Memory + Drizzle adapter parity.

## Slice 4.5 — Assignment truth — **DONE** (2026-07-25)

Evidence:

* Migration `0031_hr_work_assignment_truth` — predecessor/successor assignment FKs, `transfer_movement_id`, `manager_employee_id_snapshot`, `work_calendar_id_snapshot`; `(organization_id, employment_id, starts_on)` as-of index
* Commands: `human-resources.assignment.create`, `human-resources.assignment.end`, `human-resources.assignment.transfer` (lineage wiring)
* Queries: `human-resources.assignment.get`, `human-resources.assignment.as-of`, `human-resources.employee.org-context.resolve` (stored snapshots only)
* Verify: `pnpm --filter @afenda/human-resources test -- human-resources.core human-resources.foundation` · `pnpm --filter @afenda/human-resources typecheck` · `pnpm --filter @afenda/db test -- hr-work-assignment-truth`

Delivered:

* assignment effective range with employment containment and write-time overlap rejection;
* transfer predecessor/successor lineage tied to `hr_employment_movement`;
* immutable manager, work-calendar, and organization dimension snapshots on assignment rows;
* public assignment as-of query and deterministic org-context without live manager/calendar drift;
* future assignment and ambiguous multi-match fail-closed behavior;
* Memory + Drizzle adapter parity.

## Slice 4.6 — Organization truth — **DONE** (2026-07-25)

Evidence:

* Migration `0032_hr_organization_structure_lineage` — `hr_department_structure_version`, `hr_job_definition_version`, `hr_position_definition_version` with backfill; reporting-line `supersedes_reporting_line_id` / `superseded_by_reporting_line_id`
* Commands: `human-resources.department.update`, `human-resources.job.update`, `human-resources.position.update`, `human-resources.reporting-line.replace` (close+succeed lineage wiring)
* Queries: `human-resources.department.as-of`, `human-resources.job.as-of`, `human-resources.position.as-of`, `human-resources.organization-tree.as-of`, `human-resources.employee.org-context.resolve` (position definition as-of for department)
* Verify: `pnpm --filter @afenda/human-resources test -- human-resources.organization position-occupancy human-resources.foundation` · `pnpm --filter @afenda/human-resources typecheck` · `pnpm --filter @afenda/db test -- hr-organization-structure-lineage`

Delivered:

* department, job, and position structure/definition lineage via close+succeed version segments (stable aggregate IDs);
* reporting-line predecessor/successor links on primary replace/close;
* position occupancy remains vacant | occupied with multi-occupant `CONFLICT` rejection;
* historical organization tree and org-context department resolution from position definition at `asOf`;
* Memory + Drizzle adapter parity;
* effective-truth classification/adoption reconciled for version tables and reporting-line supersession fields.

## Slice 4.7 — Leave overlap guard

### Mission

`HR-OPS-LEAVE-OVERLAP-GUARD`

Reject overlapping approved leave windows per employee.

Use:

* command-level pre-check;
* transaction-safe database protection where practical;
* parity and concurrency tests.

## Slice 4.8 — Attendance determinism

### Mission

`HR-OPS-TIME-SESSION-DETERMINISM`

Sort attendance events by:

```text
occurredAt
→ source sequence
→ event ID
```

## Slice 4.9 — Timezone and legal date

### Missions

* `HR-OPS-TIME-TIMEZONE-HANDOFF`
* `HR-OPS-TIME-AUTHORITY-EFFECTIVE-DATE`

Define:

* organization-local work date;
* calendar timezone;
* payroll handoff timezone;
* overtime authority effective date;
* cross-midnight behavior;
* daylight-saving behavior.

## Slice 4.10 — Database invariant migration

### Mission

`HR-COREORG-DB-INVARIANTS`

Add or document:

* start date ≤ end date;
* valid bounded ranges;
* active uniqueness;
* overlap protection;
* valid status transition support;
* idempotency uniqueness;
* successor uniqueness;
* organization-scoped foreign keys.

## Phase 4 exit criteria

* Every HR table classified — **DONE** (2026-07-25, 106/106 via `effective-truth-classification.ts`).
* Every historical aggregate supports `asOf` — **partial** (person · worker · employment · employment-contract · organization structure **DONE**; assignment open).
* No ambiguous active branch — **partial** (employment · employment-contract · organization structure overlap guards **DONE**; assignment open).
* Critical overlaps protected — **partial** (employment · employment-contract · organization occupancy **DONE**; leave · assignment open).
* Effective ranges validated — **partial** (Slices **4.2–4.4** · **4.6** **DONE**; database invariant migration open).
* Historical organization context deterministic — **DONE** (Slice **4.6**, 2026-07-25).
* Timezone contract published — open (Slice **4.9**).

---

# Phase 5 — Workforce foundation and organization completion

## Objective

Finish the core employee system that every other domain depends upon.

## Slice 5.1 — Person management

Complete:

* create;
* correct identity;
* legal name;
* preferred name;
* contact information;
* identifiers;
* privacy classification;
* duplicate detection;
* subject export.

## Slice 5.2 — Worker management

Complete:

* create worker;
* worker type;
* status change;
* contingent worker;
* employee linkage;
* effective history.

## Slice 5.3 — Employee management

Complete:

* employee number;
* employee status;
* organization entry;
* profile;
* manager view;
* self-service view;
* sensitive field masking.

## Slice 5.4 — Employment management

Complete:

* hire;
* rehire;
* suspend;
* reactivate;
* terminate;
* future-dated employment;
* employment history.

## Slice 5.5 — Employment contract management

Complete:

* create;
* amend;
* renew;
* supersede;
* end;
* query current;
* query historical.

## Slice 5.6 — Assignment management

Complete:

* create;
* transfer;
* end;
* future assignment;
* primary assignment;
* concurrent assignment;
* organization dimensions;
* manager and calendar context.

## Slice 5.7 — Organization management

Complete:

* departments;
* jobs;
* positions;
* position occupancy;
* reporting lines;
* legal entity;
* business unit;
* cost centre;
* location;
* work calendar.

## Slice 5.8 — Rehire semantics

### Mission

`HR-COREORG-REHIRE-SEMANTICS`

Ensure:

* active employee cannot be rehired;
* ended employee can be rehired;
* new employment record created;
* prior history preserved;
* semantic error code returned.

## Slice 5.9 — Core product Actions

Create:

* `hr-people.ts`
* `hr-employees.ts`
* `hr-employment.ts`
* `hr-assignments.ts`
* `hr-organization.ts`

## Phase 5 exit criteria

* Full employee administrative journey available.
* Current and historical employee views available.
* Transfer and rehire supported.
* Organization context resolves for any effective date.
* Core Actions tested.
* No domain logic duplicated in apps/web.

---

# Phase 6 — Recruitment and employee lifecycle completion

## Objective

Complete candidate-to-worker and joiner-to-leaver workflows.

## Slice 6.1 — Requisition

Complete:

* draft;
* submit;
* approve;
* open;
* suspend;
* close;
* cancel;
* headcount reservation;
* hiring-manager assignment.

## Slice 6.2 — Candidate

Complete:

* create;
* update;
* consent;
* retention;
* anonymization;
* duplicate detection;
* source attribution;
* candidate search.

## Slice 6.3 — Application

Complete:

* apply;
* stage movement;
* reject;
* withdraw;
* reopen;
* one open application constraint;
* reason history.

## Slice 6.4 — Interview

Complete:

* schedule;
* assign interviewer;
* evaluation;
* confidential notes;
* scorecard;
* decision;
* field projection.

## Slice 6.5 — Offer

Complete:

* draft;
* approve;
* issue;
* accept;
* reject;
* withdraw;
* expire;
* compensation proposal reference;
* reservation consumption.

## Slice 6.6 — Hire orchestration

### Mission

`HR-COREORG-HIRE-ORCHESTRATION`

Implement:

```text
accepted offer
→ person
→ employee
→ employment
→ worker
→ assignment
→ reservation consumption
→ onboarding case
```

First deliver a reliable saga with replay and compensation actions.

Then consider an atomic orchestration command.

## Slice 6.7 — Onboarding

Complete:

* onboarding case;
* task checklist;
* document requirements;
* work eligibility;
* equipment and access handoffs;
* orientation;
* completion.

## Slice 6.8 — Probation and confirmation

Complete:

* probation period;
* review;
* extension;
* confirmation;
* rejection;
* effective date;
* reason and evidence.

## Slice 6.9 — Transfer

Complete:

* assignment end;
* new assignment;
* employment movement;
* manager change;
* dimension snapshot;
* calendar change;
* audit and event.

## Slice 6.10 — Termination and offboarding

Complete:

* termination proposal;
* approval;
* effective termination;
* offboarding case;
* clearance;
* access revocation facts;
* final payroll handoff;
* rehire eligibility.

## Slice 6.11 — Recruitment and lifecycle Actions

Create:

* `hr-recruitment.ts`
* `hr-hiring.ts`
* `hr-onboarding.ts`
* `hr-lifecycle.ts`
* `hr-offboarding.ts`

## Phase 6 exit criteria

* Requisition-to-hire journey works.
* Offer acceptance creates a complete workforce record.
* Headcount reservation consumed once.
* Onboarding and offboarding tracked.
* Confidential interview and offer data protected.
* Recruitment and lifecycle parity green.

---

# Phase 7 — Leave and time completion

## Objective

Complete operational workforce management and payroll-ready time facts.

## Slice 7.1 — Leave policy administration

Complete:

* policy versioning;
* eligibility;
* accrual basis;
* negative balance;
* carry forward;
* expiry;
* publication;
* retirement.

## Slice 7.2 — Entitlements and balances

Complete:

* entitlement grant;
* adjustment;
* opening balance;
* accrual;
* consumption;
* restoration;
* expiration;
* historical ledger.

## Slice 7.3 — Leave request workflow

Complete:

* submit;
* amend;
* cancel;
* approve;
* reject;
* manager authority;
* self-approval prevention;
* overlap prevention;
* backdated leave.

## Slice 7.4 — Leave handoff permission

### Mission

`HR-OPS-LEAVE-HANDOFF-PERMISSION`

Add least-privilege:

```text
human-resources.leave.handoff.read
```

## Slice 7.5 — Leave product Actions

### Mission

`HR-ENT-07-PRODUCT-LEAVE-ACTIONS`

Create:

* request leave;
* cancel leave;
* view balance;
* manager approval;
* team absence;
* HR policy administration;
* payroll handoff read.

## Slice 7.6 — Calendar administration

Complete:

* calendars;
* holidays;
* calendar scope;
* employee assignments;
* precedence;
* overrides;
* historical resolution.

## Slice 7.7 — Shift management

Complete:

* shift definition;
* breaks;
* overnight shift;
* scheduling;
* assignment;
* schedule amendment;
* employee schedule query.

## Slice 7.8 — Attendance

Complete:

* attendance event;
* import batch;
* replay;
* session resolution;
* exceptions;
* adjustments;
* break waiver;
* manager review.

## Slice 7.9 — Timesheets

Complete:

* generation;
* entries;
* employee submission;
* manager review;
* approval;
* rejection;
* reopen;
* legal-minute allocation;
* leave-aware generation.

## Slice 7.10 — Overtime

Complete:

* request;
* authority resolution;
* approval;
* rejection;
* repeat-approval no-op;
* effective-date resolution;
* payroll handoff.

## Slice 7.11 — Production attendance connector

### Mission

`HR-ENT-14-ATTENDANCE-CONNECTOR`

Implement a real `AttendanceSourcePort` with:

* organization isolation;
* incremental cursor;
* idempotency;
* dry run;
* source sequence;
* error capture;
* retry.

## Phase 7 exit criteria

* Leave ESS/MSS workflow available.
* Calendar resolution green.
* Attendance import deterministic.
* Timesheet approval complete.
* Overtime authority enforced.
* Leave and time payroll handoffs tested.
* All leave/time mutations registered.

---

# Phase 8 — Compensation, benefits and payroll boundary

## Objective

Complete approved compensation facts without moving payroll calculations into HR.

## Slice 8.1 — Dedicated compensation audit

Create:

```text
HR-AUD-06-COMPENSATION-PERFORMANCE-LEARNING
```

Compensation is audited first because it is directly upstream of payroll.

The current scorecard records compensation, performance and learning as unaudited-depth domains.

## Slice 8.2 — Compensation grades and bands

Complete:

* grade;
* band;
* range minimum;
* midpoint;
* maximum;
* currency;
* effective history;
* progression rules.

## Slice 8.3 — Compensation agreement

Complete:

* base salary;
* pay frequency;
* effective dates;
* assignment scope;
* approval;
* amendment;
* future change;
* retroactive correction;
* confidentiality.

## Slice 8.4 — Compensation review

Complete:

* review cycle;
* proposal;
* budget check;
* manager recommendation;
* approval;
* effective compensation result;
* audit.

## Slice 8.5 — Benefits

Complete:

* benefit plan;
* eligibility;
* enrollment;
* waiver;
* dependent coverage;
* effective range;
* employee contribution;
* employer contribution.

## Slice 8.6 — Allowance and deduction ownership

Define clearly:

* HR owns entitlement or agreement;
* payroll owns calculation;
* accounting owns posting;
* payments owns disbursement.

## Slice 8.7 — Payroll handoff contract

Publish one shared contract with:

* organization;
* employee;
* employment;
* assignment;
* effective date;
* currency;
* amount;
* decimal scale;
* rounding mode;
* pay frequency;
* compensation components;
* leave facts;
* time facts;
* overtime facts;
* source version;
* approval evidence.

## Slice 8.8 — Payroll handoff parity

Test:

```text
HR approved compensation
→ payroll input parser
→ exact decimal preservation
→ exact effective-date preservation
```

## Slice 8.9 — Compensation Actions

Create:

* `hr-compensation.ts`
* `hr-benefits.ts`
* `hr-compensation-review.ts`

## Phase 8 exit criteria

* Compensation domain audited.
* Approved compensation history complete.
* Benefits enrollment complete.
* Payroll handoff contract published.
* HR and payroll parsing parity green.
* No gross-to-net logic inside HR.

---

# Phase 9 — Performance, learning and talent

## Objective

Complete development, evaluation and succession capabilities.

## Slice 9.1 — Performance audit

Verify:

* cycle;
* goal;
* review;
* rating;
* calibration;
* improvement plan;
* confidentiality;
* effective history;
* parity.

## Slice 9.2 — Performance cycles

Complete:

* draft;
* publish;
* open;
* close;
* review periods;
* eligible population;
* rating scale.

## Slice 9.3 — Goals

Complete:

* employee goal;
* manager goal;
* weight;
* progress;
* alignment;
* completion;
* evidence.

## Slice 9.4 — Performance reviews

Complete:

* self-review;
* manager review;
* reviewer chain;
* calibration;
* acknowledgement;
* final rating;
* field projection.

## Slice 9.5 — Improvement plans

Complete:

* initiate;
* objectives;
* milestones;
* review;
* complete;
* fail;
* extend;
* evidence.

## Slice 9.6 — Learning audit

Verify:

* course;
* session;
* assignment;
* completion;
* certification;
* expiry;
* parity;
* production Actions.

## Slice 9.7 — Learning management

Complete:

* course catalog;
* session;
* instructor;
* capacity;
* assignment;
* attendance;
* completion;
* certification;
* renewal;
* expiration.

## Slice 9.8 — Talent unit depth

### Mission

`HR-ENT-TALENT-UNIT-TESTS`

Add dedicated unit tests for:

* competency;
* assessment;
* profile;
* pool;
* career;
* succession;
* readiness staleness.

## Slice 9.9 — Competency management

Complete:

* competency library;
* job competency;
* employee assessment;
* proficiency;
* evidence;
* expiration.

## Slice 9.10 — Talent profiles and pools

Complete:

* profile;
* potential;
* mobility;
* critical-role readiness;
* talent pool;
* membership;
* review.

## Slice 9.11 — Career and succession

Complete:

* career plan;
* development actions;
* succession plan;
* candidates;
* readiness;
* approval;
* stale-readiness prevention.

## Slice 9.12 — Product Actions

Create:

* `hr-performance.ts`
* `hr-learning.ts`
* `hr-talent.ts`
* `hr-succession.ts`

## Phase 9 exit criteria

* Performance and learning audits complete.
* No scaffold-only aggregate claimed complete.
* Confidential reviews protected.
* Certifications historically durable.
* Talent unit and parity tests green.
* Product Actions available.

---

# Phase 10 — Compliance, employee relations and workforce planning

## Objective

Complete governance and workforce-risk operations.

## Slice 10.1 — Document requirements

Complete:

* requirement;
* applicability;
* publication;
* retirement;
* employee-document registration;
* verification;
* rejection;
* expiry.

## Slice 10.2 — Work eligibility

Complete:

* record;
* verify;
* suspend;
* renew;
* close;
* nearing-expiry detection;
* expired status;
* evidence reference.

## Slice 10.3 — Compliance expiry operations

### Mission

`HR-ENT-COMPLIANCE-EXPIRY-OPS`

Implement scheduled detection for:

* expiring documents;
* expiring work eligibility;
* overdue policy acknowledgement;
* expiring certifications.

## Slice 10.4 — Platform document boundary

### Mission

`HR-ENT-09-PLATFORM-DOCUMENT-BOUNDARY`

Define:

* HR stores document references only;
* platform owns binary storage;
* platform owns scanning;
* platform owns download authorization;
* retention is coordinated with HR privacy classification.

## Slice 10.5 — Employee Relations product slice

Complete:

* open case;
* list authorized cases;
* assign investigator;
* case event;
* case action;
* evidence;
* appeal;
* close and reopen;
* projected subject view.

## Slice 10.6 — Workforce plans

Complete:

* plan;
* line;
* approval;
* reservation;
* release;
* consume;
* availability;
* period;
* organization scope.

## Slice 10.7 — Workforce variance actuals

### Mission

`HR-ENT-WFP-VARIANCE-ACTUALS`

Calculate:

```text
approved headcount plan
versus
active employment and assignment facts
```

Do not label consumed reservations as actual headcount.

## Slice 10.8 — Governance product Actions

### Mission

`HR-ENT-12-GOV-PRODUCT-SLICE`

Create:

* `hr-compliance.ts`
* `hr-employee-relations.ts`
* `hr-workforce-planning.ts`

## Phase 10 exit criteria

* Compliance expiry proactive.
* Employee Relations workflow usable.
* Document ownership clear.
* Workforce variance employment-backed.
* Sensitive governance Actions protected.
* Governance parity suites green.

---

# Phase 11 — Product composition and user journeys

## Objective

Turn package capability into usable HRM workflows.

Do not create one Action per package command. Compose business journeys.

## Slice 11.1 — HR administration workspace

Provide:

* employee directory;
* employee record;
* employment;
* assignment;
* organization;
* lifecycle timeline;
* compliance alerts.

## Slice 11.2 — Employee self-service

Provide:

* profile;
* leave;
* attendance;
* timesheet;
* learning;
* goals;
* documents;
* acknowledgements.

## Slice 11.3 — Manager self-service

Provide:

* team directory;
* leave approvals;
* timesheet approvals;
* attendance exceptions;
* probation;
* performance;
* talent;
* staffing gaps.

## Slice 11.4 — Recruitment workspace

Provide:

* requisitions;
* pipeline;
* interviews;
* offers;
* candidate consent;
* hire conversion.

## Slice 11.5 — HR operations workspace

Provide:

* onboarding;
* transfer;
* termination;
* offboarding;
* compliance;
* cases;
* workforce plans.

## Slice 11.6 — Compensation workspace

Provide:

* grades;
* bands;
* reviews;
* benefits;
* employee compensation history;
* payroll handoff status.

## Slice 11.7 — Standard Action contract

Every Action must:

* use the package command;
* use the standard permission runner;
* supply actor context;
* supply organization context;
* supply correlation ID;
* supply idempotency key;
* map `Result` to `ActionResult`;
* avoid domain-rule duplication;
* invalidate only relevant paths.

## Phase 11 exit criteria

* Core HR workflows usable.
* ESS and MSS available.
* No package-domain rule duplicated in apps/web.
* Negative authorization tests exist.
* Product routes align with permission catalog.
* Product Score reaches at least 75.

---

# Phase 12 — Reporting, integrations and bulk operations

## Objective

Complete enterprise interoperability and decision support.

## Slice 12.1 — HR read models

Create read projections for:

* workforce headcount;
* turnover;
* hiring;
* leave;
* attendance;
* overtime;
* compensation;
* compliance;
* learning;
* performance;
* succession;
* workforce plan variance.

## Slice 12.2 — Bulk import framework

Support:

* dry run;
* row validation;
* partial rejection;
* idempotent replay;
* source reference;
* batch status;
* downloadable error file;
* audit trail.

Start with:

* employees;
* assignments;
* leave entitlements;
* attendance;
* compensation;
* learning assignments.

## Slice 12.3 — Bulk export

Support:

* permission-aware export;
* field projection;
* tenant isolation;
* date range;
* effective date;
* privacy logging.

## Slice 12.4 — Platform workflow integration

Project facts for:

* approvals;
* tasks;
* reminders;
* escalations;
* expiry notifications;
* onboarding;
* offboarding.

## Slice 12.5 — Search integration

Index only authorized, projection-safe data.

## Slice 12.6 — Payroll integration

Support:

* approved compensation;
* approved leave;
* approved time;
* overtime;
* corrections;
* replay;
* acknowledgement;
* rejection feedback.

## Slice 12.7 — Accounting and provisioning integration

Support facts for:

* payroll posting;
* cost-centre allocation;
* headcount budgeting;
* access provisioning;
* equipment assignment.

## Phase 12 exit criteria

* Reporting projections reconciled.
* Bulk imports support dry-run and replay.
* Exports honor privacy and authorization.
* Payroll handoffs acknowledged.
* Integration failures recoverable.
* Search contains no unauthorized sensitive fields.

---

# Phase 13 — Operational readiness and enterprise certification

## Objective

Convert a functionally complete HRM into a production-ready enterprise module.

## Slice 13.1 — Observability

Measure:

* command success and failure;
* authorization denials;
* privacy operations;
* outbox lag;
* event failures;
* parity failures;
* connector health;
* bulk-import errors;
* payroll handoff failures.

## Slice 13.2 — Reliability

Implement:

* retry policy;
* dead-letter handling;
* idempotent replay;
* connector cursor recovery;
* partial outage behavior;
* transaction failure recovery.

## Slice 13.3 — Security verification

Complete:

* cross-tenant penetration tests;
* sensitive-field tests;
* privilege escalation tests;
* export authorization tests;
* document-reference tests;
* audit tamper tests.

## Slice 13.4 — Privacy verification

Complete:

* subject export;
* retention;
* legal hold;
* anonymization;
* deletion decision;
* audit evidence;
* processor and subprocessor boundaries.

## Slice 13.5 — Performance verification

Test:

* employee lists;
* case lists;
* timesheet generation;
* attendance import;
* bulk employee import;
* workforce variance;
* payroll handoff;
* large tenant isolation.

## Slice 13.6 — Recovery and runbooks

Document:

* failed migration;
* stuck outbox;
* failed payroll handoff;
* failed attendance connector;
* privacy incident;
* data correction;
* tenant leakage response;
* rollback.

## Slice 13.7 — Documentation and module status

Update:

* README;
* architecture boundaries;
* package consumption;
* permission catalog;
* event catalog;
* privacy contract;
* payroll contract;
* operational runbooks;
* module lifecycle.

## Phase 13 exit criteria

* No open P0 or P1 production blocker.
* Full tests and DB parity green.
* Product Score at least 85.
* Coding Score at least 90.
* Security sign-off complete.
* Privacy sign-off complete.
* Performance targets met.
* Recovery runbooks tested.
* Module status can move from `scaffolded` to `active`.

---

# Master execution order

| Order | Phase    | Outcome                                    |
| ----: | -------- | ------------------------------------------ |
|     1 | Phase 0  | Authority and decisions frozen             |
|     2 | Phase 1  | Full engineering baseline green            |
|     3 | Phase 2  | Authorization and privacy complete         |
|     4 | Phase 3  | Audit and emission integrity complete      |
|     5 | Phase 4  | Historical and database truth complete     |
|     6 | Phase 5  | Workforce foundation product-complete      |
|     7 | Phase 6  | Recruitment and lifecycle complete         |
|     8 | Phase 7  | Leave and time complete                    |
|     9 | Phase 8  | Compensation and payroll boundary complete |
|    10 | Phase 9  | Performance, learning and talent complete  |
|    11 | Phase 10 | Compliance, ER and WFP complete            |
|    12 | Phase 11 | Product journeys complete                  |
|    13 | Phase 12 | Reporting and integrations complete        |
|    14 | Phase 13 | Enterprise production certification        |

---

# Non-negotiable execution rules

1. One slice per implementation mission.
2. No unrelated cleanup inside a domain mission.
3. Every mission records exact exit codes.
4. Unit tests precede database parity tests.
5. No domain is marked complete without Memory/Drizzle parity.
6. No sensitive surface is complete without field projection tests.
7. No mutation is complete without audit/emission classification.
8. No product Action may duplicate package business logic.
9. No payroll calculation is implemented inside HR.
10. No enterprise claim while compensation, performance or learning remains unaudited.
11. No required parity suite may silently skip.
12. Documentation must be updated when a mission closes.

---

# Final completion gates

## Gate 1 — Safe architecture

Required after Phase 4:

* no P0 safety defects;
* privacy composed;
* authorization unified;
* 100% mutation classification — **DONE** (2026-07-25, **106/106** via `effective-truth-classification.ts`);
* full package green;
* complete table classification — **DONE** (2026-07-25, **106/106**).

## Gate 2 — Functional HRM

Required after Phase 10:

* all HR domains complete;
* compensation and payroll boundary complete;
* ESS and MSS package capabilities complete;
* governance and workforce planning complete.

## Gate 3 — Enterprise HRM

Required after Phase 13:

* Product Score ≥ 85;
* Coding Score ≥ 90;
* no P0/P1 blockers;
* complete product journeys;
* integrations operational;
* security and privacy approved;
* observability and recovery proven.

**Execution starts with Phase 0 and proceeds in the stated order.**
