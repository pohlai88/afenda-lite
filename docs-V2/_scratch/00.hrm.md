# HRM Enterprise Completion Roadmap

## Final verdict

**Continue with the existing `@afenda/human-resources` architecture. Do not redesign or split the package.**

**Program status (2026-07-28):** Phases **0–12 are implemented and locally verified**. Phase **13 local implementation is complete**, including observability, reliability, security/privacy verification kernels, eight performance workloads and recovery harnesses/runbooks; **external security, privacy, performance and recovery certification remains open**. The module lifecycle remains `scaffolded` and this Scratch roadmap does not authorize promotion to `active`.

### Status reconciliation (2026-07-28)

| Phase | Current state | Remaining gate |
|---|---|---|
| 0–1 | **DONE** | None |
| 2 | **DONE / LOCALLY VERIFIED** | External security/privacy certification remains a Phase 13 gate |
| 3–6 | **DONE** | None |
| 7 | **DONE / LOCALLY VERIFIED** | None at the implementation-evidence layer |
| 8 | **DONE / LOCALLY VERIFIED** | External payroll operational certification remains a Phase 13 gate |
| 9 | **DONE / LOCALLY VERIFIED** | External performance/privacy certification remains a Phase 13 gate |
| 10 | **DONE / LOCALLY VERIFIED** | External security/privacy certification remains a Phase 13 gate |
| 11 | **DONE / LOCALLY VERIFIED** | Six composed journeys and standard Action contracts verified by **52** focused web tests |
| 12 | **DONE / LOCALLY VERIFIED** | Reporting, resumable imports/exports and integration workers verified; Phase 12–13 focused web evidence = **64** tests |
| 13 | **LOCAL IMPLEMENTATION COMPLETE / CERTIFICATION OPEN** | Execute and approve external security/privacy reviews, representative performance benchmarks and recovery drills; then open the controlled lifecycle lane |

**Current evidence snapshot (2026-07-28):** HR unit **961 passed / 1 skipped**; full live parity **37 files / 368 passed / 1 skipped**; Phase 11 web **52 passed**; Phase 12–13 focused web **64 passed**. Database migrations are applied through **0032**. The tenancy audit covers **245** hard-tenant roots: **243 audited**, with **2 unrelated pending-DDL skips**. Human Resources package, web, events and database typecheck lanes passed. Recorded skips remain visible and are not silently promoted into external certification evidence.

The package already has a substantial domain kernel:

* 348 mutation commands
* 198 queries
* 136 HR mutation / hard-tenant tables
* 111 permissions
* Memory and Drizzle store architecture
* strong time, leave, workforce-foundation and employee-relations foundations

The remaining work is a controlled completion program covering safety, parity, historical truth, product composition, integrations and operational readiness.

---

# Program objective

Complete an enterprise HRM that supports:

1. workforce identity and employment;
2. organization structure and assignments;
3. recruitment and hiring;
4. employee lifecycle;
5. leave and time;
6. compensation and benefits;
7. performance;
8. learning;
9. talent and succession;
10. compliance and employee relations;
11. workforce planning;
12. payroll handoff;
13. privacy, authorization and audit;
14. product Actions and operator workflows;
15. reporting, integration and production operations.

---

# Phase 0 — Authority and program control

## Objective

Establish one authoritative roadmap, remove stale findings and prevent architecture decisions from reopening during implementation.

## Slice 0.1 — Close completed findings — **DONE** (2026-07-24)

Marked **closed** in Scratch audit authority (`41` · `42` · `43` · `44` · `40` · `46`):

* `HR-ENT-ER-CASE-LIST-ACL` — **CLOSED**
* `HR-COREORG-CANDIDATE-CONSENT-ALIGN` — **CLOSED**
* `HR-OPS-OVERTIME-APPROVAL-AUTHORITY` — **CLOSED**
* `HR-COREORG-P0-001` — **CLOSED**
* `HR-OPS-P1-001` — **CLOSED**

**Evidence residual only (not an active blocker reopen):** Employee Relations Memory/Drizzle list ACL database parity.

These five IDs must not appear as open/next blockers. Slice 1.1 calendar fixtures **CLOSED** (`HR-OPS-P1-005`). Next vertical repair named in [`44-next-repair-mission.md`](erp/human-resources-enterprise-audit/44-next-repair-mission.md): `HR-OPS-LEAVE-EMISSION-REGISTRY` (Phase 0 exit complete; Slice 0.1–0.3 **DONE**).

## Slice 0.2 — Ratify architecture decisions — **DONE** (2026-07-24)

Ratified in Scratch audit authority ([`41-consolidated-conflict-register.md`](erp/human-resources-enterprise-audit/41-consolidated-conflict-register.md) · Tier 0 [`HR-AUD-04-DECISION-REGISTER`](erp/human-resources-enterprise-audit/43-repair-roadmap.md) **DONE**). Choices match OPEN-DECISION canonical recommendations (no new options). OPEN-DECISION-A1 recorded ratified without reopening CLOSED implementation.

| Decision                | Final authority                                         | OPEN-DECISION |
| ----------------------- | ------------------------------------------------------- | --- |
| Authorization           | One contextual authorization facade                     | OPEN-DECISION-02 **A** |
| Employee Relations ACL  | Specialized authorization policy under the facade       | OPEN-DECISION-02 **A** (ER plugin) |
| Privacy execution       | Platform-owned privacy service through HR port          | OPEN-DECISION-03 **A** |
| Candidate consent       | Durable HR-owned candidate fact                         | OPEN-DECISION-A1 **A** (CLOSED; do not reopen) |
| Organization dimensions | Master-data keys plus assignment snapshots              | OPEN-DECISION-04 **C** |
| Effective truth         | All tables classified; lineage applied selectively      | OPEN-DECISION-01 **B** then **A** |
| Hire process            | Documented saga first; atomic orchestration for enterprise production bar | OPEN-DECISION-A2 **B** then **A** |
| Workforce variance      | Approved plan versus actual employment/assignment facts | OPEN-DECISION-C1 (employment actuals) |
| Payroll money           | Decimal-string handoff with explicit scale and rounding | OPEN-DECISION-05 |

Register completeness (same Slice): OPEN-DECISION-A3 → document-only (HR-COREORG-STRUCTURE-ALIGN). Zero orphan OPEN-DECISION rows.

## Slice 0.3 — Refresh authority artifacts — **DONE** (2026-07-24)

Authority pack refreshed for Phase 0 exit (Scratch only; no product/package behavior):

* [`00-authority-map.md`](erp/human-resources-enterprise-audit/00-authority-map.md)
* [`40-enterprise-completeness-matrix.tsv`](erp/human-resources-enterprise-audit/40-enterprise-completeness-matrix.tsv)
* [`41-consolidated-conflict-register.md`](erp/human-resources-enterprise-audit/41-consolidated-conflict-register.md)
* [`42-five-axis-domain-scorecard.md`](erp/human-resources-enterprise-audit/42-five-axis-domain-scorecard.md)
* [`43-repair-roadmap.md`](erp/human-resources-enterprise-audit/43-repair-roadmap.md)
* [`44-next-repair-mission.md`](erp/human-resources-enterprise-audit/44-next-repair-mission.md) (active mission index)
* [`45-architecture-composition-and-dual-scores.md`](erp/human-resources-enterprise-audit/45-architecture-composition-and-dual-scores.md)
* [`46-dual-score-matrix.tsv`](erp/human-resources-enterprise-audit/46-dual-score-matrix.tsv)
* [`packages/erp/human-resources/README.md`](../../packages/erp/human-resources/README.md) (count/status align only)

Aligned to Slice 0.1 **CLOSED** findings + Slice 0.2 **RATIFIED** OPEN-DECISION-01…05, A1…A3, C1. Disk inventory (2026-07-24): **286** commands · **141** queries · **99** permissions · **106** `hr_*` mutation / hard-tenant tables.

**Next vertical (Phase 1):** `HR-OPS-LEAVE-EMISSION-REGISTRY` — sole active mission queue in [`44`](erp/human-resources-enterprise-audit/44-next-repair-mission.md) (Slice 1.1 calendar fixtures **CLOSED**).

## Phase 0 exit criteria — **MET** (2026-07-24)

| Criterion | Status |
|---|---|
| No duplicated active findings | **Met** — Slice 0.1 closed IDs recorded closed; not listed as open/next blockers |
| No unresolved architecture decision without an owner | **Met** — Slice 0.2 ratified OPEN-DECISION-01…05, A1…A3, C1; each has owner mission |
| One active mission queue | **Met** — [`44-next-repair-mission.md`](erp/human-resources-enterprise-audit/44-next-repair-mission.md) names only `HR-OPS-LEAVE-EMISSION-REGISTRY` (after Slice 1.1 close) |
| Disk counts match documentation | **Met** — Scratch pack + package README = 286 / 141 / 99 / 106 |
| Completed missions no longer appear as blockers | **Met** — ER ACL, candidate consent, overtime authority, calendar fixtures removed from active blocker queues |

**Residual (non-blocking hygiene):** `docs-V2/_scratch/slice/enterprise.md` still cites 284 / 138 / 98 — tracked as HR-XCUT-P1-003; does not reopen Phase 0.

---

# Phase 1 — Full engineering baseline

## Objective

Make the complete package testable and green before broad product development.

## Slice 1.1 — Calendar resolution repair — **DONE** (2026-07-24)

### Mission

`HR-OPS-TIME-CALENDAR-RESOLUTION-FIXTURES` — **CLOSED** (`HR-OPS-P1-005`)

### Complete

* assignment-context fixture wiring;
* work-calendar scope precedence;
* legal-entity scope;
* department scope;
* employee override;
* employment calendar assignment;
* deterministic assignment resolution;
* calendar conflict error codes;
* calendar timezone resolution;
* Memory/Drizzle parity.

**Evidence:** `calendar-scope*` + `human-resources.time` unit green; `REQUIRE_DATABASE_TESTS=1` Memory/Drizzle parity green; package typecheck exit 0.

## Slice 1.2 — Lifecycle serialization parity — **DONE** (2026-07-24)

### Mission

`HR-COREORG-LIFECYCLE-SERIALIZE-PARITY` — **CLOSED** (`HR-COREORG-P2-002`)

### Complete

* normalize `createdAt`;
* normalize `updatedAt`;
* normalize transfer movement timestamps;
* enforce ISO datetime strings at public boundaries;
* test Memory and Drizzle equality.

**Evidence:** `EmploymentMovement.createdAt` / `updatedAt` = ISO datetime strings (Memory + Drizzle `mapMovement`); `human-resources.lifecycle.parity` transfer case green.

## Slice 1.3 — Workforce planning reservation repair — **DONE** (2026-07-24)

### Mission

`HR-ENT-WFP-RESERVATION-CONSUME` — **CLOSED** (`HR-GOV-P2-004`)

### Complete

* offer acceptance consumes the correct reservation;
* candidate fixture succeeds;
* duplicate consumption is rejected;
* released reservations cannot be consumed;
* cross-tenant reservation consumption is rejected.

**Evidence:** `consumes reservation on offer acceptance` + duplicate/released/cross-tenant consume reject unit tests green; package typecheck exit 0.

## Slice 1.4 — Test-environment parity — **DONE** (2026-07-24)

### Mission

`HR-OPS-TEST-ENV-PARITY` — Vitest `DATABASE_URL` resolution, unit/parity project split, CI fail-closed.

### Complete

* load `DATABASE_URL` consistently in Vitest (env injection + local `.env.local` via `resolveDatabaseUrlForTests`; CI env-only);
* shared HR gate `helpers/database-gate.ts` + parity setup `testing/setup-hr-parity-database.ts`;
* remove accidental drizzle skips that required `REQUIRE_DATABASE_TESTS` even when URL was available;
* fail closed when `CI` / `REQUIRE_DATABASE_TESTS=1` without `DATABASE_URL` (skip ≠ PASS);
* separate Vitest projects `human-resources-unit` (parallel) and `human-resources-parity` (serial);
* document local + CI commands in `testing/README.md` and package README.

**Evidence:** `pnpm test:hr:unit` exit 0; `REQUIRE_DATABASE_TESTS=1 pnpm test:hr:parity` exit 0 with `DATABASE_URL` available.

## Slice 1.5 — Test cleanup stability — **DONE** (2026-07-24)

### Complete

* `createNeonOrgTracker()` + standardized org registration across parity / concurrency suites;
* leave-concurrency + leave-failure-injection deterministic teardown via shared tracker;
* `neon-cleanup` delete order extended (`hr_worker` → `hr_employee` → `hr_person`);
* project-level parity timeouts only (30s test / 90s hook) documented in `testing/README.md`;
* no ad-hoc partial deletes for synthetic org fixtures.

**Evidence:** `REQUIRE_DATABASE_TESTS=1 pnpm test:hr:parity` exit 0; `REQUIRE_DATABASE_TESTS=1 pnpm --filter @afenda/human-resources test -- leave-concurrency time-policy-concurrency` exit 0; back-to-back parity run exit 0.

## Slice 1.6 — Full package gate — **DONE** (2026-07-24)

### Commands (recorded exit codes)

| Command | Exit | Summary |
|---|---|---|
| `pnpm --filter @afenda/human-resources lint` | **0** | 354 files, no fixes |
| `pnpm --filter @afenda/human-resources typecheck` | **0** | |
| `pnpm --filter @afenda/human-resources test` | **0** | 65 passed files · 525 passed · 125 skipped (Drizzle parity gated off without flag — expected) |
| `REQUIRE_DATABASE_TESTS=1 pnpm --filter @afenda/human-resources test` | **0** | 75 passed files · 649 passed · **1 skipped** · 650 total |
| `pnpm validate:modules` | **0** | manifests + registers + 21 negative fixtures |
| `pnpm audit:tenancy-nulls` | **0** | 199 hard tenant roots, 0 null org |

### Skip inventory (Phase 1 honesty)

| Skip | Required? | Reason |
|---|---|---|
| 125 tests (no `REQUIRE_DATABASE_TESTS`) | **No** | `describe.skipIf(!runDrizzleParity)` — inner loop by design (Slice 1.4) |
| 1 test under flag | **No** | `human-resources.organization.parity.test.ts` · `it.runIf(adapter === "memory")` · `"rolls back department create when audit port fails"` — memory-only audit-port rollback; Drizzle audits inside SQL CTE (comment on disk) |
| Hard `it.skip` | **Cleared** | Removed from `end-to-end-authorization-parity.test.ts` (empty Cross-Store stub + broken ER temporal park) |

**Blockers cleared this slice:** ER case-list ACL parity seed isolation (`employee-relations-case-list-acl.parity.test.ts` per-test org + runId). Neon `hr_candidate` consent columns present (0017 applied on branch).

**Next:** Phase 2 Slice 2.1 · active queue [`44-next-repair-mission.md`](erp/human-resources-enterprise-audit/44-next-repair-mission.md) → **HR-OPS-LEAVE-EMISSION-REGISTRY** remains sole vertical until Phase 2 auth facade opens.

## Phase 1 exit criteria — **MET** (2026-07-24)

| Criterion | Status |
|---|---|
| Lint exit 0 | **Met** |
| Typecheck exit 0 | **Met** |
| Unit tests exit 0 | **Met** |
| DB parity tests exit 0 | **Met** |
| No required suite skipped | **Met** — 1 intentional memory-only adapter skip; 0 hard `it.skip`; Drizzle describes active under flag |
| Module validation exit 0 | **Met** |
| Tenancy audit exit 0 | **Met** |

**Residual (non-blocking):** Phase 2 will add Drizzle-equivalent audit rollback coverage for organization create if desired — not a Phase 1 gate failure.

---

# Phase 2 — Authorization and privacy foundation

## Objective

Close the largest remaining enterprise safety gap.

**Status (2026-07-28): DONE / LOCALLY VERIFIED.** The facade, privacy composition, ER policy, privacy command surface (6/6), and talent, workforce-planning and compensation sensitive enforcement are closed. External security and privacy certification remains a Phase 13 concern.

## Slice 2.1 — Unified authorization facade — **DONE** (2026-07-25)

### Mission

`HR-ENT-04-AUTH-PRIVACY` (auth facade half; privacy composition closed under same mission Slice 2.10)

### Implement one flow

```text
Manifest permission
→ contextual authorization
→ resource policy
→ actor/subject scope
→ field projection
→ command or query
```

### Complete (evidence)

* one public authorization entry — `authorizeHumanResourcesOperation` (+ `runAuthorizedHumanResourcesOperation` runners);
* manifest permission always evaluated — facade Step 2 before policy evaluate;
* sensitive policy always evaluated — exclusive `HUMAN_RESOURCES_AUTHORIZATION_POLICIES` resolve;
* resource context supplied where required — `resourceRequired` fail-closed;
* subject identity resolved where required — subject-scoped policies + `resolveActorEmployeeIdentity` helpers;
* field projection applied to sensitive reads — compensation/compliance/ER projection paths.

**Verify:** `pnpm --filter @afenda/human-resources test -- authorization-policy-registry contextual-authorization-privacy authorization-facade-boundary authorization-policy-coverage`

**Closes:** `HR-XCUT-P0-001` · implements ratified `OPEN-DECISION-02`. Talent runner consolidation = **CLOSED** Slice 2.3 (`HR-ENT-TALENT-SENSITIVE-ENFORCE`).

## Slice 2.2 — Register Employee Relations policy — **DONE** (2026-07-25)

`hr.employee-relations.case` policy plugin; list/get via `authorized-case-read.ts` with projection parity.

**Verify:** `pnpm --filter @afenda/human-resources test -- employee-relations-case-authorization-policy employee-relations-case-list-acl`

## Slice 2.3 — Talent-sensitive policy — **DONE** (2026-07-25)

Policies registered (`hr.talent-assessment` · `hr.talent-profile` · `hr.succession`). Talent runners route through authorization facade with per-operation `resolveResource`; sensitive reads use field projection + `includeSensitive`; employee-scoped lists via `runAuthorizedTalentSubjectListQuery`; load-then-authorize via `runAuthorizedTalentLoadedReadQuery` (`getCareerPlanById`). Ad-hoc `requireTalentProfileSensitiveRead` removed.

**Owner:** `HR-ENT-TALENT-SENSITIVE-ENFORCE` (**CLOSED**)

**Verify:**

```bash
pnpm --filter @afenda/human-resources typecheck
# Exit 0

pnpm --filter @afenda/human-resources test -- \
  sensitive-domain-policies human-resources.talent end-to-end-authorization talent-authorization-enforcement
# Exit 0 — 4 files · 42 passed · 3 skipped

REQUIRE_DATABASE_TESTS=1 pnpm --filter @afenda/human-resources test -- human-resources.talent.parity
# Exit 0 — 1 file · 5 passed · 3 skipped
```

## Slice 2.4 — Workforce-planning-sensitive policy — **DONE** (2026-07-28)

`hr.workforce-planning` policy + employee-actual field deny. Workforce-planning operations route through the contextual authorization facade; negative enforcement coverage is in `workforce-planning-authorization-enforcement.test.ts`.

**Owner:** `HR-ENT-WFP-SENSITIVE-ENFORCE` (**CLOSED**)

## Slice 2.5 — Compensation-sensitive policy — **DONE** (2026-07-28)

`hr.compensation` policy + `COMPENSATION_FIELD_CLASSES` masking are enforced through the contextual authorization facade; negative enforcement coverage is in `compensation-authorization-enforcement.test.ts`.

**Owner:** `HR-ENT-COMP-SENSITIVE-ENFORCE` (**CLOSED**)

## Slice 2.6 — Privacy adapter — **DONE** (2026-07-25)

`createHumanResourcesPrivacyPort()` wired in `apps/web/lib/erp/human-resources-command-options.ts`; platform privacy service; tenant-scoped audited ops.

**Verify:** `pnpm --filter @afenda/web test -- human-resources-privacy-port`

**Closes:** `HR-XCUT-P0-004` (composition). DSAR inventory depth = `HR-ENT-07-DSAR-DEPTH`.

## Slice 2.7 — Privacy command surface — **DONE** (6/6 · 2026-07-25)

| Command / query | Status |
|---|---|
| `exportHumanResourcesSubjectData` | **DONE** — returns `HumanResourcesSubjectExportBundle` via collector |
| `evaluateHumanResourcesAnonymization` | **DONE** |
| `evaluateHumanResourcesRetention` | **DONE** |
| `placeHumanResourcesLegalHold` | **DONE** |
| `releaseHumanResourcesLegalHold` | **DONE** |
| `anonymizeHumanResourcesSubject` | **DONE** |
| `getHumanResourcesPrivacyCase` | **DONE** — platform case-read port + HR query |

**Verify:** `pnpm --filter @afenda/human-resources test -- human-resources.privacy` · `pnpm --filter @afenda/web test -- human-resources-privacy-port`

## Slice 2.8 — Sensitive coverage gate — **DONE** (2026-07-28)

Derived policy coverage (`authorization-policy-coverage.test.ts`) is green; unknown operations fail closed. `authorization-facade-boundary.test.ts` plus domain enforcement suites prove the sensitive domains cannot bypass the facade.

**Verify:** `pnpm --filter @afenda/human-resources test -- authorization-policy-coverage authorization-facade-boundary`

## Phase 2 exit criteria

| Criterion | Status |
|---|---|
| One authorization facade | **Met** |
| Privacy adapter composed in production | **Met** |
| Cross-tenant privacy tests pass | **Met** |
| Employee Relations list/get parity passes | **Met** |
| Talent, headcount and compensation surfaces **classified** | **Met** |
| Talent sensitive reads **enforced without bypass** | **Met** (Slice 2.3 · 2026-07-25) |
| WFP and compensation **enforced without bypass** | **Met** (Slices 2.4–2.5 · 2026-07-28) |
| No sensitive command bypass (package-wide) | **Met** (boundary + domain enforcement suites) |
| No fixed manual sensitive-policy count | **Met** |
| Privacy command surface (roadmap list) | **6/6** — all commands including case query |

**Phase 2 verdict:** **DONE / LOCALLY VERIFIED** — package enforcement and negative bypass coverage are closed. This verdict is implementation evidence, not external security/privacy certification.

---

# Phase 3 — Audit, event and correlation integrity

## Objective

Make every mutation observable and explicitly classified.

Mutation-emission registry fixture (generated 2026-07-26): **348 / 348** commands classified (**100%**). Split: **212** `audit_only` · **136** `domain_event`. Canonical domain packs include dedicated Time and Learning registries; the former catch-all classification file is removed. Audit authority: [`HR-AUD-06-COMPENSATION-PERFORMANCE-LEARNING`](erp/human-resources-enterprise-audit/HR-AUD-06-COMPENSATION-PERFORMANCE-LEARNING.md). Inventory fixture: `__tests__/fixtures/mutation-inventory.json` (regenerate via `scripts/generate-mutation-inventory.mjs`).

## Slice 3.1 — Leave emission registry — **DONE** (2026-07-25)

### Mission

`HR-OPS-LEAVE-EMISSION-REGISTRY`

### Complete (evidence)

* `HUMAN_RESOURCES_LEAVE_COMMAND_IDS` — **18** leave mutations classified in `emissions/domains/leave.ts`
* composed into `HUMAN_RESOURCES_MUTATION_EMISSION_REGISTRY_RECORD`
* Drizzle leave executors emit via `planLeaveMutationOutboxEventType` (registry-gated)
* leave emission + emission-registry + correlation fixtures green

Covered:

* policy creation and amendment;
* policy publication and retirement;
* entitlement creation and amendment;
* entitlement adjustment;
* request submission;
* request approval;
* request rejection;
* request cancellation;
* request amendment.

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- leave-emission emission-registry correlation-integrity
pnpm --filter @afenda/human-resources typecheck
```

Time already provides the reference registry pattern with full command coverage.

## Slice 3.2 — Workforce-foundation emissions — **DONE** (2026-07-25)

### Complete (evidence)

* `HUMAN_RESOURCES_WORKFORCE_FOUNDATION_COMMAND_IDS` — **5** (person / worker)
* `HUMAN_RESOURCES_CORE_ORGANIZATION_COMMAND_IDS` — **23** (department / job / position / reporting-line / employment / contract / assignment)
* domain packs: `emissions/domains/workforce-foundation.ts` · `emissions/domains/core-organization.ts`
* drained from the former catch-all classifications into canonical domain registries; matrix locked in `workforce-foundation-emission-registry.test.ts`
* correlation fixtures for workforce-foundation + core-organization `domain_event` commands

Classified:

* person;
* worker;
* employee;
* employment;
* contract;
* assignment;
* organization changes.

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- workforce-foundation-emission emission-registry correlation-integrity
pnpm --filter @afenda/human-resources typecheck
```

## Slice 3.3 — Recruitment emissions — **DONE** (2026-07-25)

### Mission

`HR-REC-CANDIDATE-CONSENT-LIFECYCLE` (3.3A) → Slice 3.3 recruitment emissions (3.3B)

### Complete (evidence)

* **3.3A:** durable `candidate.withdraw-consent` · `candidate.change-retention` (commands · schemas · store · Memory/Drizzle · `@afenda/events` · manifest)
* **3.3B:** `HUMAN_RESOURCES_RECRUITMENT_COMMAND_IDS` — **27** classified in `emissions/domains/recruitment.ts` (+ **`candidate.anonymize`** in Slice 6.2 → **28** today)
* Memory + Drizzle happy paths registry-gated (`appendRegistryGatedOutbox` / `planRecruitmentDrizzleOutbox`)
* transition helpers pass `config.command` as mutation `operation` (requisition · application · offer)
* event catalog delta in `@afenda/events` + manifest `events.emits`
* matrix lock: `recruitment-emission-registry.test.ts`; correlation fixtures for every recruitment `domain_event` command

Classified:

* requisition;
* candidate;
* application;
* interview;
* offer;
* offer acceptance;
* candidate consent withdrawal;
* candidate retention change.

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- recruitment-emission emission-registry correlation-integrity candidate-consent
pnpm --filter @afenda/events test
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — recruitment-emission/emission-registry/correlation/candidate-consent green; events 32 passed; typecheck clean
```

**Next:** Slice 3.4 — Lifecycle emissions.

## Slice 3.4 — Lifecycle emissions — **DONE** (2026-07-25)

### Mission

Lifecycle domain pack + effective-dated outbox payloads + rehire dual-outcome on `human-resources.employment.create`.

### Complete (evidence)

* `HUMAN_RESOURCES_LIFECYCLE_COMMAND_IDS` — **14** lifecycle mutations classified in `emissions/domains/lifecycle.ts`
* `employment.create` registry permits `employment.started.v1` **or** `employee.rehired.v1` (adapter emits exactly one with `effectiveOn: startsOn`)
* `@afenda/events` effective-dated payload schema + **5** new event types (`probation.extended.v1` · `probation.reviewed.v1` · `employee.confirmed.v1` · `employee.rehired.v1` · `clearance.completed.v1`)
* Memory + Drizzle: `effectiveOn` on transfer/termination/employment-start/rehire outbox payloads; probation extend/outcome · confirm · clearance domain events on happy paths
* matrix lock: `lifecycle-emission-registry.test.ts`; correlation fixtures for every lifecycle `domain_event` command + rehire branch + `effectiveOn` asserts

Classified:

* onboarding;
* probation;
* confirmation;
* transfer;
* termination;
* offboarding;
* clearance;
* rehire (via `employment.create`).

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- lifecycle-emission emission-registry correlation-integrity
pnpm --filter @afenda/events test
pnpm --filter @afenda/human-resources typecheck
```

**Next:** Slice 3.5 — Governance emissions.

## Slice 3.5 — Governance emissions — **DONE** (2026-07-25)

### Mission

Governance domain packs: employee relations · compliance · talent · workforce planning.

### Complete (evidence)

* `HUMAN_RESOURCES_EMPLOYEE_RELATIONS_COMMAND_IDS` — **15** · `HUMAN_RESOURCES_COMPLIANCE_COMMAND_IDS` — **19** · `HUMAN_RESOURCES_TALENT_COMMAND_IDS` — **32** · `HUMAN_RESOURCES_WORKFORCE_PLANNING_COMMAND_IDS` — **13** classified in `emissions/domains/{employee-relations,compliance,talent,workforce-planning}.ts`
* historical migration milestone: composed into `HUMAN_RESOURCES_MUTATION_EMISSION_REGISTRY_RECORD` while the former catch-all was being drained; the catch-all is now removed
* **14** new §15-aligned event types in `@afenda/events` (ER assign/appeal/reopen · doc reject/expire · eligibility verify/renew/expire · talent profile/pool · WFP approve/reserve/release/consume)
* matrix lock: `governance-emission-registry.test.ts` · `emission-registry-infrastructure.test.ts`; emission-registry parity blocks for all four command-id packs
* correlation fixtures updated for compliance work-eligibility/document domain_event commands + ER assign/appeal/reopen + talent profile/pool + WFP approve/reserve/release/consume
* mutation inventory fixture synced (**228** classified · **62** unclassified)
* shipped **`37742a54`** on `main`

Classified:

* employee relations;
* compliance (document · work-eligibility · policy acknowledgement);
* talent (competency · profile · pool · career · succession);
* workforce planning (headcount plan · reservation lifecycle).

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- governance-emission emission-registry correlation-integrity emission-registry-infrastructure
pnpm --filter @afenda/events test
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — governance/emission-registry/correlation/infrastructure 63 passed; events 34 passed; typecheck clean
```

**Next:** Slice 3.6 — Compensation, performance and learning emissions (scaffold).

## Slice 3.6 — Compensation, performance and learning emissions — **DONE** (2026-07-25)

### Mission

`HR-OPS-COMP-PERF-LEARN-EMISSION-REGISTRY`

### Complete (classification evidence)

* audit artifact: [`HR-AUD-06-COMPENSATION-PERFORMANCE-LEARNING`](erp/human-resources-enterprise-audit/HR-AUD-06-COMPENSATION-PERFORMANCE-LEARNING.md)
* `HUMAN_RESOURCES_COMPENSATION_BENEFITS_COMMAND_IDS` — **18** · `HUMAN_RESOURCES_PERFORMANCE_COMMAND_IDS` — **30** · `HUMAN_RESOURCES_LEARNING_COMMAND_IDS` — **11** · privacy — **3**
* full `Record` domain packs: `emissions/domains/compensation.ts` · `performance.ts` · `learning.ts` · `privacy.ts`
* canonical classification: `certification.expire` → `domain_event` · `certification.expiring.v1` (Memory + Drizzle outbox wired)
* classified count: **290/290**; **0** unclassified

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- slice-36 compensation-emission performance-emission learning-emission emission-registry correlation-integrity
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — slice-36 + emission-registry + correlation 92 passed; typecheck clean
```

## Slice 3.7 — Event catalog alignment — **DONE** (2026-07-25)

### Mission

`HR-ENT-EVENT-CATALOG-COMPLIANCE`

Ensure every domain event:

* exists in `@afenda/events`;
* has a schema;
* has a version;
* has an owner;
* has a projection or documented consumer;
* carries correlation and organization context.

### Delivered

| Artifact | Path |
|---|---|
| Event catalog types + entries | `packages/erp/human-resources/src/event-catalog/` |
| Classified catalog | `HUMAN_RESOURCES_EVENT_CATALOG` — **106** registry `domain_event` types |
| Package export | `@afenda/human-resources/event-catalog` |
| Parity test | `packages/erp/human-resources/__tests__/event-catalog-parity.test.ts` |
| Payload invariants | `packages/data-plane/events/__tests__/human-resources-schemas.test.ts` — `organizationId` + `correlationId` on all **106** schemas |
| Forward exclusions | `SLICE_36_FORWARD_DOMAIN_EVENT_TYPES` — **0** (HR-AUD-06 closed) |

### Verify

```bash
pnpm --filter @afenda/events test
pnpm --filter @afenda/human-resources test -- emission-registry manifest event-catalog
pnpm --filter @afenda/human-resources typecheck
```

## Slice 3.8 — Registry CI gate — **DONE** (2026-07-25)

**Status: DONE** — strict fail-closed CI gate for **290/290** classified commands; **0** `missing_command` exemptions.

Every mutating command must declare:

```ts
{
  commandId,
  emissionMode: "audit_only" | "domain_event",
  eventTypes: [],
  correlationRequired: true
}
```

Gate tests: `registry-ci-gate.test.ts` · `no-direct-outbox-emission.test.ts` · `emission-registry-infrastructure.test.ts`.

## Phase 3 exit criteria

| Criterion | Status |
|---|---|
| 100% mutation-command classification | **Met** — **290/290** (100%); HR-AUD-06 classification **DONE** |
| Slice 3.6 classified registry (command packs · Record registry · test locks) | **Met** — 18 + 30 + 11 + 3 commands classified · inventory alignment locked |
| Correlation coverage for classified `domain_event` commands | **Met** — all slices including compensation · performance · learning · certification.expire |
| No undocumented adapter events (closed domain packs) | **Met** — registry-gated executors across closed packs |
| Event catalog entries for emitted types | **Met** — **121/121** in `HUMAN_RESOURCES_EVENT_CATALOG` with owner + `documented_no_consumer` |
| Audit-only commands explicitly registered (closed packs) | **Met** |
| Registry parity enforced in CI | **Met** — strict **290/290** (`registry-ci-gate` · `no-direct-outbox-emission`); **0** exempt `missing_command` |

**Phase 3 verdict:** **DONE** — Slices **3.1 · 3.2 · 3.3 · 3.4 · 3.5 · 3.6 · 3.7 · 3.8 DONE** (2026-07-25); current register remains complete at **348/348** classified · catalog **121/121** · strict registry CI with **0** HR-AUD-06 exemptions.

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- registry-ci-gate emission-registry event-catalog correlation-integrity slice-36
pnpm --filter @afenda/events test
pnpm --filter @afenda/human-resources typecheck
```

---

# Phase 4 — Historical truth and database integrity

## Objective

Make historical HR facts deterministic, auditable and concurrency-safe.

## Slice 4.1 — Effective-truth classification — **DONE** (2026-07-25)

### Mission

`HR-ENT-03-EFFECTIVE-TRUTH`

Classify all 106 mutation tables as:

1. effective definition;
2. bounded assignment;
3. versioned current fact;
4. append-only operational fact;
5. transactional state machine;
6. derived projection;
7. explicit exclusion.

**109/109** classified in `packages/erp/human-resources/src/effective-truth-classification.ts` with CI validator `validateEffectiveTruthClassificationRegister()`. Phase 3 adoption matrix (36 tables) remains scoped to temporal mechanism rows in `effective-truth-adoption.ts`.

## Slice 4.2 — Person and worker truth — **DONE** (2026-07-25)

Evidence:

* Persistence evidence (current schema; no standalone slice tag): `packages/data-plane/db/src/schema/human-resources.ts` defines `hr_person_identity_version` + `hr_worker_classification_version`; the applied repository ledger currently runs through `0033_schema_reconciliation`
* Commands unchanged: `human-resources.person.update`, `human-resources.worker.change-type`, `human-resources.worker.change-status`
* Queries: `human-resources.person.as-of`, `human-resources.worker.as-of`
* Verify: `pnpm --filter @afenda/human-resources test -- workforce-foundation foundation effective-truth`

Delivered:

* identity correction via close+succeed lineage (non-destructive);
* worker status/type history on one classification chain with predecessor/successor;
* `asOf` queries;
* Memory + Drizzle adapter parity;
* effective-truth adoption/classification reconciled for person, worker, and lineage tables.

## Slice 4.3 — Employment truth — **DONE** (2026-07-25)

Evidence:

* Persistence evidence (current schema; no standalone slice tag): append-only `hr_employment_status_history` + the `hr_employment` effective-range constraint in `packages/data-plane/db/src/schema/human-resources.ts`; applied ledger through `0033_schema_reconciliation`
* Commands: `human-resources.employment.create`, `human-resources.employment.amend`, `human-resources.employment.correct`
* Queries: `human-resources.employment.as-of`, `human-resources.employment.status-history.list`
* Verify: `pnpm --filter @afenda/human-resources test -- human-resources.core` · `pnpm --filter @afenda/human-resources typecheck`

Delivered:

* future-dated employment with as-of tenure resolution;
* terminated employment with lifecycle status history;
* rehire as new tenure row with `employee.rehired` emission;
* one open employment policy plus write-time overlap prevention;
* retroactive `correctEmployment` with required reason;
* queryable employment status history and status-as-of projection;
* Memory + Drizzle adapter parity including `finalizeTermination` history append.

## Slice 4.4 — Contract truth — **DONE** (2026-07-25)

Evidence:

* Persistence evidence (current schema; no standalone slice tag): `lineage_status`, supersession FKs, `reason_code`, `source_reference`, and active-reference uniqueness in `packages/data-plane/db/src/schema/human-resources.ts`; applied ledger through `0033_schema_reconciliation`
* Commands: `human-resources.employment-contract.create`, `human-resources.employment-contract.correct`, `human-resources.employment-contract.supersede`
* Queries: `human-resources.employment-contract.get`, `human-resources.employment-contract.as-of`
* Events: `human-resources.employment-contract.changed.v1`, `human-resources.employment-contract.superseded.v1` (supersede also emits `created` for successor)
* Verify: `pnpm --filter @afenda/human-resources test -- human-resources.core` · `pnpm --filter @afenda/human-resources typecheck` · `pnpm --filter @afenda/db test -- hr-employment-contract-lineage`

Delivered:

* contract date validation and parent employment range containment;
* in-place `correctEmploymentContract` with required reason + source reference;
* non-destructive supersession with bidirectional lineage links;
* overlapping active-contract prevention and sequential non-overlapping create;
* as-of contract resolution including boundary dates;
* Memory + Drizzle adapter parity.

## Slice 4.5 — Assignment truth — **DONE** (2026-07-25)

Evidence:

* Persistence evidence (current schema; no standalone slice tag): predecessor/successor assignment FKs, `transfer_movement_id`, `manager_employee_id_snapshot`, `work_calendar_id_snapshot`, and the assignment as-of index in `packages/data-plane/db/src/schema/human-resources.ts`; applied ledger through `0033_schema_reconciliation`
* Commands: `human-resources.assignment.create`, `human-resources.assignment.end`, `human-resources.assignment.transfer` (lineage wiring)
* Queries: `human-resources.assignment.get`, `human-resources.assignment.as-of`, `human-resources.employee.org-context.resolve` (stored snapshots only)
* Verify: `pnpm --filter @afenda/human-resources test -- human-resources.core human-resources.foundation` · `pnpm --filter @afenda/human-resources typecheck` · `pnpm --filter @afenda/db test -- hr-work-assignment-truth`

Delivered:

* assignment effective range with employment containment and write-time overlap rejection;
* transfer predecessor/successor lineage tied to `hr_employment_movement`;
* immutable manager, work-calendar, and organization dimension snapshots on assignment rows;
* public assignment as-of query and deterministic org-context without live manager/calendar drift;
* future assignment and ambiguous multi-match fail-closed behavior;
* Memory + Drizzle adapter parity.

## Slice 4.6 — Organization truth — **DONE** (2026-07-25)

Evidence:

* Persistence evidence (current schema; no standalone slice tag): `hr_department_structure_version`, `hr_job_definition_version`, `hr_position_definition_version`, and reporting-line supersession fields in `packages/data-plane/db/src/schema/human-resources.ts`; applied ledger through `0033_schema_reconciliation`
* Commands: `human-resources.department.update`, `human-resources.job.update`, `human-resources.position.update`, `human-resources.reporting-line.replace` (close+succeed lineage wiring)
* Queries: `human-resources.department.as-of`, `human-resources.job.as-of`, `human-resources.position.as-of`, `human-resources.organization-tree.as-of`, `human-resources.employee.org-context.resolve` (position definition as-of for department)
* Verify: `pnpm --filter @afenda/human-resources test -- human-resources.organization position-occupancy human-resources.foundation` · `pnpm --filter @afenda/human-resources typecheck` · `pnpm --filter @afenda/db test -- hr-organization-structure-lineage`

Delivered:

* department, job, and position structure/definition lineage via close+succeed version segments (stable aggregate IDs);
* reporting-line predecessor/successor links on primary replace/close;
* position occupancy remains vacant | occupied with multi-occupant `CONFLICT` rejection;
* historical organization tree and org-context department resolution from position definition at `asOf`;
* Memory + Drizzle adapter parity;
* effective-truth classification/adoption reconciled for version tables and reporting-line supersession fields.

## Slice 4.7 — Leave overlap guard — **DONE** (2026-07-25)

### Mission

`HR-OPS-LEAVE-OVERLAP-GUARD`

Reject overlapping approved leave windows per employee.

### Evidence

* Shared blocking statuses: `ACTIVE_LEAVE_OVERLAP_STATUSES` in [`leave-guards.ts`](../../packages/erp/human-resources/src/shared/leave-guards.ts) (`draft` · `submitted` · `returned` · `approved`)
* Command pre-check: `submitLeaveRequest` + `approveLeaveRequest` via `assertNoLeaveOverlap` → `human_resources.effective_range_overlap`
* Memory fail-closed: per-employee booking lock + adapter re-check on submit/approve
* Drizzle fail-closed: `Serializable` txn + employee advisory lock + in-TX segment overlap CTE on submit/approve
* Exclusion register: [`hr-leave-overlap-exclusion-register.json`](erp/human-resources-enterprise-audit/hr-leave-overlap-exclusion-register.json) — documents intentional no Postgres `EXCLUDE` (half-day `day_portion` semantics)
* Verify: `pnpm --filter @afenda/human-resources test -- human-resources.leave leave-concurrency` · `pnpm --filter @afenda/db test -- hr-leave-overlap-exclusion-register` · `pnpm --filter @afenda/human-resources typecheck`

### Delivered

* Overlapping leave windows for one employee rejected with deterministic `EFFECTIVE_RANGE_OVERLAP`
* Command + adapter parity on submit and approve paths
* Unit · parity · Drizzle concurrency tests for overlap rejection and concurrent submit race
* **HR-OPS-P2-004 CLOSED** at command + Serializable-transaction bar (no database exclusion constraint by design)

## Slice 4.8 — Attendance determinism — **DONE** (2026-07-25)

### Mission

`HR-OPS-TIME-SESSION-DETERMINISM`

Sort attendance events by:

```text
occurredAt
→ source sequence
→ event ID
```

### Evidence

* Shared comparator: [`event-order.ts`](../../packages/erp/human-resources/src/time/attendance/event-order.ts) — `compareAttendanceEventsForSession` · `sortAttendanceEventsForSession` · `resolveAttendanceEventSourceSequence`
* Persistence evidence (current schema; no standalone slice tag): `hr_attendance_event.source_sequence` is non-null in `packages/data-plane/db/src/schema/human-resources.ts`; applied ledger through `0033_schema_reconciliation`
* Adapters: Memory + Drizzle use shared sort for session resolve, list, exception detection, break-waiver paths; import uses explicit `sourceSequence` when supplied, otherwise batch `rowIndex` (0-based)
* Assignment: import row explicit → use value; omitted → batch `rowIndex`; `recordAttendanceEvent` explicit → use value; omitted → `max+1` per `(org, employee, localWorkDate)`
* Verify: `pnpm --filter @afenda/human-resources test -- human-resources.time human-resources.time.attendance` · `pnpm --filter @afenda/human-resources typecheck` · `pnpm --filter @afenda/db test -- hr-attendance-event-source-sequence`

### Delivered

* Canonical ascending comparator: `occurredAt` → `sourceSequence` → event `id`
* `sourceSequence` persisted on `hr_attendance_event` with import/replay allocation when omitted
* Equal-timestamp tie-break yields stable session minutes, break pairing, and `resolutionStatus` across runs and adapters
* Unit + integration + Memory/Drizzle parity tests for tie-break and import replay stability
* Shared `filterAttendanceEventsForWorkDay` consolidates day-scoped filter + sort in Memory adapter paths
* Closes audit finding **HR-OPS-P2-003** at command/adapter bar

## Slice 4.9 — Timezone and legal date — **DONE** (2026-07-25)

### Missions

* `HR-OPS-TIME-TIMEZONE-HANDOFF`
* `HR-OPS-TIME-AUTHORITY-EFFECTIVE-DATE`

Define:

* organization-local work date;
* calendar timezone;
* payroll handoff timezone;
* overtime authority effective date;
* cross-midnight behavior;
* daylight-saving behavior.

### Evidence

* Contract: [`org-local-work-date.ts`](../../packages/erp/human-resources/src/time/org-local-work-date.ts) — `organizationLocalWorkDate` · `resolveOrganizationLocalWorkDateFromCalendar` · `resolveOvertimeAuthorityAsOf` · `resolveEmploymentOrganizationLocalWorkDate` (UTC provisional `asOf` → calendar → org-local civil date → one re-resolve when dates differ)
* Leave handoff: [`production-approved-leave-query.ts`](../../packages/erp/human-resources/src/production-approved-leave-query.ts) — per-segment calendar lookup with `asOf = segmentDate`; fail-closed when lookup present; `ApprovedLeaveFact.timezone` from resolved calendar
* Overtime authority: [`overtime.ts`](../../packages/erp/human-resources/src/time/overtime.ts) — create/approve `workDate` and authority `asOf` from employment calendar org-local civil date (no UTC ISO slice)
* JSDoc on [`ApprovedLeaveFact`](../../packages/erp/human-resources/src/time/handoff/ports.ts) and [`ApprovedTimeHandoff`](../../packages/erp/human-resources/src/types.ts) — display TZ on entry lines; handoff minute totals only; stored instants remain UTC
* Verify: `pnpm --filter @afenda/human-resources test -- human-resources.time.exceptions.parity legal-minute-allocation overtime-approval-authority` · `pnpm --filter @afenda/human-resources test -- human-resources.time legal-minute-allocation` · `pnpm --filter @afenda/human-resources typecheck`

### Delivered

* Single org-local work-date / timezone contract exported from `@afenda/human-resources` time domain
* Leave timesheet handoff stamps calendar IANA timezone per segment; lookup failures propagate (no silent UTC when lookup wired)
* Overtime create/approve share org-local civil date for employment `workDate` and approval-authority `asOf`
* Cross-midnight minute allocation unchanged via `allocateWorkedMinutesByCivilDate`; DST spring/fall cases in legal-minute tests
* LA midnight boundary parity: authority effective on org-local civil date, not UTC slice
* UTC residual documented: `ApprovedTimeHandoff` has no timezone field; `defaultTimezone` only when approved-leave lookup omitted (test doubles)
* Closes audit findings **HR-OPS-P2-001** and **HR-OPS-P2-006** at command/adapter bar

## Slice 4.10 — Database invariant migration — **DONE** (2026-07-25)

### Mission

`HR-COREORG-DB-INVARIANTS`

Add or document:

* start date ≤ end date;
* valid bounded ranges;
* active uniqueness;
* overlap protection;
* valid status transition support;
* idempotency uniqueness;
* successor uniqueness;
* organization-scoped foreign keys.

### Evidence

* Migrations `0018_hr_coreorg_db_invariants.sql` (core org trio) · `0035_hr_coreorg_db_invariants_completion.sql` (probation · salary band · employee compensation · benefit enrollment · shift)
* Exclusion register: [`hr-coreorg-db-invariant-exclusion-register.json`](erp/human-resources-enterprise-audit/hr-coreorg-db-invariant-exclusion-register.json) — 8-category inventory (DDL vs command-only) for all mutable `hr_*` tables; cross-link to leave overlap register
* Verify: `pnpm --filter @afenda/db test -- hr-coreorg-db-invariants hr-leave-overlap-exclusion-register` · `pnpm --filter @afenda/db test -- hr-tenant-foreign-keys`

### Delivered

* Effective-range CHECK constraints on all remaining dated mutable tables without prior DDL (0035)
* Core org assignment · contract · reporting-line range CHECKs (0018) with command-only overlap policies documented
* Partial unique indexes for open/active branches catalogued; salary-band active uniqueness documented command-only (multi-currency per grade)
* Successor/supersedes lineage command-only surfaces documented; org-scoped FKs cite migration 0012
* No PostgreSQL EXCLUDE / btree_gist (HARD-STOP retained); eight scaffold tables documented N/A until promoted

## Phase 4 exit criteria

* Every HR table classified — **DONE** (current register **136/136** via `effective-truth-classification.ts`, reconciled 2026-07-28).
* Every historical aggregate supports `asOf` — **DONE** (person · worker · employment · employment-contract · assignment · organization structure, 2026-07-25).
* No ambiguous active branch — **DONE** (employment · employment-contract · assignment · organization structure overlap guards, 2026-07-25).
* Critical overlaps protected — **DONE** (employment · employment-contract · assignment · organization occupancy · **leave**, 2026-07-25).
* Effective ranges validated — **DONE** (Slice **4.10**, migrations **0018** + **0035**, register SSOT, 2026-07-25).
* Historical organization context deterministic — **DONE** (Slice **4.6**, 2026-07-25).
* Attendance session event ordering deterministic — **DONE** (Slice **4.8**, 2026-07-25).
* Timezone contract published — **DONE** (Slice **4.9**, 2026-07-25).

---

# Phase 5 — Workforce foundation and organization completion

## Objective

Finish the core employee system that every other domain depends upon.

## Slice 5.1 — Person management — **DONE** (2026-07-25)

Complete:

* create;
* correct identity;
* legal name;
* preferred name;
* contact information;
* identifiers;
* privacy classification;
* duplicate detection;
* subject export.

Evidence:

* DDL: migration `0036_hr_person_management_completion` (`hr_person.preferred_name`, `hr_person.privacy_classification`, `hr_person_contact`, `hr_person_identifier`)
* Commands/queries: `human-resources.person.preferred-name.update`, `human-resources.person.privacy-classification.set`, `human-resources.person.contact.*`, `human-resources.person.identifier.*`, `human-resources.person.duplicates.detect`
* Subject export collector resolves worker-linked `personId` and emits `hr_person`, identity versions, contacts, identifiers
* Verify:

```bash
pnpm --filter @afenda/db test -- hr-person
pnpm --filter @afenda/events test -- human-resources
pnpm --filter @afenda/human-resources test -- foundation person privacy workforce-foundation manifest effective-truth person-management
pnpm --filter @afenda/human-resources typecheck
pnpm --filter @afenda/web test -- human-resources-privacy-port
```

Verify output (2026-07-25):

```
@afenda/db hr-person: 2 files, 3 tests passed
@afenda/events human-resources: 1 file, 7 tests passed
@afenda/human-resources: 12 files, 83 passed | 14 skipped (97)
@afenda/human-resources typecheck: exit 0
@afenda/web human-resources-privacy-port: 1 file, 7 tests passed
```

## Slice 5.2 — Worker management — **DONE** (2026-07-25)

Evidence:

* Commands: `human-resources.worker.create`, `human-resources.worker.change-type`, `human-resources.worker.change-status`
* Query: `human-resources.worker.as-of`
* Verify:

```bash
pnpm --filter @afenda/human-resources test -- human-resources.worker-foundation human-resources.foundation
pnpm --filter @afenda/human-resources typecheck
```

Delivered:

* create worker — idempotent create with fingerprint conflict; person 1:1 linkage enforced (Memory + Drizzle);
* worker type — close+succeed classification lineage with optimistic-lock and no-op rejection;
* status change — lineage-preserving status segments (`active` · `inactive` · `former`);
* contingent worker — `contingent_worker` create, reclassification, and as-of resolution;
* employee linkage — employee type may carry or omit `employeeId`; non-employee forbids non-null `employeeId`; duplicate employee link rejected;
* effective history — `getWorkerAsOf` across boundary dates on one classification chain; Memory + Drizzle parity in `human-resources.foundation.history.test.ts`.

## Slice 5.3 — Employee management — **DONE** (2026-07-25)

Complete:

* employee number — `normalizeEmployeeNumber` on create; profile surfaces normalized value;
* employee status — employment-derived (`employmentStatus` from as-of / open employment; no `hr_employee.status` column);
* organization entry — `{ enteredOn, employmentId, orgContext }` on profile (org context soft-fails when assignment/dims missing);
* profile — `getEmployeeProfile` composite assemble (person · worker · employment · org entry);
* manager view — `subject_manager_or_privileged` policy + public-tier projection for direct reports;
* self-service view — subject-scoped read with public + self tiers;
* sensitive field masking — `projectEmployeeProfileFields` tier partition (domain projection only; no adapter masking).

Evidence:

* Query: `human-resources.employee.profile.get` → manifest permission `human-resources.employee.read`; HR bypass via `human-resources.person.read`
* AuthZ: `hr.employee-profile` policy (`subject_manager_or_privileged`); narrowed `hr.employee-subject` prefixes (exclusive resolve)
* Module: `packages/erp/human-resources/src/workforce-foundation/employee-management.ts`, `employee-profile-field-projection.ts`
* Shared org context: `packages/erp/human-resources/src/core/employee-org-context-resolution.ts` (strict query + soft profile embed)
* Tests: `human-resources.employee-management.test.ts`; extended `sensitive-domain-policies.test.ts`
* **Deferred:** `apps/web/app/actions/hr-employees.ts` → **closed Slice 5.9** (2026-07-26)

Verify:

```bash
pnpm --filter @afenda/human-resources test -- human-resources.domain sensitive-domain-policies contextual-authorization-privacy employee-management
pnpm --filter @afenda/human-resources typecheck
```

Verify output (2026-07-25):

```
@afenda/human-resources: 6 files, 105 passed | 12 skipped (117)
@afenda/human-resources typecheck: exit 0
```

## Slice 5.4 — Employment management — **DONE** (2026-07-25)

Complete:

* hire — `createEmployment` · `hireEmployment` (alias);
* rehire — `createEmployment` · `rehireEmployment` (alias); blocked open tenure → `REHIRE_REQUIRES_ENDED_EMPLOYMENT`;
* suspend — `amendEmployment({ status: "notice" })` · `suspendEmployment` (`active → notice`);
* reactivate — `amendEmployment({ status: "active" })` · `reactivateEmployment` (`notice → active`);
* terminate — `amendEmployment({ status: "terminated" })` · `terminateEmployment`;
* future-dated employment — `startsOn` / amend `effectiveOn`; `getEmploymentAsOf` null before start;
* employment history — `listEmploymentStatusHistory` + `statusAsOf`; `getEmploymentAsOf`.

Evidence:

* Status SSOT: `active | notice | terminated` — suspend maps to `notice`; no parallel `suspended` vocabulary
* Module: `packages/erp/human-resources/src/core/employment.ts`, `core/employment-management.ts`, `shared/employment-status.ts`, `shared/employment-history.ts`
* Commands: `human-resources.employment.create`, `.amend`, `.correct`
* Queries: `human-resources.employment.get`, `.as-of`, `.status-history.list`
* Error: `HUMAN_RESOURCES_ERROR_REHIRE_REQUIRES_ENDED_EMPLOYMENT` on open-tenure create (command + Memory + Drizzle)
* Tests: `human-resources.core.test.ts` (Slice 5.4 block); `human-resources.core.parity.test.ts` (suspend/reactivate + rehire error); `employment-status.test.ts` (transition table)
* **Slice 5.8 overlap:** rehire semantics (`HR-COREORG-REHIRE-SEMANTICS` / HR-COREORG-P1-002) closed in this slice — see Slice 5.8

Verify:

```bash
pnpm --filter @afenda/human-resources test -- human-resources.core employment-status
pnpm --filter @afenda/human-resources typecheck
```

Verify output (2026-07-25):

```
Test Files  3 passed (3)
     Tests  75 passed | 12 skipped (87)
@afenda/human-resources typecheck: exit 0
```

## Slice 5.5 — Employment contract management — **DONE** (2026-07-26)

Complete:

* create — `createEmploymentContract` · `human-resources.employment-contract.create`;
* amend — `amendEmploymentContract` → `correctEmploymentContract` · `human-resources.employment-contract.correct`;
* renew — `renewEmploymentContract` → `supersedeEmploymentContract` · `human-resources.employment-contract.supersede` (successor term; `reasonCode: "renewal"`);
* supersede — `supersedeEmploymentContract` · `human-resources.employment-contract.supersede`;
* end — `endEmploymentContract` · `human-resources.employment-contract.end` (non-destructive `endsOn` close; `lineageStatus` stays `active`);
* query current — `getCurrentEmploymentContract` · `human-resources.employment-contract.current` (governing contract for `(employmentId, asOf)`);
* query historical — `getEmploymentContractAsOf` · `human-resources.employment-contract.as-of` + `listEmploymentContracts` · `human-resources.employment-contract.list` (full lineage).

Evidence:

* Module: `packages/erp/human-resources/src/core/employment-contract.ts`, `core/employment-contract-management.ts`, `shared/employment-contract-guards.ts`
* Commands: `human-resources.employment-contract.create`, `.correct`, `.supersede`, `.end`
* Queries: `human-resources.employment-contract.get`, `.as-of`, `.current`, `.list`
* Events: end emits `human-resources.employment-contract.changed.v1` (via correct store path); supersede unchanged (superseded + created)
* Slice 4.4 preserved: employment containment, overlap rejection, reason/source refs, bidirectional supersession links, optimistic locking; Memory + Drizzle parity
* Tests: `human-resources.core.test.ts` (Slice 5.5 block); `human-resources.core.parity.test.ts` (amend/renew/end/current/list parity)

Verify:

```bash
pnpm --filter @afenda/human-resources test -- human-resources.core
pnpm --filter @afenda/human-resources typecheck
```

Verify output (2026-07-26):

```
Test Files  2 passed (2)
     Tests  88 passed | 14 skipped (102)
@afenda/human-resources typecheck: exit 0
```

## Slice 5.6 — Assignment management — **DONE** (2026-07-25)

Complete:

* create — `createAssignment` · `human-resources.assignment.create`;
* transfer — `transferAssignment` · `human-resources.assignment.transfer` (+ `hr_employment_movement` lineage);
* end — `endAssignment` · `human-resources.assignment.end`;
* future assignment — future `startsOn`; as-of before start → null;
* primary assignment — `getAssignmentAsOf` · `resolvePrimaryAssignmentAsOf`; ambiguity → `HUMAN_RESOURCES_ERROR_MULTIPLE_PRIMARY_ASSIGNMENTS`;
* concurrent assignment — overlap rejected on create/end/transfer (`HUMAN_RESOURCES_ERROR_CONFLICT`);
* organization dimensions — five keys via `OrganizationDimensionDirectoryPort.resolveRequiredAsOf` at write;
* manager and calendar context — write-time `managerEmployeeIdSnapshot` / `workCalendarIdSnapshot`; org-context reads stored snapshots only.

Evidence:

* Module: `packages/erp/human-resources/src/core/assignment.ts`, `core/assignment-management.ts`, `lifecycle/transfer.ts`, `core/org-context.ts`, `core/employee-org-context-resolution.ts`, `shared/assignment-guards.ts`, `shared/assignment-snapshots.ts`, `schemas/organization.ts`
* Commands: `human-resources.assignment.create`, `.end`, `.transfer`
* Queries: `human-resources.assignment.get`, `.as-of`, `human-resources.employee.org-context.resolve`
* Errors: `HUMAN_RESOURCES_ERROR_MULTIPLE_PRIMARY_ASSIGNMENTS` (as-of ambiguity) · `HUMAN_RESOURCES_ERROR_ASSIGNMENT_OUTSIDE_EMPLOYMENT_RANGE` (employment containment)
* Tests: `human-resources.core.test.ts` (Slice 5.6 block + assignment historical truth); `human-resources.core.parity.test.ts` (Slice 5.6 parity); `human-resources.lifecycle.test.ts` / `.lifecycle.parity.test.ts` (transfer)
* Phase 4.5 assignment truth preserved: overlap rejection, transfer lineage, as-of fail-closed

Verify:

```bash
pnpm --filter @afenda/human-resources test -- human-resources.core human-resources.foundation human-resources.lifecycle
pnpm --filter @afenda/human-resources typecheck
```

Verify output (2026-07-25):

```
Test Files  5 passed | 1 skipped (6)
     Tests  113 passed | 28 skipped (141)
@afenda/human-resources typecheck: exit 0
```

## Slice 5.7 — Organization management — **DONE** (2026-07-26)

Complete:

* departments — `createDepartment` · `updateDepartment` · `activateDepartment` · `archiveDepartment` · `getDepartment` · `listDepartments` · `getDepartmentAsOf` · `getOrganizationTree` · `getOrganizationTreeAsOf` · `human-resources.department.*` · `human-resources.organization.tree*`;
* jobs — `createJob` · `updateJob` · `activateJob` · `archiveJob` · `getJob` · `listJobs` · `getJobAsOf` · `human-resources.job.*`;
* positions — `createPosition` · `updatePosition` · `activatePosition` · `freezePosition` · `closePosition` · `getPosition` · `listPositions` · `getPositionAsOf` · `human-resources.position.*`;
* position occupancy — `getPositionOccupancyAsOf` · `human-resources.position.occupancy-as-of` (assignment-derived vacant | occupied; multi-occupant `CONFLICT`);
* reporting lines — `assignPrimaryReportingLine` · `closeReportingLine` · `replacePrimaryReportingLine` · `resolvePrimaryManager` · `listDirectReports` · `human-resources.reporting-line.*`;
* legal entity — `@afenda/master-data` `createOrganizationDimension` · `getOrganizationDimensionEffective` · `resolveOrganizationDimensionsAsOf` · `master_data.organization_dimension.*`;
* business unit — same master-data surface (`kind: business_unit`);
* cost centre — same master-data surface (`kind: cost_centre`);
* location — same master-data surface (`kind: location`);
* work calendar — `createWorkCalendar` · `updateWorkCalendar` · `supersedeWorkCalendar` · `archiveWorkCalendar` · holiday/override add/remove · employment + scope assign/end · resolve employment/employee calendar · `human-resources.work-calendar.*` · `human-resources.employment-calendar.*` · `human-resources.employee-work-calendar.resolve`.

Evidence:

* HR modules: `packages/erp/human-resources/src/organization/{department,job,position,reporting-line}.ts`, `organization/organization-structure-lineage.ts`, `time/calendar.ts`
* Master-data module: `packages/erp/master-data/src/organization-dimension.ts` — `getOrganizationDimensionEffective` accepts all `ORGANIZATION_DIMENSION_KINDS` (Slice 5.7 close)
* Dimension resolve at assignment write: `OrganizationDimensionDirectoryPort` → `apps/web/lib/erp/human-resources-organization-dimension-port.ts` → `resolveOrganizationDimensionsAsOf` (no HR dimension CRUD duplication)
* Phase 4.6 preserved: structure/definition lineage close+succeed; reporting-line replace predecessor/successor links; position occupancy fail-closed; Memory + Drizzle adapter parity
* Tests: `human-resources.organization.test.ts` · `human-resources.organization.parity.test.ts` · `position-occupancy.test.ts` · `position-occupancy.parity.test.ts` · `human-resources.time.calendar.parity.test.ts` · `calendar-scope*.test.ts` · `organization-dimension.domain.test.ts` (per-kind create + getEffective + batch resolve)

Verify:

```bash
pnpm --filter @afenda/human-resources test -- human-resources.organization position-occupancy
pnpm --filter @afenda/human-resources test -- human-resources.time.calendar
pnpm --filter @afenda/master-data test -- organization-dimension
pnpm --filter @afenda/human-resources typecheck
pnpm --filter @afenda/master-data typecheck
```

Verify output (2026-07-26):

```
@afenda/master-data test -- organization-dimension
Test Files  1 passed (1)
     Tests  7 passed (7)

@afenda/human-resources test -- human-resources.organization position-occupancy
Test Files  4 passed (4)
     Tests  31 passed | 10 skipped (41)

@afenda/human-resources test -- human-resources.time.calendar
Test Files  1 passed (1)
     Tests  3 passed | 3 skipped (6)

@afenda/human-resources typecheck: exit 0
@afenda/master-data typecheck: exit 0
```

## Slice 5.8 — Rehire semantics — **DONE** (2026-07-25)

### Mission

`HR-COREORG-REHIRE-SEMANTICS`

Ensure:

* active employee cannot be rehired — `createEmployment` / `rehireEmployment` → `REHIRE_REQUIRES_ENDED_EMPLOYMENT`;
* ended employee can be rehired — second `createEmployment` after prior `endsOn` set;
* new employment record created — distinct tenure row; `employee.rehired.v1` when prior ended row exists;
* prior history preserved — append-only `hr_employment_status_history` on prior row unchanged;
* semantic error code returned — `HUMAN_RESOURCES_ERROR_REHIRE_REQUIRES_ENDED_EMPLOYMENT` (command + adapters).

Delivered in **Slice 5.4** — no separate implementation pass required.

Verify: same as Slice 5.4 (`human-resources.core`).

## Slice 5.9 — Core product Actions — **DONE** (2026-07-26)

Delivered:

* `apps/web/app/actions/hr-people.ts` — 20 actions (person CRUD/history · contacts · identifiers · duplicates · worker lifecycle)
* `apps/web/app/actions/hr-employees.ts` — 6 actions (employee CRUD/list · profile · member self profile)
* `apps/web/app/actions/hr-employment.ts` — 19 actions (hire/rehire/amend/suspend/reactivate/terminate/correct · contract lifecycle · employment/contract reads)
* `apps/web/app/actions/hr-assignments.ts` — 6 actions (create/end/transfer assignment · org context as-of)
* `apps/web/app/actions/hr-organization.ts` — 44 actions (department/job/position/reporting/tree · calendar assign/resolve; no duplicate of `hr-time` calendar CRUD)
* `apps/web/modules/identity/domain/session-permission.ts` — 12 HR Phase 5 permission deny messages added (person · worker · personal-details · sensitive-identifiers · assignment · organization-context · reporting-line)
* Contract tests: `hr-people-actions.test.ts` · `hr-employees-actions.test.ts` · `hr-employment-actions.test.ts` · `hr-assignments-actions.test.ts` · `hr-organization-actions.test.ts`
* Shared Action schema helper: `hrActionSchema` / `hrServerContextOmit` in `hr-mutation-context.ts` (DRY for employees · assignments · organization)

Action inventory (delegate only — no domain logic in `apps/web`):

| File | Actions |
|------|---------|
| `hr-people.ts` | `createPersonAction` · `updatePersonNameAction` · `updatePersonPreferredNameAction` · `setPersonPrivacyClassificationAction` · `addPersonContactAction` · `updatePersonContactAction` · `retirePersonContactAction` · `listPersonContactsAction` · `addPersonIdentifierAction` · `retirePersonIdentifierAction` · `listPersonIdentifiersAction` · `detectPersonDuplicatesAction` · `getPersonAction` · `getPersonAsOfAction` · `createWorkerAction` · `changeWorkerTypeAction` · `changeWorkerStatusAction` · `getWorkerAction` · `getWorkerAsOfAction` |
| `hr-employees.ts` | `createEmployeeAction` · `updateEmployeeAction` · `getEmployeeAction` · `listEmployeesAction` · `getEmployeeProfileAction` · `getOwnEmployeeProfileAction` |
| `hr-employment.ts` | `hireEmploymentAction` · `rehireEmploymentAction` · `amendEmploymentAction` · `suspendEmploymentAction` · `reactivateEmploymentAction` · `terminateEmploymentAction` · `correctEmploymentAction` · `createEmploymentContractAction` · `amendEmploymentContractAction` · `renewEmploymentContractAction` · `supersedeEmploymentContractAction` · `endEmploymentContractAction` · `getEmploymentAction` · `getEmploymentAsOfAction` · `listEmploymentStatusHistoryAction` · `getEmploymentContractAction` · `getEmploymentContractAsOfAction` · `getCurrentEmploymentContractAction` · `listEmploymentContractsAction` |
| `hr-assignments.ts` | `createAssignmentAction` · `endAssignmentAction` · `transferAssignmentAction` · `getAssignmentAction` · `getAssignmentAsOfAction` · `resolveEmployeeOrgContextAsOfAction` |
| `hr-organization.ts` | department/job/position CRUD+read+tree (18) · reporting line (5) · calendar holidays/overrides/assign/resolve (13) · see `grep '^export async function' apps/web/app/actions/hr-organization.ts` |

Verify:

```bash
pnpm --filter @afenda/web test -- hr-people hr-employees hr-employment hr-assignments hr-organization
```

Verify output (2026-07-26):

```
Test Files  5 passed (5)
     Tests  24 passed (24)
```

## Phase 5 exit criteria — **MET** (2026-07-26)

* Full employee administrative journey available — Actions wire person → worker → employee → employment → contract → assignment → organization.
* Current and historical employee views available — `getPersonAsOfAction` · `getWorkerAsOfAction` · `getEmployeeProfileAction` · `getEmploymentAsOfAction` · `getAssignmentAsOfAction` · `resolveEmployeeOrgContextAsOfAction` · org tree/position as-of reads.
* Transfer and rehire supported — `transferAssignmentAction` · `rehireEmploymentAction`.
* Organization context resolves for any effective date — `resolveEmployeeOrgContextAsOfAction` · `getOrganizationTreeAsOfAction`.
* Core Actions tested — **24/24** contract tests green (authz envelope · validation · happy-path delegate).
* No domain logic duplicated in apps/web — Actions validate + stamp session + delegate to `@afenda/human-resources` via `createHumanResourcesCommandOptions()` only.

---

# Phase 6 — Recruitment and employee lifecycle completion

## Objective

Complete candidate-to-worker and joiner-to-leaver workflows.

## Slice 6.1 — Requisition — **DONE** (2026-07-26)

### Mission

Requisition lifecycle completion: hiring-manager assignment, status-gated headcount reservation (1A), suspend mapping, Memory/Drizzle parity.

### Command / query map

| Bullet | Command / query |
|---|---|
| draft | `createDraftRequisition` |
| submit | `submitRequisition` (requires `hiringManagerEmployeeId`) |
| approve | `approveRequisition` (requires hiring manager; emits `requisition.approved.v1`) |
| open | `openRequisition` (requires hiring manager) |
| suspend | `placeRequisitionOnHold` → status `on_hold`; resume via `openRequisition` |
| close | `closeRequisition` (releases active headcount reservation) |
| cancel | `cancelRequisition` (releases active headcount reservation) |
| headcount reservation | explicit `reserveHeadcount` (WFP) — allowed only when requisition is `approved` or `open`; duplicate active rejected; no auto-reserve on approve/open |
| hiring manager | `assignHiringManager` · optional on `createDraftRequisition` / `amendRequisition` (draft only); `getRequisition` / `listRequisitions` expose `hiringManagerEmployeeId` |

### Complete (evidence)

* `hiring_manager_employee_id` on `hr_job_requisition` — migration `0038_hr_job_requisition_hiring_manager.sql` + additive migration test
* shared test fixture `seedRequisitionPipeline` (DRY across recruitment/WFP unit + parity suites)
* `assignHiringManager` command + emission (`human-resources.requisition.assign-hiring-manager`) + manifest permission
* `validateHiringManagerEmployee` — active employment as-of mutation UTC date via `findEmploymentByEmployeeAsOf`
* `assertRequisitionAllowsHeadcountReservation` gates `reserveHeadcount` to `approved` \| `open`
* cancel/close adapter release of active reservations unchanged; close + duplicate-active + status-gate tests added
* Tests: `human-resources.recruitment.test.ts` (Slice 6.1 lifecycle block); `human-resources.workforce-planning.test.ts` (headcount status gate); parity suites green

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- human-resources.recruitment human-resources.workforce-planning recruitment-emission
pnpm --filter @afenda/db test -- hr-job-requisition-hiring-manager-migration
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — 51 passed (11 skipped parity drizzle); migration test green; typecheck clean
```

**Next:** Slice 6.2 — Candidate.

## Slice 6.2 — Candidate — **DONE** (2026-07-26)

### Mission

Candidate domain completion: anonymization (PII scrub + `anonymized` status), explicit duplicate detection, list search filters, Memory/Drizzle parity — on top of shipped create/update/consent/retention/`consentSource`.

### Command / query map

| Bullet | Command / query |
|---|---|
| create | `createCandidate` (email uniqueness · idempotency · `consentSource` required) |
| update | `updateCandidateProfile` (displayName/phone; rejected when `anonymized`) |
| consent | create consent facts · `withdrawCandidateConsent` (Slice 3.3A) |
| retention | `changeCandidateRetention` · `listCandidates.retentionDueAsOf` |
| anonymization | `anonymizeCandidate` — PII scrub per `recruitment_and_background`/`delete_identifiers`; status `anonymized`; frees original email via sentinel |
| duplicate detection | `detectCandidateDuplicates` (email and/or displayName; excludes `anonymized`) + create-time email CONFLICT |
| source attribution | existing `consentSource` (`self_service` \| `recruiter_recorded` \| `import`) — no acquisition-source column |
| candidate search | `listCandidates` optional `query` (name/email/normalizedEmail substring) |

### Complete (evidence)

* Status enum + guards: `anonymized` terminal; eligibility = consent withdrawn **or** retention due as-of
* Store + Memory/Drizzle: `anonymizeCandidate` · `detectCandidateDuplicates` · `listCandidates.query`
* Event: `human-resources.candidate.anonymized.v1` · emission registry · manifest permission/`events.emits`
* Tests: `human-resources.recruitment.candidate-consent{,.parity}.test.ts` Slice 6.2 block (email CONFLICT · profile update · consentSource enum · search · duplicates · anonymize · post-anonymize guard on profile/withdraw/retention)
* Recruitment classified command count: **28** (`HUMAN_RESOURCES_RECRUITMENT_COMMAND_IDS` incl. `candidate.anonymize`)
* DB: status comment only (`active \| archived \| anonymized`) — no migration (no status CHECK)

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- human-resources.recruitment candidate-consent
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — 32 passed (16 skipped parity drizzle without DB gate); typecheck clean
```

**Next:** Slice 6.3 — Application.

## Slice 6.3 — Application — **DONE** (2026-07-26)

### Mission

Application lifecycle completion: stage movement with reason history, reject/withdraw reasons, reopen from terminal statuses, one-open constraint hardening, Memory/Drizzle parity.

### Command / query map

| Bullet | Command / query |
|---|---|
| apply | `createApplication` → `submitted` (append history `create`) |
| stage movement | `moveApplicationToInReview` · `moveApplicationToInterviewing` (append history `lifecycle`; `offered`/`accepted` deferred to Slice 6.5 offer flow) |
| reject | `rejectApplication` — optional `reason` / `reasonCode` + history row |
| withdraw | `withdrawApplication` — optional `reason` / `reasonCode` + history row |
| reopen | `reopenApplication` — `rejected` \| `withdrawn` → `submitted`; one-open pre-check; event `application.reopened.v1` |
| one open application | existing partial unique `hr_candidate_application_org_candidate_requisition_open_uidx` + adapter pre-check; hardened in reopen/re-apply tests |
| reason history | `listApplicationStatusHistory` · append-only `hr_candidate_application_status_history` on create/transition/reopen |

### Complete (evidence)

* DDL `0039_hr_candidate_application_status_history.sql` + schema + hard-tenant root + migration test
* Guards: `canTransitionApplicationStatus` reopen path; `assertApplicationReopenable`
* Store + Memory/Drizzle: history append on create/transition; `reopenApplication` · `listApplicationStatusHistory`
* Event: `human-resources.application.reopened.v1` · emission registry · manifest permission
* Recruitment classified command count: **29** (`HUMAN_RESOURCES_RECRUITMENT_COMMAND_IDS` incl. `application.reopen`)
* Tests: `human-resources.recruitment.test.ts` Slice 6.3 block (history, one-open re-apply, reopen one-open block, non-terminal deny); `human-resources.recruitment.parity.test.ts` Slice 6.3 reopen/history; emission matrix 29-row

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- human-resources.recruitment recruitment-emission-registry
pnpm --filter @afenda/db test -- hr-candidate-application-status-history-migration
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — 39 passed (17 skipped parity drizzle without DB gate); migration test green; typecheck clean
```

**Next:** Slice 6.5 — Offer (**DONE** @ 2026-07-26).

## Slice 6.4 — Interview — **DONE** (2026-07-26)

### Mission

Interview lifecycle completion: post-schedule interviewer assign/reassign, durable evaluation scorecard (JSONB), hiring decision = evaluation `result`, confidential `privateNotes` field projection via `human-resources.interview.read` / `.record`.

### Command / query map

| Bullet | Command / query |
|---|---|
| schedule | `scheduleInterview` (existing) |
| assign interviewer | `assignInterviewInterviewer` — `scheduled` only · optimistic concurrency · emission `audit_only` |
| evaluation | `recordInterviewEvaluation` — required `scorecard` · completes interview |
| confidential notes | `privateNotes` on evaluation; masked unless actor holds `human-resources.interview.record` |
| scorecard | structured criteria persisted in `hr_interview_evaluation.scorecard_json` (migration `0042`) |
| decision | evaluation `result` (`advance` \| `hold` \| `reject`) — no separate decision command |
| field projection | `getInterviewEvaluation` + `interview-field-projection.ts`; queries bound to `human-resources.interview.read` |

### Complete (evidence)

* DDL `0042_hr_interview_evaluation_scorecard.sql` + Drizzle `scorecardJson` CHECK + migration test
* Permission catalog + session copy: `human-resources.interview.read` (queries); `.record` (mutations + confidential unmask)
* Store + Memory/Drizzle: `assignInterviewInterviewer`; scorecard round-trip on `recordInterviewEvaluation`
* Manifest: interview queries → `.read`; assign/schedule/cancel/evaluate → `.record`
* Recruitment classified command count: **30** (`HUMAN_RESOURCES_RECRUITMENT_COMMAND_IDS` incl. `interview.assign-interviewer`)
* Tests: `human-resources.recruitment.test.ts` Slice 6.4 block (assign/reassign · scorecard+result · projection read vs record · post-complete assign deny); projection test; emission matrix 30-row

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- human-resources.recruitment
pnpm --filter @afenda/db test -- hr-interview-evaluation-scorecard-migration
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — 40 passed (17 skipped parity drizzle without DB gate); migration test green; typecheck clean
```

**Next:** Slice 6.5 — Offer (**DONE** @ 2026-07-26).

## Slice 6.5 — Offer — **DONE** (2026-07-26)

### Mission

Offer lifecycle completion with pre-hire compensation proposal reference: `draft → approved → issued → accepted | declined | withdrawn | expired`; active-offer uniqueness on `draft | approved | issued`; reservation consumption remains on `acceptOffer` only.

### Command / query map

| Bullet | Command / query |
|---|---|
| draft | `createOffer` · `amendOfferDraft` (optional `compensationProposalId` while draft) |
| approve (offer) | `approveOffer` — requires non-null approved proposal on same application |
| issue | `issueOffer` — **only** from `approved` |
| accept | `acceptOffer` — consumes active headcount reservation |
| reject | `declineOffer` |
| withdraw / expire | `withdrawOffer` · `expireOffer` (also from `approved`) |
| compensation proposal reference | `createCompensationProposal` · `amendCompensationProposal` · `approveCompensationProposal` · `getCompensationProposal` · `listCompensationProposals` |
| reservation consumption | on `acceptOffer` only — decline / withdraw / expire do not consume |

### Complete (evidence)

* DDL `0043_hr_compensation_proposal.sql` — `hr_compensation_proposal` + `hr_employment_offer.compensation_proposal_id` + active uidx `(draft|approved|issued)` + migration test
* Guards: `canTransitionOfferStatus` — no `draft → issued`; `assertOfferReadyForApproval` · `assertOfferProposalMutable` · `validateOfferCompensationProposalAttachment`
* Store + Memory/Drizzle: proposal CRUD/approve; offer `approveOffer`; issue gated to `approved`
* Events: `human-resources.offer.approved.v1` · `human-resources.compensation-proposal.approved.v1` (domain_event on approve)
* Recruitment classified command count: **30** (`HUMAN_RESOURCES_RECRUITMENT_COMMAND_IDS` incl. `offer.approve`)
* Compensation classified command count: **21** (`HUMAN_RESOURCES_COMPENSATION_BENEFITS_COMMAND_IDS` incl. 3× `compensation-proposal.*`)
* Tests: `human-resources.recruitment.test.ts` Slice 6.5 block; `human-resources.workforce-planning.test.ts` accept consumes + decline/withdraw/expire non-consume; `human-resources.recruitment.parity.test.ts` Slice 6.5 FK parity; emission matrix 30-row recruitment

**Compensation proposal resolution:** `hr_employment_offer.compensation_proposal_id` → `hr_compensation_proposal.id` (same `organization_id` + `application_id`). Nullable on draft; required before `approveOffer`; proposal must be `approved`; immutable once offer is `approved` or `issued`.

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- human-resources.recruitment human-resources.workforce-planning recruitment-emission-registry
pnpm --filter @afenda/db test -- hr-compensation-proposal-migration
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — 72 passed (5 files: recruitment + WFP + parity + hire-orchestration + emission-registry); migration test green; typecheck clean
```

**Residual (Slice 6.6+):** proposal → `hr_employee_compensation` conversion post-accept — out of scope; FK preserved on accept.

**Next:** Slice 6.8 — Probation and confirmation (Slice 6.7 onboarding **DONE**).

## Slice 6.6 — Hire orchestration — **DONE** (2026-07-26)

### Mission

`HR-COREORG-HIRE-ORCHESTRATION`

Phase **1 documented saga** shipped as the enterprise production hire path. **Phase 2 atomic `hireFromAcceptedOffer` TX is deferred** — reservation is already consumed inside `acceptOffer`, and domain work spans workforce-foundation / core / lifecycle / hire-progress stores with no approved cross-slice transaction host.

### Saga step map

| Step | Forward command | Idempotency | Compensation on later failure |
|------|-----------------|-------------|-------------------------------|
| 0 Load | read offer/candidate/requisition | N/A | none |
| 1 Reservation | `listHeadcountReservations` verify (no active) | N/A | none (already consumed on accept) |
| 2 Person | `createPerson` | `{sagaKey}:person` | retain identity; ledger marks compensated |
| 3 Employee | `createEmployee` | `{sagaKey}:employee` | retain; ledger marks compensated |
| 4 Employment | `hireEmployment` | ledger only | `terminateEmployment` |
| 5 Worker | `createWorker` | `{sagaKey}:worker` | `changeWorkerStatus` → `inactive` |
| 6 Assignment | `createAssignment` | ledger only | `endAssignment` |
| 7 Onboarding | `startOnboarding` (`sourceOfferId`) | `{sagaKey}:onboarding` | no cancel — ledger `onboardingOrphaned=true` |

Durable progress: `hr_hire_attempt` (`packages/data-plane/db/drizzle/0044_hr_hire_attempt.sql`) · command `human-resources.hire.from-accepted-offer` · permission `human-resources.hire.orchestrate` · event `human-resources.hire.from-accepted-offer.completed.v1`.

### Verify

```bash
pnpm --filter @afenda/human-resources test -- human-resources.recruitment human-resources.core human-resources.foundation human-resources.lifecycle human-resources.hire-orchestration
pnpm --filter @afenda/human-resources typecheck
```

## Slice 6.7 — Onboarding — **DONE** (2026-07-26)

**Commands / queries**

| Surface | Id |
|---|---|
| `startOnboarding` | `human-resources.onboarding.start` |
| `completeOnboardingTask` | `human-resources.onboarding.complete-task` |
| `completeOnboarding` | `human-resources.onboarding.complete` |
| `recordOnboardingOrientation` | `human-resources.onboarding.record-orientation` |
| `recordOnboardingEquipmentHandoff` | `human-resources.onboarding.record-equipment-handoff` |
| `recordOnboardingAccessHandoff` | `human-resources.onboarding.record-access-handoff` |
| `getOnboardingCase` | `human-resources.onboarding.case.get` |
| `listOnboardingTasks` | `human-resources.onboarding.tasks.list` |
| `getOnboardingOrientationByCase` | `human-resources.onboarding-orientation.get-by-case` |
| `getOnboardingEquipmentHandoffByCase` | `human-resources.onboarding-equipment-handoff.get-by-case` |
| `getOnboardingAccessHandoffByCase` | `human-resources.onboarding-access-handoff.get-by-case` |

**Verify**

```bash
pnpm --filter @afenda/human-resources test -- human-resources.lifecycle human-resources.compliance human-resources.hire-orchestration onboarding-completion-readiness correlation-integrity
pnpm --filter @afenda/human-resources test -- human-resources.lifecycle.parity
pnpm --filter @afenda/db test -- hr-onboarding-completion-facts-migration tenancy.test
pnpm --filter @afenda/human-resources typecheck
```

Verify (2026-07-26):

```text
pnpm --filter @afenda/human-resources test -- human-resources.lifecycle human-resources.compliance human-resources.hire-orchestration onboarding-completion-readiness correlation-integrity  → 69 passed
pnpm --filter @afenda/human-resources test -- human-resources.lifecycle.parity  → 8 passed when runDrizzleParity gate open (memory + drizzle; incl. onboarding handoff facts)
pnpm --filter @afenda/db test -- hr-onboarding-completion-facts-migration  → exit 0
pnpm --filter @afenda/db test -- tenancy.test  → current inventory 245 hard-tenant roots, including 136 `hr_*` roots
pnpm audit:tenancy-nulls                         → 243 audited, 2 unrelated pending-DDL skips
pnpm --filter @afenda/human-resources typecheck  → exit 0
```

Complete:

* onboarding case;
* task checklist;
* document requirements;
* work eligibility;
* equipment and access handoffs;
* orientation;
* completion.

## Slice 6.8 — Probation and confirmation — **DONE** (2026-07-26)

Command map:

| Capability | Command / query |
|---|---|
| Open probation | `openProbation` |
| Interim review (stays open) | `recordProbationAssessment` |
| List assessments | `listProbationAssessments` |
| Extend with reason/evidence | `extendProbation` |
| Outcome (passed / failed) | `recordProbationOutcome` |
| List reviews by employment | `listProbationReviewsByEmployment` |
| Confirm employment | `confirmEmployment` |

Events: `human-resources.probation.assessment.recorded.v1` (new); enriched payloads on `probation.extended.v1`, `probation.reviewed.v1`, `employee.confirmed.v1`.

Verify (2026-07-26):

```text
pnpm --filter @afenda/db test -- hr-probation-confirmation-lifecycle-migration  → exit 0
pnpm --filter @afenda/human-resources typecheck                                  → exit 0
pnpm --filter @afenda/human-resources test -- human-resources.lifecycle          → 19 passed
pnpm --filter @afenda/human-resources test -- lifecycle-emission-registry          → exit 0
pnpm --filter @afenda/human-resources test -- authorization-policy-coverage        → exit 0
pnpm --filter @afenda/human-resources test -- correlation-integrity                → 24 passed
```

Complete:

* probation period;
* review;
* extension;
* confirmation;
* rejection;
* effective date;
* reason and evidence.

## Slice 6.9 — Transfer — **DONE** (2026-07-26)

### Mission

Transfer closes the open assignment, opens a successor with write-time org-dimension / manager / calendar snapshots, records `hr_employment_movement` (`movementKind: transfer`), audit on the movement, and `human-resources.employee.transferred.v1`. **Store + command shipped in Slices 4.5 / 5.6** — Slice 6.9 closed with dedicated lifecycle tests + scratch evidence (no second transfer command).

### Command / query map

| Bullet | Command / query |
|---|---|
| assignment end | predecessor `endsOn` = day before `effectiveOn`; `successorAssignmentId` / `transferMovementId` wired |
| new assignment | successor `startsOn` = `effectiveOn`; `predecessorAssignmentId`; snapshots at write |
| employment movement | `transferAssignment` → `hr_employment_movement` `movementKind: transfer`; `fromAssignmentId` / `toAssignmentId` |
| manager change | `resolveAssignmentContextSnapshots` → `managerEmployeeIdSnapshot` on successor |
| dimension snapshot | `OrganizationDimensionDirectoryPort.resolveRequiredAsOf` at `effectiveOn` → `organizationDimensions` on successor |
| calendar change | `resolveAssignmentContextSnapshots` → `workCalendarIdSnapshot` on successor |
| audit + event | audit `entity: hr_employment_movement` `action: CREATE`; outbox `human-resources.employee.transferred.v1` |
| idempotency | `fingerprintTransfer`; key reuse with different payload → `CONFLICT` |

Canonical command: `transferAssignment` · `human-resources.assignment.transfer`

### Complete (evidence)

* Modules: `packages/erp/human-resources/src/lifecycle/transfer.ts`, `shared/assignment-snapshots.ts`, `shared/fingerprint.ts` (`fingerprintTransfer`), `schemas/lifecycle.ts` (`transferAssignmentInputSchema`), Memory/Drizzle `adapters/*/lifecycle.ts` (`transferAssignment`)
* Event: `human-resources.employee.transferred.v1` (emissions registry + correlation-integrity coverage retained)
* Tests: `human-resources.lifecycle.test.ts` — `describe("Slice 6.9 — Transfer")` (lineage + audit + event; context-change manager/calendar/dimension snapshots; same position / frozen target / idempotency mismatch); `human-resources.lifecycle.parity.test.ts` — `movementKind === "transfer"` + existing lineage/event parity; core Slice 4.5 / 5.6 lineage tests retained

**Verify:**

```bash
pnpm --filter @afenda/human-resources test -- human-resources.lifecycle
pnpm --filter @afenda/human-resources typecheck
# Exit 0 — Test Files 1 passed | 1 skipped (2); Tests 19 passed | 4 skipped (23); typecheck clean
```

**Next:** Slice 6.10 — Termination and offboarding (**DONE** — see below).

## Slice 6.10 — Termination and offboarding — **DONE** (2026-07-26)

Command map:

| Capability | Command / query |
|---|---|
| Propose termination (draft + `rehireEligible`) | `proposeTermination` → `human-resources.termination.propose` |
| Approve termination facts | `approveTermination` → `human-resources.termination.approve` |
| Finalize approved draft | `finalizeTermination` → `human-resources.termination.finalize` (input: `terminationId` + `expectedVersion`) |
| Get termination | `getTermination` |
| Start offboarding (+ seed access/payroll facts) | `startOffboarding` |
| Record access revocation | `recordOffboardingAccessRevocation` |
| Record final payroll handoff | `recordOffboardingPayrollHandoff` |
| Get access revocation / payroll handoff | `getOffboardingAccessRevocationByCase` · `getOffboardingPayrollHandoffByCase` |
| Complete offboarding (gates access `revoked` + payroll `ready`) | `completeOffboarding` |

Migration: `0047_hr_termination_offboarding_facts` — `hr_termination.approved_at` · `approved_by` · `rehire_eligible`; `hr_offboarding_access_revocation`; `hr_offboarding_payroll_handoff`.

Events: `human-resources.employee.terminated.v1` on finalize; offboarding fact records audit-only (existing offboarding/clearance domain events unchanged).

### Complete (evidence)

* Modules: `packages/erp/human-resources/src/lifecycle/termination.ts`, `lifecycle/offboarding.ts`, `shared/lifecycle-guards.ts`, `schemas/lifecycle.ts`, Memory/Drizzle `adapters/*/lifecycle.ts`
* Migration: `packages/data-plane/db/drizzle/0047_hr_termination_offboarding_facts.sql` · test `hr-termination-offboarding-facts-migration.test.ts`
* Authorization: `shared/authorization-policies/lifecycle.ts` prefixes for onboarding/offboarding fact query IDs
* Tests: `human-resources.lifecycle.test.ts` — happy path (propose → approve → finalize → offboarding facts → complete); `describe("Slice 6.10 — Termination and offboarding")` (finalize without approve, double approve, open draft conflict, double finalize, complete blocked without facts); `human-resources.lifecycle.parity.test.ts` — termination + offboarding fact row parity; `correlation-integrity.test.ts` — termination finalize + offboarding fact correlation; `lifecycle-emission-registry.test.ts` — audit-only propose/approve/fact commands

Verify (2026-07-26):

```text
pnpm --filter @afenda/human-resources test -- human-resources.lifecycle          → exit 0
pnpm --filter @afenda/human-resources test -- lifecycle-emission-registry       → exit 0
pnpm --filter @afenda/db test -- hr-termination-offboarding                     → exit 0
pnpm --filter @afenda/human-resources typecheck                                → exit 0
```

Complete:

* termination proposal;
* approval;
* effective termination;
* offboarding case;
* clearance;
* access revocation facts;
* final payroll handoff;
* rehire eligibility.

## Slice 6.11 — Recruitment and lifecycle Actions — **DONE** (2026-07-26)

Delivered:

* `apps/web/app/actions/hr-recruitment.ts` — 45 actions (requisition · candidate · application · interview · offer)
* `apps/web/app/actions/hr-hiring.ts` — 1 action (`hireFromAcceptedOfferAction`)
* `apps/web/app/actions/hr-onboarding.ts` — 11 actions (onboarding cmds + employee.read queries)
* `apps/web/app/actions/hr-lifecycle.ts` — 13 actions (probation · confirm · termination — no transfer duplicate)
* `apps/web/app/actions/hr-offboarding.ts` — 12 actions (offboarding cmds + employee.read queries)
* `apps/web/modules/identity/domain/session-permission.ts` — added `human-resources.hire.orchestrate` deny message
* `apps/web/app/actions/hr-mutation-context.ts` — `hrActionSchema` refined-schema fallback (Zod `.omit` on refined package schemas)
* Contract tests: `hr-recruitment-actions.test.ts` · `hr-hiring-actions.test.ts` · `hr-onboarding-actions.test.ts` · `hr-lifecycle-actions.test.ts` · `hr-offboarding-actions.test.ts`

Action inventory (delegate only — no domain logic in `apps/web`):

| File | Actions |
|------|---------|
| `hr-recruitment.ts` | `createDraftRequisitionAction` · `amendRequisitionAction` · `assignHiringManagerAction` · `submitRequisitionAction` · `approveRequisitionAction` · `openRequisitionAction` · `placeRequisitionOnHoldAction` · `closeRequisitionAction` · `cancelRequisitionAction` · `getRequisitionAction` · `listRequisitionsAction` · `createCandidateAction` · `updateCandidateProfileAction` · `withdrawCandidateConsentAction` · `changeCandidateRetentionAction` · `anonymizeCandidateAction` · `getCandidateAction` · `listCandidatesAction` · `detectCandidateDuplicatesAction` · `createApplicationAction` · `moveApplicationToInReviewAction` · `moveApplicationToInterviewingAction` · `rejectApplicationAction` · `withdrawApplicationAction` · `reopenApplicationAction` · `getApplicationAction` · `listApplicationsAction` · `listApplicationStatusHistoryAction` · `scheduleInterviewAction` · `cancelInterviewAction` · `assignInterviewInterviewerAction` · `recordInterviewEvaluationAction` · `getInterviewAction` · `listInterviewsAction` · `getInterviewEvaluationAction` · `createOfferAction` · `amendOfferDraftAction` · `approveOfferAction` · `issueOfferAction` · `acceptOfferAction` · `declineOfferAction` · `expireOfferAction` · `withdrawOfferAction` · `getOfferAction` · `listOffersAction` |
| `hr-hiring.ts` | `hireFromAcceptedOfferAction` |
| `hr-onboarding.ts` | `startOnboardingAction` · `completeOnboardingTaskAction` · `recordOnboardingOrientationAction` · `recordOnboardingEquipmentHandoffAction` · `recordOnboardingAccessHandoffAction` · `completeOnboardingAction` · `getOnboardingCaseAction` · `listOnboardingTasksAction` · `getOnboardingOrientationByCaseAction` · `getOnboardingEquipmentHandoffByCaseAction` · `getOnboardingAccessHandoffByCaseAction` |
| `hr-lifecycle.ts` | `openProbationAction` · `extendProbationAction` · `recordProbationAssessmentAction` · `recordProbationOutcomeAction` · `getProbationReviewAction` · `listProbationReviewsByEmploymentAction` · `listProbationAssessmentsAction` · `confirmEmploymentAction` · `getEmploymentConfirmationAction` · `proposeTerminationAction` · `approveTerminationAction` · `finalizeTerminationAction` · `getTerminationAction` |
| `hr-offboarding.ts` | `startOffboardingAction` · `completeOffboardingTaskAction` · `recordExitInterviewAction` · `recordClearanceAction` · `recordOffboardingAccessRevocationAction` · `recordOffboardingPayrollHandoffAction` · `completeOffboardingAction` · `getOffboardingCaseAction` · `listOffboardingTasksAction` · `getClearanceByOffboardingCaseAction` · `getOffboardingAccessRevocationByCaseAction` · `getOffboardingPayrollHandoffByCaseAction` |

Intentionally unwired (package surface absent or owned elsewhere):

| Surface | Reason |
|---------|--------|
| `transferAssignment` | Already on `hr-assignments.ts` (Slice 6.9) |
| `terminateEmployment` | Direct status amend on `hr-employment.ts`; lifecycle uses propose→approve→finalize |
| `compensateHireAttemptProgress` | Saga-internal — not on main package barrel |
| `listOnboardingCases` · `listOffboardingCases` · `listTerminations` · `getHireAttempt` | No public package export |

Verify:

```bash
pnpm --filter @afenda/web test -- hr-recruitment hr-hiring hr-onboarding hr-lifecycle hr-offboarding
pnpm --filter @afenda/web typecheck
```

Verify output (2026-07-26):

```
Test Files  5 passed (5)
     Tests  15 passed (15)
```

Phase 6 Action files: **0 type errors** (`hr-recruitment` · `hr-hiring` · `hr-onboarding` · `hr-lifecycle` · `hr-offboarding` · `hr-mutation-context` refinement path). `@afenda/web` typecheck **exit 0** (2026-07-26) after corporate-administration readonly integration alignment.

## Phase 6 exit criteria — **MET** (2026-07-26)

* Requisition-to-hire journey callable via Actions — recruitment → hiring Actions wire full package path through `acceptOfferAction` / `hireFromAcceptedOfferAction`.
* Offer acceptance handoff exposed — `acceptOfferAction` → `{ handoff: OfferAcceptanceHandoff }`; hire orchestration delegates to package saga.
* Onboarding and offboarding tracked — dedicated Action files wire cmds + reads.
* Confidential interview data protected — Actions delegate to package `getInterviewEvaluation` (projection + `interview.record` gate); no adapter bypass in `apps/web`.
* Recruitment and lifecycle Actions tested — **15/15** contract tests green (authz envelope · validation · happy-path delegate with org/actor/correlation/idempotency).
* No domain logic duplicated in apps/web — Actions validate + stamp session + delegate to `@afenda/human-resources` via `createHumanResourcesCommandOptions()` only.

---

# Phase 7 — Leave and time completion

## Objective

Complete operational workforce management and payroll-ready time facts.

**Status (2026-07-26):** **In progress** — Slices **7.1–7.2 DONE** (policy admin · entitlement ledger); **7.8–7.9 DONE** (attendance · timesheets); **7.10 DONE** (overtime); **7.3–7.7 · 7.11** open (request workflow · handoff permission · product Actions · calendar/shift · attendance connector).

## Slice 7.1 — Leave policy administration — **DONE** (2026-07-26)

### Mission

`HR-OPS-LEAVE-POLICY-ADMIN`

Versioned leave policies with eligibility, balance-rule configuration, negative-balance flag, publish, and retire — policy admin only (entitlement execution deferred to Slice 7.2).

### Command / query map

| Surface | ID |
|---|---|
| Commands | `human-resources.leave-policy.create` · `.update` · `.publish` · `.supersede` · `.archive` |
| Queries | `human-resources.leave-policy.get` · `.list` · `.resolve-applicable` |
| Permissions | `human-resources.leave-policy.read` · `human-resources.leave-policy.manage` |
| Emissions | audit-only on all five policy commands ([`emissions/domains/leave.ts`](../../packages/erp/human-resources/src/emissions/domains/leave.ts)) |

### Policy config (7.1) vs entitlement execution (7.2)

| Concern | Slice 7.1 (persisted on `hr_leave_policy`) | Slice 7.2 (deferred) |
|---|---|---|
| Accrual | `accrualBasis` · `accrualFrequency` · `accrualQuantityPerPeriod` | `accrueLeaveEntitlement` applies quantities per period |
| Carry forward | `carryForwardEnabled` · `carryForwardMaxQuantity` | `carryForwardLeaveEntitlement` moves balances |
| Expiry | `entitlementExpiryRule` · `entitlementExpiryDays` | `expireLeaveEntitlement` closes entitlements |
| Negative balance | `allowsNegativeBalance` (consumed by request guards) | balance consumption / ledger |
| Eligibility | `hr_leave_policy_eligibility` (`minTenureDays` · `allowedEmploymentStatuses`) | resolve-applicable filter only |

### Evidence

* Consolidated migration [`0000_damp_blue_shield.sql`](../../packages/data-plane/db/drizzle/0000_damp_blue_shield.sql) — balance-rule columns + CHECK constraints on `hr_leave_policy`
* Domain: [`leave-policy.ts`](../../packages/erp/human-resources/src/leave/leave-policy.ts) · [`leave-policy-lineage.ts`](../../packages/erp/human-resources/src/leave/leave-policy-lineage.ts) · [`schemas/leave.ts`](../../packages/erp/human-resources/src/schemas/leave.ts) · [`shared/leave-policy-balance-rules.ts`](../../packages/erp/human-resources/src/shared/leave-policy-balance-rules.ts)
* Adapters: Memory + Drizzle parity; Drizzle resolve loads `published` **and** `superseded` for lineage as-of
* Tests: [`leave-policy-administration.test.ts`](../../packages/erp/human-resources/__tests__/leave-policy-administration.test.ts) · [`leave-policy-lineage.test.ts`](../../packages/erp/human-resources/__tests__/leave-policy-lineage.test.ts) · [`human-resources.leave.parity.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.leave.parity.test.ts) (supersede + config parity)
* Verify:

```text
pnpm --filter @afenda/human-resources test -- leave-policy-administration leave-policy-lineage human-resources.leave
# Test Files  4 passed (4) · Tests  35 passed | 4 skipped (39)

pnpm --filter @afenda/human-resources test -- human-resources.leave.parity
# Test Files  1 passed (1) · Tests  4 passed | 4 skipped (8)

pnpm --filter @afenda/human-resources typecheck
# exit 0

pnpm --filter @afenda/db test -- hr-leave-policy-balance-rules
# Test Files  1 passed (1) · Tests  1 passed (1)
```

### Delivered

* Policy versioning via `supersedeLeavePolicy` + `supersedesPolicyId` + effective range + lineage as-of resolution
* Eligibility on create/update/supersede with resolve-applicable tenure/status gates
* Balance-rule configuration persisted (accrual basis/frequency/quantity · carry-forward · expiry rule/days) — not executed until 7.2
* `allowsNegativeBalance` flag on policy rows
* Draft-only edits until publish; `publishLeavePolicy` / `archiveLeavePolicy` retirement path
* Slice 4.7 overlap guards unchanged

## Slice 7.2 — Entitlements and balances — **DONE** (2026-07-26)

Append-only entitlement ledger, derived balance, consumption/restoration evidence, historical readback.

### Command / query map

| Surface | ID |
|---|---|
| Commands | `human-resources.leave-entitlement.grant` · `.accrue` · `.carry-forward` · `.expire` · `.adjust` |
| Queries | `human-resources.leave-entitlement.get` · `.list` · `human-resources.leave-balance.get` · `.reconcile` |
| Adjustment kinds | `manual` · `accrual` · `carry_forward` · `expiry` · `consumption` · `cancellation_reversal` |
| Balance rule | `openingQuantity + Σ(posted adjustment deltas)` via [`computeLeaveBalance`](../../packages/erp/human-resources/src/shared/leave-balance.ts) |
| Consumption / restoration | Posted by request approve / cancel-approved paths (Slice 7.3 workflow); ledger effects proven here |

### Policy execution (7.1 deferral closed)

| Concern | Guard |
|---|---|
| Accrual | `accrueLeaveEntitlement` rejects `accrualBasis: none`; quantity must match `accrualQuantityPerPeriod` when set |
| Carry forward | `carryForwardLeaveEntitlement` requires `carryForwardEnabled`; enforces balance and `carryForwardMaxQuantity` |
| Manual adjust | Signed delta; projected balance respects `allowsNegativeBalance` |
| Expire | Explicit ops close — posts `expiry` wipe while active, then `expired` status (Memory + Drizzle atomic) |

### Ledger semantics

* **Grant:** published policy only; `openingQuantity` seeds balance
* **Carry-forward:** source `carry_forward` debit `-carriedQuantity`; target `openingQuantity = carriedQuantity` (no target credit double-count)
* **Expire:** `expiry` adjustment clears residual while active; zero-balance skip
* **Historical readback:** `reconcileLeaveBalance` — posted adjustments ordered `createdAt` then `id`

### Evidence

* Domain: [`entitlement.ts`](../../packages/erp/human-resources/src/leave/entitlement.ts) · [`leave-balance.ts`](../../packages/erp/human-resources/src/shared/leave-balance.ts) · [`leave-guards.ts`](../../packages/erp/human-resources/src/shared/leave-guards.ts)
* Adapters: Memory carry/expire ordering · Drizzle [`buildCarryForwardEntitlementSql`](../../packages/erp/human-resources/src/adapters/drizzle/leave-sql-builders.ts) · [`buildExpireEntitlementSql`](../../packages/erp/human-resources/src/adapters/drizzle/leave-sql-builders.ts)
* Tests: [`leave-entitlement-ledger.test.ts`](../../packages/erp/human-resources/__tests__/leave-entitlement-ledger.test.ts) · [`human-resources.leave.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.leave.test.ts) · [`human-resources.leave.parity.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.leave.parity.test.ts) (entitlement ledger parity)
* Verify:

```text
pnpm --filter @afenda/human-resources test -- leave-entitlement-ledger human-resources.leave leave-concurrency leave-failure-injection
# Test Files  3 passed | 2 skipped (5) · Tests  33 passed | 26 skipped (59)

pnpm --filter @afenda/human-resources test -- leave-entitlement-ledger
# Test Files  1 passed (1) · Tests  6 passed (6)

pnpm --filter @afenda/human-resources typecheck
# exit 0
```

### Delivered

* entitlement grant on published policy with `openingQuantity`
* manual `adjustLeaveEntitlement` (signed delta + negative-balance guard)
* `accrueLeaveEntitlement` with policy quantity guard and idempotency
* `carryForwardLeaveEntitlement` / `expireLeaveEntitlement` with correct ledger semantics
* consumption (`consumption`) and restoration (`cancellation_reversal`) balance effects via request paths
* historical ledger via posted adjustments + `reconcileLeaveBalance`

**Next:** Slice 7.5 — Leave product Actions *(closed — see Slice 7.5 below)*.

## Slice 7.3 — Leave request workflow — **DONE** (2026-07-26)

### Mission

`HR-OPS-LEAVE-REQUEST-WORKFLOW`

Full leave request command surface on [`leave-request.ts`](../../packages/erp/human-resources/src/leave/leave-request.ts) — no parallel request API.

### Command / query map

| Surface | ID |
|---|---|
| Commands | `human-resources.leave-request.create-draft` · `.submit` · `.amend` · `.approve` · `.reject` · `.return` · `.withdraw` · `.cancel-approved` |
| Queries | `human-resources.leave-request.get` · `.list` · `.list-pending-approval` · `.team-calendar` · `human-resources.approved-leave-handoff.get` |
| Permissions | `human-resources.leave-request.own` · `human-resources.leave-request.approve-team` · `human-resources.leave-request.backdate` · `human-resources.leave-request.sensitive-read` |
| Ledger (7.2) | approve → `consumption`; cancel-approved → `cancellation_reversal` |

### Cancel semantics

| Command | Status transition | Meaning |
|---|---|---|
| `return` | `submitted` → `returned` | Manager send-back (amendable) — **not** cancel |
| `withdraw` | → `withdrawn` | Employee pre-approval cancel |
| `cancel-approved` | `approved` → `cancelled` | Post-approval cancel + `cancellation_reversal` ledger |

Checklist **cancel** = `withdraw` + `cancel-approved`.

### Guards

| Guard | When |
|---|---|
| `assertNoLeaveOverlap` | submit · approve (Slice **4.7** preserved — unchanged) |
| `assertNoSelfApproval` | approve (`allowSelfApproval` policy flag) |
| `assertApproverIsPrimaryManager` | approve · reject · return (skipped when allowed self-approval: actor is request employee + policy permits) |
| `requireLeaveRequestBackdatePermission` | create-draft · amend when `isBackdated` |
| `requireLeaveCancelApprovedPermission` | cancel-approved (`approve-team` **or** `backdate`) |
| `assertLeaveRequestAmendable` | amend (`draft` \| `returned` only) |

### Evidence

* Domain: [`leave-request.ts`](../../packages/erp/human-resources/src/leave/leave-request.ts) · [`leave-guards.ts`](../../packages/erp/human-resources/src/shared/leave-guards.ts) · [`leave-command.ts`](../../packages/erp/human-resources/src/shared/leave-command.ts)
* Adapters: Memory + Drizzle ([`adapters/memory/leave.ts`](../../packages/erp/human-resources/src/adapters/memory/leave.ts) · [`adapters/drizzle/leave.ts`](../../packages/erp/human-resources/src/adapters/drizzle/leave.ts))
* Manifest: [`module.manifest.ts`](../../packages/erp/human-resources/src/module.manifest.ts) — supplemental `backdate` + `sensitive-read` registered
* Tests: [`human-resources.leave.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.leave.test.ts) — `describe("Slice 7.3 leave request workflow")` · [`human-resources.leave.parity.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.leave.parity.test.ts) · [`leave-concurrency.test.ts`](../../packages/erp/human-resources/__tests__/leave-concurrency.test.ts)
* Verify:

```text
pnpm --filter @afenda/human-resources test -- human-resources.leave leave-concurrency
# Test Files  2 passed | 1 skipped (3) · Tests  38 passed | 17 skipped (55)

pnpm --filter @afenda/human-resources typecheck
# exit 0
```

### Delivered

* submit with balance + overlap guards
* amend on `draft` / `returned`; denied on `submitted`
* cancel via `withdraw` (pre-approval) and `cancel-approved` (post-approval + ledger reversal)
* approve / reject with primary-manager authority at approval `asOf` (= request `startDate`)
* self-approval blocked by default; allowed when `allowSelfApproval` + actor is request employee
* overlap on submit and approve (`assertNoLeaveOverlap`; Slice 4.7 overlap statuses unchanged)
* backdated create/amend gated by `human-resources.leave-request.backdate`
* Memory vs Drizzle parity for return/amend/reject/withdraw/cancel-approved workflow
* Slice **4.7 overlap guards unchanged**

## Slice 7.4 — Leave handoff permission — **DONE** (2026-07-26)

### Mission

`HR-OPS-LEAVE-HANDOFF-PERMISSION`

Add least-privilege:

```text
human-resources.leave.handoff.read
```

### Before / after

| Surface | Before | After |
|---|---|---|
| `getApprovedLeaveHandoff` query auth | `human-resources.leave-request.approve-team` | `human-resources.leave.handoff.read` |
| Manager with only `approve-team` | Could read handoff | Denied |
| Payroll/integration consumer | Needed manager approval permission | Grant `leave.handoff.read` only |

### Permission / manifest map

| Surface | ID |
|---|---|
| Permission | `human-resources.leave.handoff.read` (`HUMAN_RESOURCES_PERMISSION_LEAVE_HANDOFF_READ`) |
| Query | `human-resources.approved-leave-handoff.get` |
| Manifest remap | `APPROVED_LEAVE_HANDOFF_GET` → `leave.handoff.read` (was `leave-request.approve-team`) |
| Policy | `hr.leave` — operation prefix `human-resources.approved-leave-handoff.` (unchanged) |

### Evidence

* [`permissions.ts`](../../packages/erp/human-resources/src/permissions.ts) — constant + `HUMAN_RESOURCES_PERMISSION_CODES`
* [`module.manifest.ts`](../../packages/erp/human-resources/src/module.manifest.ts) — query auth remap
* [`platform-permission-catalog.ts`](../../packages/data-plane/db/src/platform-permission-catalog.ts) — catalog row (sensitive)
* Tests: [`human-resources.leave.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.leave.test.ts) — `requires leave.handoff.read (not leave-request.approve-team) for approved handoff query` · `returns approved leave handoff with plan shape` · [`manifest.test.ts`](../../packages/erp/human-resources/__tests__/manifest.test.ts) — handoff query auth parity
* App integration: [`hr-leave.ts`](../../apps/web/app/actions/hr-leave.ts) — 23 leave Actions including `getApprovedLeaveHandoffAction` on `leave.handoff.read`; [`session-permission.ts`](../../apps/web/modules/identity/domain/session-permission.ts) deny message; [`hr-leave-actions.test.ts`](../../apps/web/__tests__/hr-leave-actions.test.ts)
* Residual closed: **HR-OPS-P1-002**
* App deny message: [`session-permission.ts`](../../apps/web/modules/identity/domain/session-permission.ts) — `human-resources.leave.handoff.read`
* Verify:

```text
pnpm --filter @afenda/human-resources test -- human-resources.leave subject-aware-authorization
# Test Files  2 passed (2) · Tests  38 passed | 7 skipped (45)

pnpm --filter @afenda/human-resources typecheck
# exit 0

pnpm --filter @afenda/db db:ensure-permission-catalog
# permissionCount: 230 · exit 0
```

**Consumer note:** payroll/integration jobs must grant `human-resources.leave.handoff.read`, not `leave-request.approve-team`. Product Actions wired in Slice 7.5 (`getApprovedLeaveHandoffAction`).

## Slice 7.5 — Leave product Actions — **DONE** (2026-07-26)

### Mission

`HR-ENT-07-PRODUCT-LEAVE-ACTIONS`

Wire leave checklist capabilities to Server Actions — one Action per in-scope package command/query; no product routes/UI in this slice.

### Action inventory

| Group | Action | Permission |
|---|---|---|
| Request | `createDraftLeaveRequestAction` · `submitLeaveRequestAction` · `amendLeaveRequestAction` | `human-resources.leave-request.own` |
| Cancel | `withdrawLeaveRequestAction` | `human-resources.leave-request.own` |
| Cancel | `cancelApprovedLeaveRequestAction` | `human-resources.leave-request.approve-team` |
| Balance | `getLeaveBalanceAction` · `reconcileLeaveBalanceAction` · `getLeaveEntitlementAction` · `listLeaveEntitlementsAction` | `human-resources.leave-entitlement.read` |
| Manager approval | `approveLeaveRequestAction` · `rejectLeaveRequestAction` · `returnLeaveRequestAction` · `listPendingApprovalLeaveRequestsAction` | `human-resources.leave-request.approve-team` |
| Team absence | `listTeamCalendarLeaveRequestsAction` | `human-resources.leave-request.approve-team` |
| Policy admin | `createLeavePolicyAction` · `updateLeavePolicyAction` · `publishLeavePolicyAction` · `supersedeLeavePolicyAction` · `archiveLeavePolicyAction` · `getLeavePolicyAction` · `listLeavePoliciesAction` · `resolveApplicableLeavePolicyAction` | `human-resources.leave-policy.manage` / `.read` |
| Payroll handoff | `getApprovedLeaveHandoffAction` | `human-resources.leave.handoff.read` |

**Intentionally unwired (out of slice):** entitlement mutations (`grant`/`accrue`/etc.); `getLeaveRequest`/`listLeaveRequests`; separate Action-level gates for `backdate`/`sensitive-read` (package guards remain).

### Evidence

* Actions: [`hr-leave.ts`](../../apps/web/app/actions/hr-leave.ts) — 23 exports; refined policy schemas use `parseHrStampedPackageInput` from [`hr-mutation-context.ts`](../../apps/web/app/actions/hr-mutation-context.ts)
* Tests: [`hr-leave-actions.test.ts`](../../apps/web/__tests__/hr-leave-actions.test.ts) — authz deny table (17 cases incl. handoff ≠ `approve-team`), validation fail, happy-path stamp/delegate
* Handoff (7.4): `getApprovedLeaveHandoffAction` gates on `human-resources.leave.handoff.read`

### Verify

```text
pnpm --filter @afenda/web test -- hr-leave
# Test Files  1 passed (1) · Tests  10 passed (10)

pnpm --filter @afenda/human-resources test -- human-resources.leave manifest.test
# Test Files  3 passed (3) · Tests  44 passed | 7 skipped (51)

pnpm --filter @afenda/web typecheck
# exit 0
```

### Delivered

* 23 Server Actions with `ActionResult` envelope via `runOperatorPermissionAction` → `hrActionSchema` / `parseHrStampedPackageInput` → package delegate + `mapPackageResult`
* Payroll handoff least-privilege: `leave.handoff.read` (not `approve-team`)
* Shared HR mutation helpers normalized in `hr-mutation-context.ts` (`HrActionInput`, `parseHrStampedPackageInput`)

## Slice 7.6 — Calendar administration — **DONE** (2026-07-26)

Complete:

* calendars — `createWorkCalendar` · `updateWorkCalendar` · `supersedeWorkCalendar` · `archiveWorkCalendar` · effective lineage preserved on supersede;
* holidays — `addWorkCalendarHoliday` · `removeWorkCalendarHoliday` · `listWorkCalendarHolidays`;
* calendar scope — `assignWorkCalendarScope` · `endWorkCalendarScopeAssignment`;
* employee assignments — `assignEmploymentCalendar` · `endWorkCalendarAssignment`;
* precedence — `employment > employee > location > department > legal_entity > organization` via `calendar-scope-resolution.ts`; same-scope tie → `CONFLICT` fail-closed;
* overrides — `addCalendarDateOverride` · `removeCalendarDateOverride` (Model A on `hr_work_calendar_holiday`);
* historical resolution — `resolveEmploymentCalendar` · `resolveEmployeeWorkCalendar` · shared `work-calendar-lineage.ts` lineage as-of before/after supersede.

Evidence:

* Modules: [`calendar.ts`](../../packages/erp/human-resources/src/time/calendar.ts) · [`calendar-scope-resolution.ts`](../../packages/erp/human-resources/src/time/calendar-scope-resolution.ts) · [`employee-work-calendar-resolution.ts`](../../packages/erp/human-resources/src/time/employee-work-calendar-resolution.ts) · [`work-calendar-lineage.ts`](../../packages/erp/human-resources/src/time/work-calendar-lineage.ts)
* Adapters: [`adapters/memory/time.ts`](../../packages/erp/human-resources/src/adapters/memory/time.ts) · [`adapters/drizzle/time.ts`](../../packages/erp/human-resources/src/adapters/drizzle/time.ts)
* Actions (unchanged): `apps/web/app/actions/hr-time.ts` · `apps/web/app/actions/hr-organization.ts`
* Tests: `human-resources.time.calendar-admin.parity.test.ts` · `human-resources.time.calendar.parity.test.ts` · `successor-lineage.parity.test.ts` · `calendar-scope*.test.ts`

Verify:

```bash
pnpm --filter @afenda/human-resources test -- human-resources.time.calendar calendar-scope
pnpm --filter @afenda/human-resources typecheck
```

Verify output (2026-07-26):

```
@afenda/human-resources test -- human-resources.time.calendar calendar-scope
Test Files  5 passed (5)
     Tests  27 passed | 18 skipped (45)

@afenda/human-resources typecheck: exit 0
```

## Slice 7.7 — Shift management — **DONE** (2026-07-26)

Complete:

* shift definition — `createShift` · `updateShift` (draft-only) · `activateShift` · `deactivateShift` · `supersedeShift` · effective lineage preserved on supersede; breaks cloned to successor;
* breaks — `addShiftBreak` · `removeShiftBreak` · `listShiftBreaks` (ordered by `breakOrder`);
* overnight shift — `computeIsOvernight` default on create/supersede; `isOvernight` persisted; overnight assign + publish;
* scheduling — `assignShift` (`planned`) · `publishShiftAssignment` (`published`);
* assignment lifecycle — `cancelShiftAssignment` · `completeShiftAssignment`; status transitions via `time-guards.ts`;
* schedule amendment — `changeShiftAssignment` → `publicationStatus: changed`; overlap re-check excluding self; attendance lock (Slice 7.8);
* employee schedule query — `getScheduledShiftForEmployeeDate` · `listShiftAssignments` · `listLocationSchedule`;
* overlap rejection — `findOverlappingShiftAssignments` → `CONFLICT` on assign and amend (cancelled excluded).

Evidence:

* Modules: [`shift.ts`](../../packages/erp/human-resources/src/time/shift.ts) · [`scheduling.ts`](../../packages/erp/human-resources/src/time/scheduling.ts) · [`time-guards.ts`](../../packages/erp/human-resources/src/shared/time-guards.ts)
* Adapters: [`adapters/memory/time.ts`](../../packages/erp/human-resources/src/adapters/memory/time.ts) · [`adapters/drizzle/time.ts`](../../packages/erp/human-resources/src/adapters/drizzle/time.ts)
* Actions (unchanged; subset only): [`hr-time.ts`](../../apps/web/app/actions/hr-time.ts) — `createShiftAction` · `supersedeShiftAction` · `assignShiftAction` · `publishShiftAssignmentAction`
* Tests: [`human-resources.time.scheduling-admin.parity.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.time.scheduling-admin.parity.test.ts) · [`human-resources.time.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.time.test.ts) · [`human-resources.time.scheduling.parity.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.time.scheduling.parity.test.ts) · [`human-resources.time.attendance.parity.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.time.attendance.parity.test.ts)

Verify:

```bash
pnpm --filter @afenda/human-resources test -- human-resources.time.scheduling human-resources.time
pnpm --filter @afenda/human-resources typecheck
```

Verify output (2026-07-26):

```
@afenda/human-resources test -- human-resources.time.scheduling human-resources.time
Test Files  10 passed (10)
     Tests  95 passed | 36 skipped (131)

@afenda/human-resources typecheck: exit 0
```

**Residual (document only):** `updateShift` does not recompute `isOvernight` when times change; caller may override the flag explicitly.

## Slice 7.8 — Attendance — **DONE** (2026-07-26)

Complete:

* attendance event — `recordClockIn` · `recordClockOut` · `recordBreakStart` · `recordBreakEnd` · `recordManualAttendance` · `getAttendanceEvent` · `listAttendanceEvents`;
* import batch — `importAttendanceEvents` · `dryRunAttendanceImport`; batch idempotency via `import-keys.ts`;
* replay — re-import idempotency + partial-failure convergence + stable `sourceSequence` ordering via [`event-order.ts`](../../packages/erp/human-resources/src/time/attendance/event-order.ts) (no separate replay API);
* session resolution — `resolveAttendanceSession` · `getAttendanceSession` · `listAttendanceSessions`; triggers `runAttendanceExceptionDetection`;
* exceptions — `createAttendanceException` · `getAttendanceException` · `listAttendanceExceptions` · `listUnresolvedAttendanceExceptions` · canonical detection via `detectAttendanceExceptionsForSession`;
* adjustments — `correctAttendanceEvent` (append-only provenance) · `voidAttendanceEvent` · `listAttendanceAdjustments`;
* break waiver — `approveAttendanceBreakWaiver` · `listAttendanceBreakWaiverDecisions`;
* manager review — `reviewAttendanceException` · `excuseAttendanceException` · `rejectAttendanceException` · `resolveAttendanceException` (attendance exception workflow; not timesheet approval).

Evidence:

* Modules: [`events.ts`](../../packages/erp/human-resources/src/time/attendance/events.ts) · [`import.ts`](../../packages/erp/human-resources/src/time/attendance/import.ts) · [`event-order.ts`](../../packages/erp/human-resources/src/time/attendance/event-order.ts) · [`sessions.ts`](../../packages/erp/human-resources/src/time/attendance/sessions.ts) · [`session-resolution.ts`](../../packages/erp/human-resources/src/time/attendance/session-resolution.ts) · [`exceptions.ts`](../../packages/erp/human-resources/src/time/attendance/exceptions.ts) · [`exception-detection.ts`](../../packages/erp/human-resources/src/time/attendance/exception-detection.ts) · [`break-waivers.ts`](../../packages/erp/human-resources/src/time/attendance/break-waivers.ts) · [`dry-run.ts`](../../packages/erp/human-resources/src/time/attendance/dry-run.ts)
* Adapters: [`adapters/memory/time.ts`](../../packages/erp/human-resources/src/adapters/memory/time.ts) · [`adapters/drizzle/time.ts`](../../packages/erp/human-resources/src/adapters/drizzle/time.ts)
* Actions: [`hr-time.ts`](../../apps/web/app/actions/hr-time.ts) — clock/import/correct/void/session/break-waiver + full exception manager review (`reviewAttendanceExceptionAction` · `excuseAttendanceExceptionAction` · `rejectAttendanceExceptionAction` · `resolveAttendanceExceptionAction`); tests [`hr-time-actions.test.ts`](../../apps/web/__tests__/hr-time-actions.test.ts)
* Tests: [`human-resources.time.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.time.test.ts) · [`human-resources.time.attendance.parity.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.time.attendance.parity.test.ts) · [`human-resources.time.exceptions.parity.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.time.exceptions.parity.test.ts) · [`human-resources.time.event-order.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.time.event-order.test.ts) · [`human-resources.time.timesheet.parity.test.ts`](../../packages/erp/human-resources/__tests__/human-resources.time.timesheet.parity.test.ts) (correct provenance)

Verify:

```bash
pnpm --filter @afenda/human-resources test -- human-resources.time human-resources.time.attendance human-resources.time.exceptions human-resources.time.event-order
pnpm --filter @afenda/human-resources typecheck
pnpm --filter @afenda/web test -- hr-time
```

Verify output (2026-07-26):

```
@afenda/human-resources test -- human-resources.time human-resources.time.attendance human-resources.time.exceptions human-resources.time.event-order
Test Files  10 passed (10)
     Tests  97 passed | 37 skipped (134)

@afenda/human-resources typecheck: exit 0

@afenda/web test -- hr-time
Test Files  1 passed (1)
     Tests  21 passed (21)
```

## Slice 7.9 — Timesheets — **DONE** (2026-07-26)

Complete:

* generation — `createTimesheet` · `generateTimesheetEntries` (attendance + leave-aware + absence detection);
* entries — `addTimesheetEntry` · `updateTimesheetEntry` · `removeTimesheetEntry` · `listTimesheetEntries` · `getTimesheetTotals`;
* employee submission — `submitTimesheet`;
* manager review — `returnTimesheet`;
* approval — `approveTimesheet` · `listTimesheetApprovalDecisions` · `getApprovedTimeHandoff`;
* rejection — `rejectTimesheet`;
* reopen — `returned → draft` and `rejected → draft` via `reopenTimesheet` (clears rejection/approval snapshot);
* legal-minute allocation — [`legal-minute-allocation.ts`](../../packages/erp/human-resources/src/time/legal-minute-allocation.ts) wired through `buildAttendanceTimesheetEntryPlans`;
* leave-aware generation — `ApprovedLeaveQueryPort` + `mapApprovedLeaveFactToEntryInput` in generate path.

Verify (2026-07-26):

```bash
pnpm --filter @afenda/human-resources test -- timesheet
pnpm --filter @afenda/human-resources test -- legal-minute
pnpm --filter @afenda/human-resources test -- human-resources.time.exceptions.parity
pnpm --filter @afenda/human-resources typecheck
```

```
timesheet:        Test Files 1 passed | Tests 4 passed | 4 skipped (8)
legal-minute:     Test Files 2 passed | Tests 8 passed | 4 skipped (12)
exceptions.parity: Test Files 1 passed | Tests 4 passed | 4 skipped (8)
typecheck:        exit 0
```

**Residual (document only):** entry/query Server Actions and ESS/MSS UI deferred to product slices; Drizzle `generateTimesheetEntries` non-transactional insert path unchanged.

## Slice 7.10 — Overtime — **DONE** (2026-07-26)

Complete:

* request — `createOvertimeRequest` + idempotency replay;
* authority resolution — org-local civil `asOf` via [`org-local-work-date.ts`](../../packages/erp/human-resources/src/time/org-local-work-date.ts) + `store.resolveTimeApprovalAuthority`;
* approval — `approveOvertimeRequest` with authority gate;
* rejection — `rejectOvertimeRequest`;
* repeat-approval no-op — identical `approvedMaximumMinutes` short-circuit (no version/audit/outbox); divergent minutes → `CONFLICT` (HR-OPS-P1-006 closed);
* effective-date resolution — LA timezone boundary cases in authority suite;
* payroll handoff — `recordOvertimeActual` → `verifyOvertimeRequest` → distinct `payrollApprovedMinutes`.

Evidence:

* Module: [`overtime.ts`](../../packages/erp/human-resources/src/time/overtime.ts)
* Adapters: [`adapters/memory/time.ts`](../../packages/erp/human-resources/src/adapters/memory/time.ts) · [`adapters/drizzle/time.ts`](../../packages/erp/human-resources/src/adapters/drizzle/time.ts)
* Tests: [`overtime-approval-authority.test.ts`](../../packages/erp/human-resources/__tests__/overtime-approval-authority.test.ts) · [`overtime-approval-authority.parity.test.ts`](../../packages/erp/human-resources/__tests__/overtime-approval-authority.parity.test.ts) · [`helpers/overtime-approval-authority-suite.ts`](../../packages/erp/human-resources/__tests__/helpers/overtime-approval-authority-suite.ts)

Verify:

```bash
pnpm --filter @afenda/human-resources test -- overtime-approval-authority
pnpm --filter @afenda/human-resources typecheck
```

**Product Actions:** [`hr-time.ts`](../../apps/web/app/actions/hr-time.ts) — create · approve · reject · record actual · verify · get · list · list pending (cancel remains package-only until UI needs it).

## Slice 7.11 — Production attendance connector — **DONE** (2026-07-26)

### Mission

`HR-ENT-14-ATTENDANCE-CONNECTOR`

Implement a real `AttendanceSourcePort` with:

* organization isolation;
* incremental cursor;
* idempotency;
* dry run;
* source sequence;
* error capture;
* retry.

**Delivered:** connector preview is available through the product Action boundary; accepted and source-rejected rows preserve original row indexes; rejected rows are retained in import outcomes; mixed batches settle as `partial`; all-rejected batches settle as `failed`; connector cursor and row mapping participate in the idempotency fingerprint.

**Evidence:** `production-attendance-source.test.ts` and `hr-time-actions.test.ts` green; HR and web typechecks green; targeted Biome check green.

## Phase 7 exit criteria

* Leave ESS/MSS workflow available.
* Calendar resolution green.
* Attendance import deterministic.
* Timesheet approval complete.
* Overtime authority enforced.
* Leave and time payroll handoffs tested.
* All leave/time mutations registered.

---

# Phase 8 — Compensation, benefits and payroll boundary

## Objective

Complete approved compensation facts without moving payroll calculations into HR.

## Slice 8.1 — Dedicated compensation audit — **DONE** (2026-07-26)

Domain-depth audit (not emission reclassification):

* Pack: [`cluster-c/`](erp/human-resources-enterprise-audit/cluster-c/) (scope · findings · matrix · conflicts · repair readiness)
* Executive scorecard: [`35-compensation-performance-learning-cluster.md`](erp/human-resources-enterprise-audit/35-compensation-performance-learning-cluster.md)
* Emission matrix (Phase 3, frozen): [`HR-AUD-06-COMPENSATION-PERFORMANCE-LEARNING.md`](erp/human-resources-enterprise-audit/HR-AUD-06-COMPENSATION-PERFORMANCE-LEARNING.md)
* Five-axis scores: compensation **Partial** · performance **Partial** · learning **Partial** (was Unaudited on all axes)

Verify (2026-07-26):

```bash
pnpm --filter @afenda/human-resources typecheck
pnpm --filter @afenda/human-resources test -- compensation performance learning
```

Exit 0 — typecheck green; **70** tests passed, **14** drizzle parity skipped (no `DATABASE_URL` in session).

**Residual reconciliation (2026-07-28):** the earlier Drizzle, exact-money, sensitive-runner and product-composition residuals are closed by the current live parity, Phase 2 enforcement and Phase 11 evidence.

## Slice 8.2 — Compensation grades and bands — **DONE / LOCALLY VERIFIED** (2026-07-28)

Complete:

* grade;
* band;
* range minimum;
* midpoint;
* maximum;
* currency;
* effective history;
* progression rules.

**Status:** package commands, Memory/Drizzle adapters and Actions are present. Exact decimal range ordering avoids JavaScript safe-integer collapse. The approved database ledger is applied through **0032**, and the full live parity lane is green.

## Slice 8.3 — Compensation agreement — **DONE** (2026-07-26)

**Status: DONE** — employment-scoped employee compensation agreement lifecycle in `@afenda/human-resources`, with its database shape present on the approved branch through migration **0032**.

| Control | Evidence |
|--------|----------|
| Lifecycle `draft → scheduled → active → ended \| superseded` | `employee-compensation-lifecycle.ts` · memory + drizzle adapters |
| Commands create/amend/approve/schedule/activate/correct/end + get/list | `compensation-benefits/employee-compensation.ts` · manifest + emissions |
| `payFrequency` · confidential projection · lineage (`supersedesCompensationId`) | `schemas/compensation.ts` · `employee-compensation-projection.ts` · approved database ledger through **0032** |
| `applyApprovedCompensationResult` (8.4) stays direct **active** | `employee-compensation-lifecycle.ts` (`memoryNewEmployeeCompensationFromReview`) |
| Memory/drizzle parity | `adapters/memory/employee-compensation-lifecycle.ts` · `adapters/drizzle/employee-compensation-lifecycle-drizzle.ts` |

**Verify (2026-07-26):**

```text
pnpm --filter @afenda/human-resources typecheck
pnpm --filter @afenda/human-resources test -- compensation
```

Exit 0 — typecheck green; **25** compensation tests passed (**2** drizzle parity skipped without `DATABASE_URL`).

**Residual reconciliation (2026-07-28):** schedule/activate/correct paths and Slice **8.9** Actions are covered by the current green lanes. Controlled module-readiness and external certification remain Phase 13 gates.

## Slice 8.4 — Compensation review — **DONE / LOCALLY VERIFIED** (2026-07-28)

Complete:

* review cycle;
* proposal;
* budget check;
* manager recommendation;
* approval;
* effective compensation result;
* audit.

**Status:** review-cycle/proposal/recommendation/approval/result flows and product Actions are present. Budget and increase arithmetic use exact decimal-string operations. The approved database ledger is applied through **0032**, and the full live parity lane is green.

## Slice 8.5 — Benefits — **DONE** (2026-07-26)

**Status: DONE** — benefit plan, eligibility, enrollment, waiver, dependent coverage, effective range, and contribution facts are present in `@afenda/human-resources` and on the approved database branch through migration **0032**.

| Control | Evidence |
|--------|----------|
| Benefit plan create/update/archive | `compensation-benefits/benefit-plan.ts` · memory + drizzle adapters |
| Eligibility (`minTenureDays` + `allowedEmploymentStatuses`) | `benefit-eligibility.ts` · `shared/benefit-guards.ts` · `hr_benefit_eligibility` |
| Enrollment with employee/employer contribution + frequency | `benefit-enrollment.ts` · enrollment money columns |
| Waiver (`status: waived` + `waiverReason`) | `waiveBenefit` · unique index active+waived mutex |
| Dependent coverage child rows | `benefit-dependent.ts` · `hr_benefit_enrollment_dependent` |
| Effective range guards | `shared/benefit-guards.ts` `assertEffectiveRange` |
| Manifest + emissions + no `@afenda/payroll` | `module.manifest.ts` · `emissions/domains/compensation.ts` · unit guard test |
| Memory/drizzle parity | `human-resources.compensation-benefits.parity.test.ts` (`benefit eligibility, enrollment, waiver, and dependents`) |

**Verify (2026-07-26):**

```bash
pnpm --filter @afenda/human-resources typecheck
pnpm --filter @afenda/human-resources test -- compensation
```

Exit 0 — typecheck green; **26** compensation tests passed (**3** drizzle parity skipped without `DATABASE_URL`).

**Residual reconciliation (2026-07-28):** Slice **8.9** Actions and Phase 11 composition are closed. Payroll calculation correctly remains in `@afenda/payroll`.

## Slice 8.6 — Allowance and deduction ownership — **DONE** (2026-07-26)

**Status: DONE** — four-way ownership codified in Scratch SSOT; package README and mutation surfaces aligned.

| Layer | Owner |
|-------|-------|
| Entitlement / agreement | `@afenda/human-resources` |
| Calculation | `@afenda/payroll` |
| Posting | `@afenda/accounting` |
| Disbursement | `@afenda/payments` |

**Authority:** [`allowance-deduction-ownership.md`](erp/allowance-deduction-ownership.md) (single authoritative statement — all package READMEs link here).

**Out of scope this slice:** Slice **8.7** handoff contract · Slice **8.8** parsing parity · `hr_allowance_entitlement` domain commands (table registered; commands forward).

**Verify (2026-07-26):**

```bash
pnpm validate:modules:write
pnpm governance:packages
pnpm --filter @afenda/human-resources typecheck
pnpm --filter @afenda/payroll typecheck
```

## Slice 8.7 — Payroll handoff contract — **DONE** (2026-07-26)

Publish one shared contract with:

* organization;
* employee;
* employment;
* assignment;
* effective date;
* currency;
* amount;
* decimal scale;
* rounding mode;
* pay frequency;
* compensation components;
* leave facts;
* time facts;
* overtime facts;
* source version;
* approval evidence.

**Evidence:** the approved HR handoff producer and payroll-owned parser share the decimal-string/effective-date contract without moving gross-to-net calculation into HR.

## Slice 8.8 — Payroll handoff parity — **DONE** (2026-07-26)

Test:

```text
HR approved compensation
→ payroll input parser
→ exact decimal preservation
→ exact effective-date preservation
```

**Evidence:** payroll parser and schema-constraint tests cover exact decimal scale, rounding mode, approval evidence and effective-date preservation.

## Slice 8.9 — Compensation Actions

**Status:** DONE (2026-07-26)

Create:

* `hr-compensation.ts` — 31 actions (grades, bands, progression, employee compensation lifecycle, proposals, payroll handoff)
* `hr-benefits.ts` — 11 actions (plan, eligibility, enrollment, dependents)
* `hr-compensation-review.ts` — 12 actions (review cycles + review workflow)
* `hr-action-runner.ts` — shared permission → Zod → session stamp → package delegate envelope

**Out of scope (store-only package surface):** `getBenefitPlan` / `listBenefitPlans` — no public package commands → no Server Actions.

Verify:

```bash
pnpm --filter @afenda/web test -- hr-compensation hr-benefits hr-compensation-review
pnpm --filter @afenda/web typecheck
```

## Phase 8 exit criteria — **MET AT LOCAL IMPLEMENTATION LAYER** (2026-07-28)

* Compensation domain audited.
* Approved compensation history complete.
* Benefits enrollment complete.
* Payroll handoff contract published.
* HR and payroll parsing parity green.
* No gross-to-net logic inside HR.

---

# Phase 9 — Performance, learning and talent

## Objective

Complete development, evaluation and succession capabilities.

**Status (2026-07-28): DONE / LOCALLY VERIFIED.** Performance cycles, goals, reviews, calibration, improvement plans, learning catalog/session/assignment/completion/certification, competency, talent profile/pool, career and succession command surfaces and product Actions are present. Unit and full live parity evidence is green, including career/succession scenarios. External performance and privacy certification remains Phase 13 work.

## Slice 9.1 — Performance audit

Verify:

* cycle;
* goal;
* review;
* rating;
* calibration;
* improvement plan;
* confidentiality;
* effective history;
* parity.

## Slice 9.2 — Performance cycles

Complete:

* draft;
* publish;
* open;
* close;
* review periods;
* eligible population;
* rating scale.

## Slice 9.3 — Goals

Complete:

* employee goal;
* manager goal;
* weight;
* progress;
* alignment;
* completion;
* evidence.

## Slice 9.4 — Performance reviews

Complete:

* self-review;
* manager review;
* reviewer chain;
* calibration;
* acknowledgement;
* final rating;
* field projection.

## Slice 9.5 — Improvement plans

Complete:

* initiate;
* objectives;
* milestones;
* review;
* complete;
* fail;
* extend;
* evidence.

## Slice 9.6 — Learning audit

Verify:

* course;
* session;
* assignment;
* completion;
* certification;
* expiry;
* parity;
* production Actions.

## Slice 9.7 — Learning management

Complete:

* course catalog;
* session;
* instructor;
* capacity;
* assignment;
* attendance;
* completion;
* certification;
* renewal;
* expiration.

## Slice 9.8 — Talent unit depth

### Mission

`HR-ENT-TALENT-UNIT-TESTS`

Add dedicated unit tests for:

* competency;
* assessment;
* profile;
* pool;
* career;
* succession;
* readiness staleness.

## Slice 9.9 — Competency management

Complete:

* competency library;
* job competency;
* employee assessment;
* proficiency;
* evidence;
* expiration.

## Slice 9.10 — Talent profiles and pools

Complete:

* profile;
* potential;
* mobility;
* critical-role readiness;
* talent pool;
* membership;
* review.

## Slice 9.11 — Career and succession

Complete:

* career plan;
* development actions;
* succession plan;
* candidates;
* readiness;
* approval;
* stale-readiness prevention.

## Slice 9.12 — Product Actions

Create:

* `hr-performance.ts`
* `hr-learning.ts`
* `hr-talent.ts`
* `hr-succession.ts`

## Phase 9 exit criteria — **MET AT LOCAL IMPLEMENTATION LAYER** (2026-07-28)

* Performance and learning audits complete.
* No scaffold-only aggregate claimed complete.
* Confidential reviews protected.
* Certifications historically durable.
* Talent unit and parity tests green.
* Product Actions available.

---

# Phase 10 — Compliance, employee relations and workforce planning

## Objective

Complete governance and workforce-risk operations.

## Slice 10.1 — Document requirements

Complete:

* requirement;
* applicability;
* publication;
* retirement;
* employee-document registration;
* verification;
* rejection;
* expiry.

## Slice 10.2 — Work eligibility

Complete:

* record;
* verify;
* suspend;
* renew;
* close;
* nearing-expiry detection;
* expired status;
* evidence reference.

## Slice 10.3 — Compliance expiry operations

### Mission

`HR-ENT-COMPLIANCE-EXPIRY-OPS`

Implement scheduled detection for:

* expiring documents;
* expiring work eligibility;
* overdue policy acknowledgement;
* expiring certifications.

## Slice 10.4 — Platform document boundary

### Mission

`HR-ENT-09-PLATFORM-DOCUMENT-BOUNDARY`

Define:

* HR stores document references only;
* platform owns binary storage;
* platform owns scanning;
* platform owns download authorization;
* retention is coordinated with HR privacy classification.

## Slice 10.5 — Employee Relations product slice

Complete:

* open case;
* list authorized cases;
* assign investigator;
* case event;
* case action;
* evidence;
* appeal;
* close and reopen;
* projected subject view.

## Slice 10.6 — Workforce plans

Complete:

* plan;
* line;
* approval;
* reservation;
* release;
* consume;
* availability;
* period;
* organization scope.

## Slice 10.7 — Workforce variance actuals

### Mission

`HR-ENT-WFP-VARIANCE-ACTUALS`

Calculate:

```text
approved headcount plan
versus
active employment and assignment facts
```

Do not label consumed reservations as actual headcount.

## Slice 10.8 — Governance product Actions

### Mission

`HR-ENT-12-GOV-PRODUCT-SLICE`

Create:

* `hr-compliance.ts`
* `hr-employee-relations.ts`
* `hr-workforce-planning.ts`

## Phase 10 exit criteria

**Implementation status (2026-07-28): DONE / LOCALLY VERIFIED.** Slices **10.1–10.8** are implemented in `@afenda/human-resources` and `@afenda/web`; applicability, expiry/overdue semantics, platform object-policy boundaries and workforce-variance Memory/Drizzle behavior are covered by the current green unit and live parity lanes.

* Compliance expiry proactive — `detectComplianceExpiryOperations` aggregates expiring employee documents, work eligibility risks, outstanding policy acknowledgements and expiring certifications.
* Employee Relations workflow usable — governance Server Actions delegate through the HR action runner with employee-case permissions.
* Document ownership clear — HR command options can resolve platform document objects through the vault-reference adapter while HR persists immutable references only.
* Workforce variance employment-backed — approved plan variance compares planned lines against active employment/assignment facts and returns actual FTE/headcount separately from reservation availability.
* Sensitive governance Actions protected — compliance, employee-relations and workforce-planning Actions use permission gate → Zod parse → session stamp → package delegate → `ActionResult`.
* Governance parity suites — Memory and Drizzle paths are green; workforce cleanup remains child-first.
* Compliance semantics — structured applicability, Memory/Drizzle no-filter behavior, eligibility expiry states, acknowledgement due dates, document-boundary enforcement and workforce-variance parity are locally verified.

Evidence captured:

* `pnpm --filter @afenda/human-resources test -- compliance workforce-planning learning vault-document-reference sensitive-domain-policies` — **PASS** (111 passed, 17 skipped).
* `pnpm --filter @afenda/web test -- hr-compliance hr-employee-relations hr-workforce-planning` — **PASS** (3 passed).
* `pnpm --filter @afenda/human-resources typecheck` — **PASS**.
* `pnpm validate:modules` — **PASS**.
* `pnpm audit:tenancy-nulls` — **PASS**.
* `pnpm lint` — **PASS** (2026-07-26; Turbo **35/35** lint tasks successful after import and non-null assertion cleanup).
* `pnpm typecheck` — **PASS** (2026-07-26; Turbo **35/35** typecheck tasks successful).
* Current consolidated evidence (2026-07-28): HR unit **961 passed / 1 skipped**; full live parity **37 files / 368 passed / 1 skipped**; package/web/events/database typecheck lanes **PASS**.

---

# Phase 11 — Product composition and user journeys

## Objective

Turn package capability into usable HRM workflows.

**Status (2026-07-28): DONE / LOCALLY VERIFIED.** The Admin, ESS, MSS, recruitment, HR operations and compensation journeys compose package capabilities without duplicating domain rules. The Phase 11 focused web lane records **52 passed tests**, including negative authorization and standard Action-envelope coverage.

Composition evidence:

* [`hr-admin-journeys.ts`](../../apps/web/app/actions/hr-admin-journeys.ts)
* [`hr-self-service-journeys.ts`](../../apps/web/app/actions/hr-self-service-journeys.ts)
* [`hr-manager-journeys.ts`](../../apps/web/app/actions/hr-manager-journeys.ts)
* [`hr-recruitment.ts`](../../apps/web/app/actions/hr-recruitment.ts)
* [`hr-operations.ts`](../../apps/web/app/actions/hr-operations.ts)
* [`hr-compensation.ts`](../../apps/web/app/actions/hr-compensation.ts)

Do not create one Action per package command. Compose business journeys.

## Slice 11.1 — HR administration workspace

Provide:

* employee directory;
* employee record;
* employment;
* assignment;
* organization;
* lifecycle timeline;
* compliance alerts.

## Slice 11.2 — Employee self-service

Provide:

* profile;
* leave;
* attendance;
* timesheet;
* learning;
* goals;
* documents;
* acknowledgements.

## Slice 11.3 — Manager self-service

Provide:

* team directory;
* leave approvals;
* timesheet approvals;
* attendance exceptions;
* probation;
* performance;
* talent;
* staffing gaps.

## Slice 11.4 — Recruitment workspace

Provide:

* requisitions;
* pipeline;
* interviews;
* offers;
* candidate consent;
* hire conversion.

## Slice 11.5 — HR operations workspace

Provide:

* onboarding;
* transfer;
* termination;
* offboarding;
* compliance;
* cases;
* workforce plans.

## Slice 11.6 — Compensation workspace

Provide:

* grades;
* bands;
* reviews;
* benefits;
* employee compensation history;
* payroll handoff status.

## Slice 11.7 — Standard Action contract

Every Action must:

* use the package command;
* use the standard permission runner;
* supply actor context;
* supply organization context;
* supply correlation ID;
* supply idempotency key;
* map `Result` to `ActionResult`;
* avoid domain-rule duplication;
* invalidate only relevant paths.

## Phase 11 exit criteria — **MET AT LOCAL IMPLEMENTATION LAYER** (2026-07-28)

* Core HR workflows usable.
* ESS and MSS available.
* No package-domain rule duplicated in apps/web.
* Negative authorization tests exist.
* Product routes align with permission catalog.
* Product Score reaches at least 75.

---

# Phase 12 — Reporting, integrations and bulk operations

## Objective

Complete enterprise interoperability and decision support.

**Status (2026-07-28): DONE / LOCALLY VERIFIED.** Reconciled read models, resumable/import replay, permission-aware exports, workflow/search projections, durable payroll delivery, accounting/provisioning facts and recovery workers are composed. The combined Phase 12–13 focused web lane records **64 passed tests**.

Composition and worker evidence:

* [`hr-reporting-bulk.ts`](../../apps/web/app/actions/hr-reporting-bulk.ts) — reporting and bulk-import Actions
* [`human-resources-reporting-bulk-worker.ts`](../../apps/web/lib/erp/human-resources-reporting-bulk-worker.ts) — resumable reporting/import worker
* [`hr-bulk-export.ts`](../../apps/web/app/actions/hr-bulk-export.ts) — authorization- and privacy-aware export Actions
* [`human-resources-bulk-export-worker.ts`](../../apps/web/lib/erp/human-resources-bulk-export-worker.ts) — durable export worker
* [`human-resources-bulk-export-registry.ts`](../../apps/web/lib/erp/human-resources-bulk-export-registry.ts) — allowlisted export registry
* migration [`0030_hr_payroll_handoff_delivery.sql`](../../packages/data-plane/db/drizzle/0030_hr_payroll_handoff_delivery.sql) — durable payroll delivery
* migration [`0031_platform_work_items.sql`](../../packages/data-plane/db/drizzle/0031_platform_work_items.sql) — platform workflow facts
* migration [`0032_hr_bulk_reliability_durability.sql`](../../packages/data-plane/db/drizzle/0032_hr_bulk_reliability_durability.sql) — bulk checkpoints, export jobs and connector cursors

## Slice 12.1 — HR read models

Create read projections for:

* workforce headcount;
* turnover;
* hiring;
* leave;
* attendance;
* overtime;
* compensation;
* compliance;
* learning;
* performance;
* succession;
* workforce plan variance.

## Slice 12.2 — Bulk import framework

Support:

* dry run;
* row validation;
* partial rejection;
* idempotent replay;
* source reference;
* batch status;
* downloadable error file;
* audit trail.

Start with:

* employees;
* assignments;
* leave entitlements;
* attendance;
* compensation;
* learning assignments.

## Slice 12.3 — Bulk export

Support:

* permission-aware export;
* field projection;
* tenant isolation;
* date range;
* effective date;
* privacy logging.

## Slice 12.4 — Platform workflow integration

Project facts for:

* approvals;
* tasks;
* reminders;
* escalations;
* expiry notifications;
* onboarding;
* offboarding.

## Slice 12.5 — Search integration

Index only authorized, projection-safe data.

## Slice 12.6 — Payroll integration

Support:

* approved compensation;
* approved leave;
* approved time;
* overtime;
* corrections;
* replay;
* acknowledgement;
* rejection feedback.

## Slice 12.7 — Accounting and provisioning integration

Support facts for:

* payroll posting;
* cost-centre allocation;
* headcount budgeting;
* access provisioning;
* equipment assignment.

## Phase 12 exit criteria — **MET AT LOCAL IMPLEMENTATION LAYER** (2026-07-28)

* Reporting projections reconciled.
* Bulk imports support dry-run and replay.
* Exports honor privacy and authorization.
* Payroll handoffs acknowledged.
* Integration failures recoverable.
* Search contains no unauthorized sensitive fields.

---

# Phase 13 — Operational readiness and enterprise certification

## Objective

Convert a functionally complete HRM into a production-ready enterprise module.

**Status (2026-07-28): LOCAL IMPLEMENTATION COMPLETE / EXTERNAL CERTIFICATION OPEN.** HR-specific metrics, bounded retries/dead letters, security/privacy verification kernels, eight benchmark workloads, recovery harnesses and operator runbooks are implemented and locally tested. They have **not** received external security/privacy approval, representative production-scale performance certification or signed recovery-drill certification. The module remains `scaffolded`; this Scratch plan does not authorize lifecycle promotion.

Implementation evidence:

* [`src/performance-verification/`](../../packages/erp/human-resources/src/performance-verification/) — eight workload definitions, thresholds and harness
* [`src/recovery-verification/`](../../packages/erp/human-resources/src/recovery-verification/) — recovery decisions, drills and harness
* [`44-operational-recovery-runbooks.md`](erp/human-resources-enterprise-audit/44-operational-recovery-runbooks.md) — migration, outbox, payroll, attendance, privacy, correction, leakage and rollback runbooks
* [`47-current-implementation-evidence-and-dual-scores.md`](erp/human-resources-enterprise-audit/47-current-implementation-evidence-and-dual-scores.md) — current measured evidence, scores and remaining certification gates

## Slice 13.1 — Observability

Measure:

* command success and failure;
* authorization denials;
* privacy operations;
* outbox lag;
* event failures;
* parity failures;
* connector health;
* bulk-import errors;
* payroll handoff failures.

## Slice 13.2 — Reliability

Implement:

* retry policy;
* dead-letter handling;
* idempotent replay;
* connector cursor recovery;
* partial outage behavior;
* transaction failure recovery.

## Slice 13.3 — Security verification

Complete:

* cross-tenant penetration tests;
* sensitive-field tests;
* privilege escalation tests;
* export authorization tests;
* document-reference tests;
* audit tamper tests.

## Slice 13.4 — Privacy verification

Complete:

* subject export;
* retention;
* legal hold;
* anonymization;
* deletion decision;
* audit evidence;
* processor and subprocessor boundaries.

## Slice 13.5 — Performance verification

Test:

* employee lists;
* case lists;
* timesheet generation;
* attendance import;
* bulk employee import;
* workforce variance;
* payroll handoff;
* large tenant isolation.

## Slice 13.6 — Recovery and runbooks

Document:

* failed migration;
* stuck outbox;
* failed payroll handoff;
* failed attendance connector;
* privacy incident;
* data correction;
* tenant leakage response;
* rollback.

## Slice 13.7 — Documentation and module status

Update:

* README;
* architecture boundaries;
* package consumption;
* permission catalog;
* event catalog;
* privacy contract;
* payroll contract;
* operational runbooks;
* module lifecycle.

## Phase 13 exit criteria — **IMPLEMENTATION EVIDENCED; CERTIFICATION NOT MET** (2026-07-28)

* No open P0 or P1 production blocker.
* Full tests and DB parity green.
* Product Score at least 85.
* Coding Score at least 90.
* Security sign-off complete.
* Privacy sign-off complete.
* Performance targets met.
* Recovery runbooks tested.
* Module status can move from `scaffolded` to `active`.

---

# Master execution order

| Order | Phase    | Outcome                                    |
| ----: | -------- | ------------------------------------------ |
|     1 | Phase 0  | Authority and decisions frozen             |
|     2 | Phase 1  | Full engineering baseline green            |
|     3 | Phase 2  | Authorization and privacy complete         |
|     4 | Phase 3  | Audit and emission integrity complete      |
|     5 | Phase 4  | Historical and database truth complete     |
|     6 | Phase 5  | Workforce foundation product-complete      |
|     7 | Phase 6  | Recruitment and lifecycle complete         |
|     8 | Phase 7  | Leave and time complete                    |
|     9 | Phase 8  | Compensation and payroll boundary complete |
|    10 | Phase 9  | Performance, learning and talent complete  |
|    11 | Phase 10 | Compliance, ER and WFP complete            |
|    12 | Phase 11 | Product journeys complete                  |
|    13 | Phase 12 | Reporting and integrations complete        |
|    14 | Phase 13 | Enterprise production certification        |

---

# Non-negotiable execution rules

1. One slice per implementation mission.
2. No unrelated cleanup inside a domain mission.
3. Every mission records exact exit codes.
4. Unit tests precede database parity tests.
5. No domain is marked complete without Memory/Drizzle parity.
6. No sensitive surface is complete without field projection tests.
7. No mutation is complete without audit/emission classification.
8. No product Action may duplicate package business logic.
9. No payroll calculation is implemented inside HR.
10. No enterprise certification claim from local implementation evidence alone (Phase 8/9 domain depth and product composition are closed; external Phase 13 controls remain mandatory).
11. No required parity suite may silently skip.
12. Documentation must be updated when a mission closes.

---

# Final completion gates

## Gate 1 — Safe architecture

Required after Phase 4:

* no P0 safety defects;
* privacy composed;
* authorization unified;
* 100% mutation classification — **DONE** (current table register **136/136** and emission register **348/348**, reconciled 2026-07-28);
* full package green;
* complete table classification — **DONE** (current register **136/136**, reconciled 2026-07-28).

## Gate 2 — Functional HRM

Required after Phase 10:

* all HR domains complete;
* compensation and payroll boundary complete;
* ESS and MSS package capabilities complete;
* governance and workforce planning complete.

## Gate 3 — Enterprise HRM

Required after Phase 13:

* Product Score ≥ 85;
* Coding Score ≥ 90;
* no P0/P1 blockers;
* complete product journeys;
* integrations operational;
* security and privacy approved;
* observability and recovery proven.

**Execution starts with Phase 0 and proceeds in the stated order.**
