# Surfaces (R2)

Category folder for Rank-2 surface packages. It organizes the repository only; it is not a dependency or ownership unit. Do not publish or import `@afenda/surfaces`.

| Package | Published name |
|---------|----------------|
| [`ui-system`](./ui-system/README.md) | `@afenda/ui-system` |
| [`emails`](./emails/README.md) | `@afenda/emails` |

Import by published name only (`@afenda/<name>`) or a declared package export subpath. Catalog authority: [packages/README.md](../README.md).

## Unregistered folders

| Folder | Package name | Status |
|--------|--------------|--------|
| [`ui-blocks`](./ui-blocks/README.md) | `@afenda/ui-blocks` | On disk; not a governed catalog entry until `pnpm governance:packages` passes with its catalog and edge updates |
