/** Aggregate boundary marker — commands ship in a later slice. */
export const HUMAN_RESOURCES_AGGREGATE_BENEFIT_PLAN = "benefit-plan" as const;
export type HumanResourcesBenefitPlanAggregate =
	typeof HUMAN_RESOURCES_AGGREGATE_BENEFIT_PLAN;
