/**
 * Compatibility facade for the domain-sliced Human Resources persistence contracts.
 *
 * Prefer importing a focused contract from `./store/<domain>` in domain-owned code.
 * Existing imports from `./store` remain valid through these re-exports.
 */
export * from "./store/index";
