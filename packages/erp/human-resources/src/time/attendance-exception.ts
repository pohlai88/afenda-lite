/** Aggregate boundary marker — commands ship in a later slice. */
export const HUMAN_RESOURCES_AGGREGATE_ATTENDANCE_EXCEPTION =
	"attendance-exception" as const;
export type HumanResourcesAttendanceExceptionAggregate =
	typeof HUMAN_RESOURCES_AGGREGATE_ATTENDANCE_EXCEPTION;
