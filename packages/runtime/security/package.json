{
	"name": "@afenda/security",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"lint": "biome check .",
		"typecheck": "tsc --noEmit -p tsconfig.json && pnpm run typecheck:contract",
		"typecheck:contract": "tsc --noEmit -p tsconfig.contract.json",
		"test": "vitest run --config ../../../testing/vitest.unit.config.ts --project security"
	},
	"devDependencies": {
		"@afenda/config": "workspace:*",
		"@types/node": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
